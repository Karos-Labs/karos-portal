{
 "verdicts": {
  "85": {
   "n": 85,
   "verdict": "fixed",
   "evidenceNow": "src/components/correct-info-modal.tsx:134-137",
   "detail": "The price is now disclosed before the charge: the hint paragraph renders \"Costs {correctionPricing.cost} credits.\" (correct-info-modal.tsx:134-136), and a shortfall renders a warning strip with the server's own refusal line (139-144) while disabling Apply Correction (`shortfall` at :58, used in handleSubmit :62 and the button's disabled at :171). The gate is resolved server-side, not in the browser: src/app/(app)/layout.tsx:142-149 builds `correctionPricing` only when `isBillableClientActor(user)` and attaches `blockReason: creditBlockReason(credits, CREDIT_COSTS.targetedCorrection)` when spendable < 2, so staff and admins in View-as-Client see no price (they are not charged) and the modal cannot invent a different limit than the charge would cite. Threading is complete and mounted: layout.tsx:170 -> client-rail.tsx:166 and :268 (mobile sheet) -> client-documents.tsx:607/1141 -> the CorrectInfoModal at client-documents.tsx:604. The staff shell (sidebar.tsx:545/668) deliberately passes nothing, which is correct.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "101": {
   "n": 101,
   "verdict": "fixed",
   "evidenceNow": "src/components/linkedin-seats-workspace.tsx:239-253, 261-304, 106-121",
   "detail": "The trash icon no longer deletes: its onClick only arms the row (`setConfirmingId(seat.id)`, :241-244) and a danger-toned confirm strip appears beneath it (:261-304) naming the employee, saying the sign-in goes with the seat, that removal is not refunded, and \u2014 when the roster is over plan \u2014 that re-adding is a one-time 100-credit charge, plus a \"pause instead\" escape. Buttons are \"Yes, remove seat\" / \"Keep it\". The re-add test is arithmetically right: `reAddCharges = seats.length > seatLimit` (:67), which is exactly when `seats.length - 1 >= seatLimit` and evaluateSeatAddition (credits.ts:116) would charge again. Failure handling landed too: remove() wraps the call in try/catch, checks `!res.ok`, and writes into `removeError` rendered inside the strip (:301). `SeatActionResult` (seat-actions.ts:23-25) is a union carrying `error`, so the res.ok branch typechecks. Only the optional half of the instruction (storing the charged amount per seat / refunding on removal) was skipped; the live-count derivation makes the confirm text accurate anyway.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "102": {
   "n": 102,
   "verdict": "fixed",
   "evidenceNow": "src/components/credits-panel.tsx:146 and 227-237; src/app/(app)/layout.tsx:135,169; src/components/client-rail.tsx:198,240",
   "detail": "All three surfaces now render the same spendable figure. layout.tsx:135 computes `availableCredits(credits)` and passes it as `spendableCredits` (:169); client-rail.tsx renders it in the desktop pill (:198) and the mobile pill (:240, aria-label \"N credits remaining\"). credits-panel.tsx:146 calls the same helper and makes it the headline over \"credits available\" (:227-228), with the raw balance demoted to \"{balance} on balance\" and shown only when the two differ (:235-236). No component renders a raw `credits.balance` as an availability figure any more (grep for `.balance` in src/app + src/components returns only the panel's secondary line and the admin grant notice). The Agents page was left alone as instructed and still uses the clipped figure (clients/[id]/agents/page.tsx:97,144), as do /clients (page.tsx:20) and admin analytics (page.tsx:48). Omitting `now` in the panel/layout calls is safe: getClientCredits rolls the windows on read in the same request.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "103": {
   "n": 103,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/settings/page.tsx:224-259; src/components/settings-tabs.tsx:28-77; src/components/integrations-tab.tsx:146-222",
   "detail": "The IA defect is gone. The page now builds six section variables and hands them to SettingsTabs (settings/page.tsx:224-231, mounted at :259) \u2014 Profile / Channels / Credits / Automation / Meetings / Team \u2014 with null-content tabs filtered out, so a client is never shown an empty tab. The active tab lives in `?tab=` (settings-tabs.tsx:36-40, seeded server-side from searchParams at page.tsx:51), so it is linkable. The PageHeader description was rewritten to name credits, channels, automation, meetings and teammates (:237). The sign-out-only Account card is deleted (:257-258). Inside the channels tab, ChannelSection (integrations-tab.tsx:146-222) partitions on a three-way `platformStatus` (live / needs-reconnect / absent, :1017-1021): live and needs-reconnect keep full cards, only never-connected platforms collapse into an \"Add a channel\" row list that expands in place \u2014 a better call than the two-way `integrationIsUsable` split the instruction asked for, since it keeps an expired channel's Reconnect badge on screen. No role gate shifted in the refactor (Profile still isStaff, agent access and scheduled runs still isAdmin; Team is now MORE gated, isStaff || isGroupAdmin). Residual from the finding's \"also reported as\" copy note: settings-form.tsx card titles were lowercased (:114,127,195,277,312) but the tab labels \"Profile Information\"/\"Account Security\" (settings-form.tsx:398-399), \"Company Employee Roster\"/\"Add Employee Seat\" (linkedin-seats-workspace.tsx:129,142), \"Manage Employee Seats\" (integrations-tab.tsx:548), the ALL-CAPS `{seat.status.toUpperCase()}` badge (linkedin-seats-workspace.tsx:228) and \"Contact Support\" (contact-us-modal.tsx:121) are still Title Case/shouting on client-facing surfaces.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Confirm on a phone that a CLIENT_USER has no sign-out anywhere (see newBugDetail) \u2014 this needs a product decision on where to put it back, and it is a real lockout-shaped regression, not a cosmetic one.",
   "newBug": true,
   "newBugDetail": "Deleting the Account card removed the ONLY sign-out a client user can reach at phone width. The fix instruction's justification (\"LogoutButton already renders in the rail's AccountMenu\") is true only on desktop: AccountMenu is mounted at client-rail.tsx:209, inside the `hidden ... md:block` aside (client-rail.tsx:102). The mobile Company sheet (client-rail.tsx:280-320) has Settings, Team, the bell, Contact us and the theme switch \u2014 no LogoutButton. The staff shell does have one in its mobile sheet (sidebar.tsx:739), and /settings (settings-form.tsx) has no sign-out either, so grep confirms LogoutButton is reachable for a CLIENT_USER only via account-menu.tsx:99. At ddcef3e the deleted card was that path (the sheet links to Settings), so this is a regression introduced by faithfully implementing a wrong clause of the instruction."
  },
  "104": {
   "n": 104,
   "verdict": "fixed",
   "evidenceNow": "src/components/credits-panel.tsx:98-103, 259-281, 290-318",
   "detail": "UsageMeter gained an optional `resetNote` rendered under each bar and coloured warning at 100% (:98-103), fed from the new CREDIT_WINDOW_RESET constants (credits.ts:259-262) which CREDIT_BLOCK_REASON is now composed from (credits.ts:264-268) \u2014 so the meter note and the denial line cannot drift. Both meters pass it (:246,252). A \"What actions cost\" <details> table renders from CREDIT_COSTS + TASK_EXECUTION_COSTS (PRICE_ROWS :40-59, rendered :259-281). The cap wall now explains itself: the strip at :290-318 fires on `blocked = spendable < CREDIT_COSTS.chatMessage` (:151-152) rather than the instruction's `pct >= 100` \u2014 strictly better, because it also catches a balance shortfall, and it names the limit via bindingCreditLimit at the cheapest billable action (:153-154), i.e. the same ladder assessCharge uses, so it cannot tell a capped client to top up. It says what is paused and offers ContactUsButton variant=\"row\" labelled \"Request more credits\" / \"Ask about your limit\" (:302-315), with viewer name/email threaded from settings/page.tsx:127. Copy is sentence case with em dashes. Minor oddity, not a defect: `viewer` is passed for staff/admin too, so an admin viewing a blocked client sees \"Request more credits\" beside their own grant form.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "105": {
   "n": 105,
   "verdict": "fixed",
   "evidenceNow": "src/components/integrations-tab.tsx:325-344, 346-365, 537-541, 713-731, 893-897; src/lib/actions/integration-actions.ts:77-125",
   "detail": "All three empty catches are gone and each now has somewhere to write. handleAutoPublishToggle keeps the optimistic revert but sets a reason (:332-340); handleDisconnect sets one instead of the old \"revalidation corrects state\" comment (:351-361); handleDisconnectSub sets `subError` (:713-731). He deviated from the instruction deliberately and for the better: rather than reusing `formError` (which renders inside the collapsed admin credentials form, invisible exactly when these controls are used) he added `actionError` rendered in the card's action area (:276, :537-541) and a `subError` slot the Google card never had (:701, :893-897). The role half is fixed at both ends: Disconnect is hidden for non-staff (`{!isClientViewer && ...}`, :522, with isClientViewer derived at :925), and deleteIntegrationAction / setIntegrationAutoPublishAction were converted from throw to data results (integration-actions.ts:77-125) with client-readable refusals \u2014 necessary because Next masks thrown action errors behind a production digest. The Google per-service disconnect is admin-gated anyway (:856). Impersonation is consistent: an admin in View-as-Client is CLIENT_USER on both client and server, so the hidden button matches the server's refusal.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "141": {
   "n": 141,
   "verdict": "fixed",
   "evidenceNow": "src/components/credits-panel.tsx:221-224 and 40-59, 259-281",
   "detail": "The ledger is no longer the only place a price appears. The Credits card subtitle now carries the short list \"Copilot message 1 \u00b7 doc correction 2 \u00b7 agent run from 25\" rendered straight from CREDIT_COSTS (:221-224), backed by the full collapsible rate card built from CREDIT_COSTS + TASK_EXECUTION_COSTS (:40-59, :259-281), so no figure is hand-typed. The panel is not staff-gated (settings/page.tsx:120-129, credits tab shown to all roles; only the grant/limits form is isAdmin), so a client sees it. Point-of-action pricing is now on the doc correction (correct-info-modal.tsx:135), the agent run dialog and cards (custom-agents.tsx:173, 1291-1293) and the seat add button (linkedin-seats-workspace.tsx:153-161). One clause of the instruction is unimplemented: the copilot input has no price on it \u2014 grep for /credit|cost/ in src/components/copilot-dock.tsx returns nothing, so a chat message still costs 1 credit with no figure at the send button (only in the card's list). Everything else in the finding is resolved.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "15": {
   "n": 15,
   "verdict": "fixed",
   "evidenceNow": "src/components/chatbot-widget.tsx:812",
   "detail": "`Message` now carries an optional `display` (chatbot-widget.tsx:26), `send(text, display?)` stores it (196-205), the API history still maps only `{role, content}` (216), and the user bubble renders `msg.display ?? msg.content` (812). The action chips pass `action.label` as the display value \u2014 `onRun(action.trigger, action.label)` at 436 \u2014 so the transcript shows \"Competitor Deep-Dive\" / \"Content Plan\", never the hidden instruction. The three suggestion chips still call `send(prompt)` with no display (546), so the client's own words render verbatim, exactly as the fix asked. `isPersistedMessage` (40-49) validates `display` on restore, so the substitution survives a reload.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "87": {
   "n": 87,
   "verdict": "fixed",
   "evidenceNow": "src/lib/ai/prompts/proactive-assistant.ts:384",
   "detail": "He took the second branch of the fix (declare web access out of scope). Sublabel is now \"Brief on a tracked competitor + counter-strategy tasks\" and the trigger asks for a brief \"built from the tracked competitor data you already hold\" (chatbot-widget.tsx:84-86). Action 2 in the prompt now opens with \"You have NO web search and NO page fetch\u2026 Never ask for a web address, never claim to have opened one, and never write a brief from your own recollection of a brand\", requires naming the tracked competitors, and tells the model to say what is missing when the tracker is thin (proactive-assistant.ts:384-387). The three uncalled builders are gone \u2014 `grep -rn buildCompetitorResearchPrompt|buildBrandAuditPrompt|buildContentDispatchPrompt src` returns nothing. No web tools were added to the chat route (route.ts:714-719 still lists four tools), which is consistent with the copy now shipped.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": true,
   "humanEyesWhy": "One file away, the same defect class survives untouched: proactive-assistant.ts:259 (Scenario B, no social accounts) still instructs \"Perform an external footprint scan using world knowledge about this client's URL, industry, and market position\" \u2014 a confident external scan from model recollection, on the Scan & Refresh path. Out of F87's stated scope but the same credibility risk; someone should decide whether that line goes too."
  },
  "88": {
   "n": 88,
   "verdict": "fixed",
   "evidenceNow": "src/components/chatbot-widget.tsx:828",
   "detail": "Both halves landed. The four actions and the quick-add form were extracted into `ActionChips` (415-466) and `QuickTaskForm` (294-407) and now also render in a strip above the input bar, collapsed behind an \"AI actions\" toggle, whenever `defaultOpen && messages.length > 0` (828-856) \u2014 so reaching an action no longer requires the thread-destroying reset. The transcript persists to sessionStorage under a viewer+client scoped key (36-38, 137, 144-175), capped at 40 messages, written only after a turn settles and never cleared on an empty list; `reset()` removes the key explicitly (177-188). Dock state persists too: `DOCK_STATE_KEY` in localStorage restores `collapsed`/`sheetOpen` (copilot-dock.tsx:15, 79-104), so a reload no longer re-expands a collapsed rail. Sublabel clipping also fixed \u2014 `line-clamp-2` replaces `truncate` (453).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": true,
   "newBugDetail": "Stale-mirror transcript loss across the lg breakpoint. CopilotDock now keeps BOTH widgets permanently mounted (the sheet is hidden with `!sheetOpen && \"hidden\"` at copilot-dock.tsx:181 rather than conditionally rendered as at ddcef3e), and both share one storage key. The restore effect runs once per mount with deps `[storageKey]` (chatbot-widget.tsx:144-158) and there is no `storage`/BroadcastChannel listener anywhere in the file, so the hidden mount never re-reads the key. Sequence: load at \u22651024px with 4 persisted messages \u2014 both mounts restore 4; send two messages in the rail \u2014 storage now holds 6, the sheet still holds 4 in memory; rotate an iPad to portrait (crosses lg) \u2014 the sheet paints its stale 4 and, on the next send, its write-back (162-175) persists that stale list, permanently dropping two messages the client was charged for. The same gap resurrects a cleared thread: `reset()` in the rail removes the key but cannot clear the sibling mount's in-memory copy, which re-persists it on its next send. His \"never clear on empty\" guard (170) covers only the empty-clobber case, not a stale non-empty one.",
   "needsHumanEyes": true,
   "humanEyesWhy": "The stale-mirror loss needs one real device check (rotate a tablet across 1024px mid-conversation, or resize a desktop window) to confirm severity; the code path is unambiguous but the frequency is a product call."
  },
  "89": {
   "n": 89,
   "verdict": "fixed",
   "evidenceNow": "src/components/chatbot-widget.tsx:808",
   "detail": "Both sides of the fix landed. Assistant bubbles render `<div dangerouslySetInnerHTML={{ __html: renderSectionBody(msg.content) }} />` (chatbot-widget.tsx:800-808) and user bubbles keep the plain pre-wrapped span (812); `renderSectionBody` escapes `& < > \"` before formatting (doc-render.ts:266) and gates hrefs through `isSafeHref` (410-414), so model output cannot inject markup. Since ddcef3e the renderer also grew explicit `#`/`##` rules for exactly this caller (doc-render.ts:285-298) and an escaped-marker blockquote rule (387-390), so hash marks and `>` no longer print. Prompt side: `buildCopilotSystemPrompt` now opens with a \"HOW TO WRITE YOUR REPLIES\" block pinning the supported marks \u2014 bold/italics/code/hyphen bullets/numbered lists/blockquotes, no `##` headings, no tables, sentence case (copilot-context.ts:49-54). doc-render.ts carries no `server-only` marker, so importing it into the client component is legitimate.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "90": {
   "n": 90,
   "verdict": "fixed",
   "evidenceNow": "src/components/strategy-war-room.tsx:307",
   "detail": "The footer is now branched on the count. `created > 0` keeps the green \"Consensus reached\" panel plus a \"View task map \u2192\" link (292-306); `created === 0` renders a warning-styled panel headed \"Nothing new to add \u2014 no tasks were created\" with a plain-English cause and next step from `zeroOutcomeExplanation` (307-327, 348-363), which reads the `duplicatesSkipped`/`capSkipped`/`note` now stored in `outcome` state on the persisted event (54-58, 92-96) and names the concrete remedy (\"Approve or complete some tasks, then run this again\"). The `setTimeout(onClose, 1600)` is gone entirely \u2014 the done effect only calls `onComplete()` (178-182), so nothing self-destructs. He deviated from the letter of the fix by dropping the auto-close for `created > 0` as well, deliberately and citing F65 (comment at 175-177); that is better, not worse, since the success panel now has a link worth clicking.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "91": {
   "n": 91,
   "verdict": "fixed",
   "evidenceNow": "src/components/chatbot-widget.tsx:111",
   "detail": "The cheapest-correct branch of the fix, plus more. The chip is now labelled \"Content Plan\" with sublabel \"Propose this week's content plan as ready-to-run tasks\", and the trigger asks for a plan \"I can turn into tasks\" with no \"queue\" claim (chatbot-widget.tsx:108-113). Action 4 in the prompt was renamed to Content Plan and now ends \"`create_tasks` files cards on the task board; it does NOT start an agent. Say so \u2014 the plan is ready to run once a task is moved into In Progress. Never tell the client that runs have been queued, dispatched or started\" (proactive-assistant.ts:394-398). The write path is unchanged (route.ts:662-685 still creates pending cards and reports \"Created N tasks in your task board\"), which is now what the copy says.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": true,
   "humanEyesWhy": "The same prompt file still mandates the word it now forbids: TASK PHRASING STANDARDS at proactive-assistant.ts:321 gives \"Generate and queue 5 LinkedIn posts via [Agent Name]\" as a model task title. Those titles land on the client's task board, so \"queue\" can still reach the client one surface over, and line 321 directly contradicts the new rule at line 398. Pre-existing text, not introduced by this fix, but the contradiction is new and someone should reword 321."
  },
  "92": {
   "n": 92,
   "verdict": "fixed",
   "evidenceNow": "src/lib/campaign-engine.ts:217",
   "detail": "All three parts landed. Console: a `campaign` kind joined the Line union (strategy-war-room.tsx:24), `handleEvent` has its case (101-106), and `ConsoleLine` prints title, theme and \"N extra tasks added to your board\" (395-401). Count: `runSwarm` accumulates `createdTotal = persisted.created + campaign.taskIds.length` and yields it on `done` (agent-swarm.ts:330, 343, 378), and a null/failed campaign is now reported as a Campaign Director console line instead of being dropped silently (351-375). Ceiling: `generateCampaignBundle` runs `getTaskBoardCapacity` + `findDuplicateReason` over the drafts, admitting against `MAX_ACTIVE_TASKS` with accepted pieces joining the dedup pool, and returns null without writing anything (not even the campaign shell) when the anchor is refused (campaign-engine.ts:213-258). Tests pin both halves: campaign-engine.test.ts:151-196 asserts `capSkipped: 2` / `duplicatesSkipped: 1`, agent-swarm.test.ts:244-263 asserts the campaign event and that `done` includes its tasks.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "93": {
   "n": 93,
   "verdict": "fixed",
   "evidenceNow": "src/components/strategy-war-room.tsx:186",
   "detail": "Done differently from the letter of the fix but strictly better. Instead of `closeOnBackdrop={false}`, every dismissal path is intercepted at one point: `requestClose` raises a confirmation while `status === \"running\"` and only calls `onClose` otherwise (186-192), and it is what `Modal` receives (195). Since Modal routes the backdrop click (modal.tsx:99), the global Escape handler (53-56) and the corner \u2715 (118-124) through that same `onClose`, all three are covered \u2014 `closeOnBackdrop={false}` alone would have left Escape lethal. The confirmation names the cost (\"Leaving now discards the run \u2014 nothing is saved\") with \"Keep running\" / \"Discard run\" (252-274), and an explicit \"Cancel run\" control sits in the running footer (275-286). Duration copy added \u2014 \"This takes about a minute; leaving before it finishes discards the run\" (225-229) \u2014 plus a live \"Round n / total\" counter in the header from the already-handled round_start event (61, 78, 219-223).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "94": {
   "n": 94,
   "verdict": "fixed",
   "evidenceNow": "src/components/copilot-dock.tsx:179",
   "detail": "The sheet is now `flex max-h-[70dvh] flex-col` \u2014 capped rather than fixed, so it grows with content to 70dvh instead of the old `h-[35vh]` (copilot-dock.tsx:179; the bottom offset moved to the shared `MOBILE_TAB_BAR_OFFSET_CLASS` = bottom-[54px], md:bottom-0). Both companion trims landed: the greeting is one line (chatbot-widget.tsx:513-517) and `ActionChips` lays out `grid grid-cols-2 gap-2 lg:flex lg:flex-col` (428) \u2014 two-by-two below lg where only the sheet is painted, single column in the 380px desktop rail. On a 812pt phone that leaves roughly 450px of content space against roughly 300px of welcome column, so all four actions and the chip row land above the fold. No resize handle was added, which the fix allowed as an either/or.",
   "agreesWithLedger": true,
   "confidence": "medium",
   "newBug": false,
   "needsHumanEyes": true,
   "humanEyesWhy": "This is a visual claim I verified only by reading classes and doing the height arithmetic. The sheet has no definite height (fixed, left/right/bottom, max-h only), so whether the inner `grow overflow-y-auto` regions scroll correctly rather than overflowing the cap is worth one look on a real phone \u2014 the finding has a screenshot, so a re-shoot is cheap."
  },
  "95": {
   "n": 95,
   "verdict": "fixed",
   "evidenceNow": "src/app/api/clients/[id]/chat/route.ts:203",
   "detail": "The appendix now names both omitted prices and goes further than asked: \"AI agent runs (the Agents page) cost 25 credits per run by default; some agents are priced individually\", followed by this client's own granted agents with their exact `creditCost` overrides (route.ts:186-206), plus \"An extra LinkedIn employee-advocacy seat beyond the plan's limit costs 100 credits, charged once \u2014 it is not a monthly subscription\" (207). The per-agent list comes from `getClientCustomAgents`, which applies the same enabled + `customAgentIds` + repo-slug allowlist the client's Agents page uses (agent-roster.ts:47-68) and the same `creditCost ?? CREDIT_COSTS.customAgentRun` fallback the agent cards render, so the quoted figures match the screen. The headline balance was also switched to `availableCredits(credits)` (195) so the copilot quotes a spendable number.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": true,
   "humanEyesWhy": "He wrote \"per run\" where the fix said \"per output\", and for a manual run he is right (multiplier is 1). But scheduled runs bill `creditCost * outputsPerRun` on every fire (submit-custom.ts:319-322, run-scheduled/route.ts:79), and the appendix never mentions the multiplier while telling the model \"Never invent credit figures beyond these\" \u2014 so a client asking what their scheduled agent costs per week gets an understated number. Worth one line of copy."
  },
  "107": {
   "n": 107,
   "verdict": "fixed",
   "evidenceNow": "src/components/asset-detail-modal.tsx:417 (PublishNowRow, mounted at :369) + src/components/run-calendar.tsx:907-912 (canPublish={canSchedule}, connectedPlatforms from connectedPlatformsByClient)",
   "detail": "Publish Now now exists on the calendar's post detail panel, staff-only: calendar-body.tsx:383-385 builds connectedPlatformsByClient via the new shared pushablePlatformsByClient (src/lib/publish-targets.ts:18) and returns undefined for isClient, and RunCalendar passes canPublish={canSchedule} (true only for staff, calendar-body.tsx:113/141) plus the platform list into AssetDetailModal, whose PublishNowRow requires canPublish && a compatible connected platform. Part 1 of the fix also landed: AssetCard now receives connectedPlatforms on both call sites (src/components/assets-view.tsx:124-126 fed by src/app/(app)/assets/page.tsx:51, and src/app/(app)/jobs/[id]/page.tsx:37/74), so canPublishNow (asset-card.tsx:481) is reachable at last. The copy strings now name a control that exists and are role-aware: integrations-tab.tsx:474-478 tells a client viewer to post themselves and mark as posted, staff to press Publish Now on the calendar; task-ticket-modal.tsx:255 says \"lands on the calendar, where you open the post and press Publish Now\"; asset-card.tsx:146 keeps \"On the calendar, you push it live with Publish Now\", which is now true (AssetCard mounts only on staff routes \u2014 /assets redirects CLIENT_USER at assets/page.tsx:25).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": true,
   "newBugDetail": "Making the prop flow exposed a gate that was dead code before. src/components/asset-card.tsx:481-482 is `canApprove && compatibleConnected.length > 0 && asset.status !== \"published\"` \u2014 no `publishMode !== \"placeholder\"` and no approved/scheduled requirement, unlike the new modal gate (asset-detail-modal.tsx:436-440, which excludes placeholders). assets-view.tsx:124 hands the client's platform list to EVERY card of that client, and pushablePlatformsByClient only decides WHICH CLIENTS to read integrations for, not which assets. So on /assets and /jobs/[id] a staff member now sees \"Publish Now\" on (a) unapproved drafts and (b) \"Calendar-only roadmap item. Karos never posts it\" placeholders, and publishAssetNowAction (src/lib/actions/asset-actions.ts, publishAssetNowAction body) refuses neither \u2014 it only rejects status === \"published\" \u2014 so pressing it really posts an unapproved draft or a placeholder. Fix: copy the modal's two extra conditions into asset-card.tsx:481 and add a server-side refusal for publishMode === \"placeholder\" / status === \"draft\".",
   "needsHumanEyes": true,
   "humanEyesWhy": "Decide the product answer for the new hole: Publish Now now renders on drafts and on \"Karos never posts it\" placeholders in the staff assets list, and the server action allows both."
  },
  "108": {
   "n": 108,
   "verdict": "done-differently",
   "evidenceNow": "src/lib/scheduled-runs.ts:100-121 (timezone-pinned branch of computeNextRun) + src/lib/types.ts:592 (PlannedScheduledRun.timeZone) + src/app/(app)/calendar/calendar-body.tsx:283-286",
   "detail": "The intent is now persisted with its zone and every recompute site passes it: create (planned-run-actions.ts:100,129-137,150), the client pace path (:277-285,301), resume re-anchor (:407-417) and the cron advance (src/app/api/run-scheduled/route.ts:107-118). ScheduleRunModal sends the browser zone (schedule-run-modal.tsx:61-64,88) and previews with the same pure helper, so preview and stored instant agree; the preview and describeCadence both print a zone suffix (scheduled-runs.ts:213-219,236). Rendering reads one clock: dayKey and timeStr take an optional timeZone (run-calendar.tsx:108-133), calendar-body passes runZone(r.timeZone) with a documented legacy fallback to the runtime zone (:81-85), and ScheduledRunCard prints cadenceLabel + \"next\" + zoneLabel all off that zone (run-calendar.tsx:285-288). Deviation: the doc said to use computeNextRunAt from run-cadence.ts; he instead made computeNextRun zone-aware on top of run-cadence's localYMD/zonedWallToUtc/weekdayOf primitives. His way is better \u2014 computeNextRunAt only models RunCadence.daysOfWeek (types.ts:304-310) and cannot express monthly/dayOfMonth or once, so following the instruction literally would have broken monthly schedules. Residual, unchanged and arguably correct: past runs and published posts are instants, so their day bucket and the \"today\" highlight still come from the renderer's zone (dayKey called without a zone at run-calendar.tsx:612, today at :588), which can still shift between SSR and hydration.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "109": {
   "n": 109,
   "verdict": "fixed",
   "evidenceNow": "src/components/run-calendar.tsx:392-451 (PastRunCard header link/button + \"Review deliverable\" action), wired at :870-876 with canOpenJob={canSchedule}",
   "detail": "The completed-run card header is now actionable and role-split exactly as the fix asked: staff (canOpenJob, fed from canSchedule which calendar-body.tsx:141 sets only for staff) get a Link to /jobs/${run.id}; a client gets a button calling onOpenAsset(run.assets[0].id) \u2192 setOpenAssetId, the same handler PostCard uses, which opens AssetDetailModal (:875, :907). An explicit \"Review deliverable \u2192\" control renders when run.jobStatus === \"review\" (:437-451). No client is pointed at /jobs/[id] (still staff-guarded at src/app/(app)/jobs/[id]/page.tsx:21). A client run whose deliverables are all still locked yields no asset id and falls back to the plain non-clickable header plus \"No client-facing assets from this run\" (:455-457), which is correct rather than broken.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "110": {
   "n": 110,
   "verdict": "fixed",
   "evidenceNow": "src/components/run-calendar.tsx:230-268 (both handlers) and :301-357 (confirm step + \"Delete schedule\")",
   "detail": "remove() and pause() now capture the action result, render res.error inline (:359) and clear busy in a finally, so a refused call recovers instead of spinning; pause additionally sets a local paused state with an explanatory line rather than letting the card blink out (:296-299). The danger button is renamed \"Delete schedule\" and gated behind an inline confirm (\"Yes, delete it\" / \"Keep it\", :303-327). The optional part landed too, with the server made to match: canManage = canManageRuns || canSchedule and calendar-body passes canManageRuns unconditionally (calendar-body.tsx:445), so a client can pause from the calendar, while canDelete stays canSchedule (:862) and deletePlannedRunAction is now requireStaff (planned-run-actions.ts:432) with setPlannedRunStatusAction refusing \"completed\" for a CLIENT_USER (:364-366). No privilege regression from the requireClientAccess\u2192requireStaff swap: requireClientAccess already admitted any staff member for any client (src/lib/actions/_shared.ts:22-30), so the change only removed the client's delete.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "111": {
   "n": 111,
   "verdict": "fixed",
   "evidenceNow": "src/components/run-calendar.tsx:715 and :722 (`hidden grid-cols-7 sm:grid`) + :796-830 (phone agenda `<ul className=\"divide-y divide-border sm:hidden\">`)",
   "detail": "The day-label row and the seven-column grid are now sm-and-up only, and below sm a real agenda renders \u2014 one <li> per day that has content, a full-width day header button (min-h-[24px]) that opens the day detail, and the same RunChip/PostChip at size=\"row\" (px-2 py-2 text-xs min-h-[36px]). Chip metrics are shared through CHIP_SIZE (:142-145): the desktop cell variant is now px-1 py-1 text-[11px] min-h-[24px], clearing the 24px minimum for the PostChip control, and the \"+N more\" line is text-[11px] (:786). agendaDays (:632-641) is keyed with the same `${year}-${month}-${day}` shape dayKey produces, so the mobile list and the grid bucket identically. Cell min-h-[84px] kept as the fix allowed. Minor: RunChip is still a non-interactive <div> in both layouts, so a mobile run row isn't tappable \u2014 the day header above it is the control.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": true,
   "humanEyesWhy": "Layout-only change with a screenshot finding behind it \u2014 worth one look at 375px and at laptop width with the copilot dock open to confirm the agenda and the taller chips read as intended."
  },
  "112": {
   "n": 112,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/calendar/calendar-body.tsx:419-436 (EmptyState with action Link) + src/components/run-calendar.tsx:736-745 (empty-cell click \u2192 schedule prefill)",
   "detail": "When runs.length + posts.length === 0 and a single client is in scope (:393-394), an EmptyState renders above the grid with a \"Set up an agent schedule\" link to /clients/${scopedClientId}/agents and copy that says where schedules come from. The target is right for a client: their AI Agents page IS /clients/[clientId]/agents (sidebar.tsx:415-421) and that page admits a CLIENT_USER for their own id (clients/[id]/agents/page.tsx:49-52), where configureClientAgentScheduleAction is reachable. Part two landed as well: for staff, an empty day cell is now a button (canScheduleHere, aria-label \"Schedule a run on July 14\") that stores the clicked day at 09:00 and opens ScheduleRunModal with prefillAt (:741, :915-926), which the modal consumes for cadence/weekday/dayOfMonth/runAt (schedule-run-modal.tsx:42-52). Empty-cell prefill exists only in the desktop grid; the mobile agenda lists only days with content.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "142": {
   "n": 142,
   "verdict": "fixed",
   "evidenceNow": "src/components/run-calendar.tsx:695 (`shrink-0 whitespace-nowrap` on the Schedule a run button) with the header row at :672 and the heading at :677-678",
   "detail": "The header row is flex-wrap with gap-x-3 gap-y-2, the title block is min-w-0 with truncate on both lines, and the action cluster is shrink-0 \u2014 so the row wraps as a row and the heading ellipsises before the primary button's label can break. The button itself carries whitespace-nowrap, and the month label got w-[130px] shrink-0 whitespace-nowrap so the arrows/label group cannot wrap either.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "151": {
   "n": 151,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/calendar/calendar-body.tsx:315-321 (title via cleanTitle, preview via toPlainSummary) + :337-339 (composed outputSummary, staff-only staffRef) + src/components/run-calendar.tsx:399-401,409,433-435",
   "detail": "The card line is composed, not echoed. calendar-body replaces the old raw `(a.content ?? \"\").slice(0, 240)` with toPlainSummary (src/lib/doc-render.ts:195-233), which drops the record's own bookkeeping lines via isInternalLine (:134-143,164) and strips inline markdown, and titles go through cleanTitle (:88-90) which removes leading `#` and marks. The status badge now renders through JOB_STATUS_META (\"In review\", src/components/job-status.tsx:15) instead of the raw enum the old card printed (`run.jobStatus ?? \"done\"` at ddcef3e). \"drafted N posts\" is composed from the run's own deliverables (describeRunOutput, :60-66) and product/job ids are confined to a staff-only tooltip: staffRef is built only when !isClient (:339) and consumed as the card's title attribute (run-calendar.tsx:409). The exact photographed leak is pinned in tests: src/lib/__tests__/doc-render.test.ts:106-123 asserts \"pending_review\", \"e13\" and \"e52ffe1e\" are absent while \"Karos X\" survives; both doc-render and scheduled-runs suites pass at HEAD (92 tests).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false,
   "needsHumanEyes": false
  },
  "113": {
   "n": 113,
   "verdict": "fixed",
   "evidenceNow": "src/components/sidebar.tsx:617-619",
   "detail": "The ClientContextPicker is no longer admin-gated \u2014 it renders for `isStaff` (sidebar.tsx:617), and its trigger carries the \u2715 that calls setActiveClient(null) then router.push(\"/clients\") (sidebar.tsx:114-120, 137-147). On top of that, option (a)'s labelled exit exists as a separate always-mounted bar: ClientContextBar (src/components/client-context-bar.tsx:24-50, \"Exit client view\") is rendered in the staff shell for any staff member with an active context (src/app/(app)/layout.tsx:235), and the narrow-width Company sheet has its own \"Exit client view\" row (sidebar.tsx:728-738). Option (b) was consciously NOT taken \u2014 the nav is still swapped wholesale at sidebar.tsx:426 \u2014 and the comment at sidebar.tsx:435-439 says why. The uninvited copilot panel the finding also describes is unchanged: StaffCopilotDock still mounts for any staff with an active client (src/components/staff-chatbot-widget.tsx:14-15, layout.tsx:246).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The escape hatch is fixed, but the second half of what the employee saw \u2014 a client copilot dock appearing on the right the moment they open a client page \u2014 is still there by design and was never ruled on.",
   "newBug": true,
   "newBugDetail": "The picker is now shown to employees but is inert for them: src/app/(app)/layout.tsx builds the employee-scoped list as `staffClients` (line 53) yet only assigns `clients` inside the admin-only `if (adminData)` branch (line 107), so Sidebar receives `clients={[]}` (line 225) for every KAROS_EMPLOYEE. Their dropdown always reads \"No clients found\" (sidebar.tsx:174-176). The comment justifying the change (sidebar.tsx:612-616) asserts \"`clients` is already fenced to their assigned clients by the app layout\" \u2014 that is not true at HEAD. Clearing the context still works, so F113 itself is resolved; what shipped is a switcher an employee cannot switch with, plus a false code comment."
  },
  "115": {
   "n": 115,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/user-actions.ts:37-67, 159, 170-172",
   "detail": "All three additions landed. (1) notifyRegistrationDecision (user-actions.ts:37-67) builds an approval or decline mail through emailShell + sendEmail with replyTo=SUPPORT_EMAIL and soft-fails to console.error; approveRegistrationAction awaits it after the upsert (line 159) and rejectRegistrationAction captures the address BEFORE the delete and mails after (lines 167-172). (2) src/app/pending/page.tsx:26 mounts <AutoRefresh intervalMs={15000} />, and the pre-existing `if (!user.disabled) redirect(\"/dashboard\")` at line 10 turns that poll into an automatic hand-off. (3) pending/page.tsx:37-47 adds the timeframe line (\"This usually happens within one business day\u2026\") and an \"Email your Karos team\" mailto next to Sign out.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "116": {
   "n": 116,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-rail.tsx:201-207",
   "detail": "The desktop rail's bottom block now renders the icon variant of NotificationBell beside the Credits pill with panelPlacement=\"up\" and the same actionItems/reviewJobs/taskAlerts the rail already receives (client-rail.tsx:188-208), so the count is visible without opening anything. The nested \"Notifications\" row was deleted from the account menu \u2014 src/components/account-menu.tsx:13-17 now carries only the comment explaining why, and its action block (lines 85-95) is ThemeSwitch / Settings / Support. ClientRail is mounted only for CLIENT_USER (src/app/(app)/layout.tsx:110, 155), so this is the client shell specifically. Deviation, deliberate and recorded: at phone width the bell moved OFF the top bar into the Company sheet (client-rail.tsx:307-315) with the signal carried by a dot on the Company tab (client-rail.tsx:253, mobile-shell.tsx:130-138) \u2014 CD-G9/CD-H5 overruled F116's placement there.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "117": {
   "n": 117,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/page.tsx:12-22 and src/components/clients-grid.tsx:458-467",
   "detail": "The Clients page now fetches getClientCredits per visible client and maps it through availableCredits into a clientId\u2192spendable record (clients/page.tsx:12-22), passes it as `credits` (line 62), and each card renders a Badge next to the asset/job counts \u2014 danger at 0 (\"Out of credits\"), warning at or below LOW_CREDIT_THRESHOLD, neutral above (clients-grid.tsx:458-467). Admin Analytics gained the queue: lowCreditClients() (analytics/page.tsx:41-53) sorts spendable ascending and filters \u2264 LOW_CREDIT_THRESHOLD (=20, src/lib/constants.ts:124 \u2014 the same number the copilot's line uses), rendered as a \"Clients low on credits\" card whose rows link to /clients/[id]/settings (analytics/page.tsx:298-324). Gate verified: /admin/analytics redirects any non-KAROS_ADMIN (analytics/page.tsx:60-61); /clients is staff-only (clients/page.tsx:10). Minor deviation: the grid badge itself is not a link (it sits inside the card's Link to /clients/[id]); the analytics rows carry the settings deep link instead.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "118": {
   "n": 118,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/connect/page.tsx:20, 61-70",
   "detail": "He took the second branch \u2014 rewrite the copy rather than build the tool. The header now reads \"read client data, browse jobs & assets, run repo agents, and upload files\" (connect/page.tsx:20); step 3's example is \"\u2026run their content agent with a prompt for three Instagram posts\u2026\" and the explanation says Claude Code \"start[s] the repo agent through the agent service\", explicitly adding \"Running an agent is the only thing the server starts \u2014 the catalog products run from the client's own task board, not over MCP\" (lines 61-70). The Available tools sidebar (lines 77-88) matches the twelve real tools in src/lib/mcp/tools.ts one-for-one, and no submit_managed_job was added (grep for submit_job/submitManagedJob in src/lib/mcp still returns nothing). The stale comment is corrected: src/lib/actions/external-job-actions.ts:9-17 now states \"There is NO MCP entry point to that core: the twelve tools in src/lib/mcp/tools.ts start work only through `run_agent` (QA F118)\".",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The reworded example says \"run their content agent\" \u2014 whether a repo agent by that name exists in a real client's imported catalog is data-dependent, so a staff member could still follow the worked example and get \"agent not found\" from list_agents."
  },
  "119": {
   "n": 119,
   "verdict": "fixed",
   "evidenceNow": "src/app/globals.css:33, 36, 230, 231",
   "detail": "--muted-2 is now #8f8f97 in dark (globals.css:33 \u2014 I measured 4.81:1 against --surface #242429, matching the claimed 4.8:1) and #767469 in light (line 230 \u2014 4.69:1 on white, was 3.6:1). The decorative escape hatch exists and is wired: --muted-3 pinned to the old #77777e / #8a887f (lines 36, 231), exposed to Tailwind at line 71, and the two scrollbar-thumb hovers repointed to it (lines 158-159, 273-274); the Meter fallback fill also uses it (seo-geo-panel.tsx:70, 203). The ten-pixel sweep is partial but no longer a contrast problem, since every text-muted-2 now clears 4.5:1 \u2014 e.g. the calendar's \"+N more\" moved to text-[11px] (run-calendar.tsx:786). tsc --noEmit is clean.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "A token used 463 times was lightened; only a visual pass can confirm the muted/muted-2 hierarchy did not flatten, which is the risk the fix instruction itself named.",
   "newBug": true,
   "newBugDetail": "Two client-visible spans use the decorative token as TEXT, at exactly the size F119 is about: src/components/seo-geo-panel.tsx:135 and :761 render `<span className=\"ml-1.5 normal-case text-muted-3\">\u00b7 {group.basisLabel}</span>` inside 10px intent headers. globals.css:34-35 says of --muted-3: \"kept for DECORATIVE use only \u2026 Text must never use it.\" Those two labels therefore still render at the old ~3.5:1 on the SEO/GEO panel, which is mounted on the client dashboard (src/app/(app)/clients/[id]/page.tsx:19, 88)."
  },
  "120": {
   "n": 120,
   "verdict": "done-differently",
   "evidenceNow": "src/components/x-agent-intake.tsx:720, src/components/linkedin-agent-intake.tsx:678, src/components/reddit-agent-intake.tsx:345",
   "detail": "Instead of exporting a plain JOB_STATUS_LABEL map, all three recent-run lists now render the shared <JobStatusBadge status={r.status} /> component, and the run's status field is typed JobStatus rather than string (x-agent-intake.tsx:62, linkedin:61, reddit:58). The label source is src/components/job-status.tsx:9-20 (JOB_STATUS_META: review \u2192 \"In review\", queued \u2192 \"Queued\", \u2026) with a `?? JOB_STATUS_META.queued` fallback at line 23, so no raw enum can reach the screen. Equivalent-or-better than the instruction: one source of truth shared with /jobs, the dashboard and the calendar, and grep for `{r.status}` / `{job.status}` across src/ now returns only JobStatusBadge call sites. The doc's secondary note also landed \u2014 the ISO slice is gone: staff see `Run ${formatDate(r.createdAt)}` and clients `Worked on your content \u00b7 ${relativeTime(r.createdAt)}` (x-agent-intake.tsx:708-710).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "121": {
   "n": 121,
   "verdict": "fixed",
   "evidenceNow": "src/components/notification-bell.tsx:69-73",
   "detail": "He took the \"delete the X\" branch: `const visibleJobs = reviewJobs;` with the comment explaining that the X wrote only local state and that listReviewJobs queries status == \"review\" so the row self-clears (notification-bell.tsx:69-73). The review row's JSX (lines 263-285) has no dismiss button at all; the surviving X belongs to transcript action items and does reach the server via dismissAssignedActionItemAction (lines 94-104, 315-325). No dismissedBy/seenBy field was added to jobs, consistent with that choice (src/lib/data.ts:1678-1707 still filters on status alone). The role half of the note is also handled, though differently from the instruction: rather than pointing client viewers at the asset, a client's review row is rendered as a non-navigable div while staff keep the /jobs/[id] link (lines 272-282), with the reasoning that the client archive provably excludes drafts.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "122": {
   "n": 122,
   "verdict": "fixed",
   "evidenceNow": "src/lib/integrations/platforms.ts:117-141",
   "detail": "PLATFORM_LABELS is the single map (linkedin \u2192 \"LinkedIn\", youtube \u2192 \"YouTube\", tiktok \u2192 \"TikTok\", x \u2192 \"X\", twitter \u2192 \"X (Twitter)\", plus the google_* ids), with platformLabel(id) falling back to the id with underscores opened rather than a snake_case token (lines 139-141). The connected-channels row on the client dashboard now calls it: src/components/client-analytics.tsx:170 renders {platformLabel(i.platform)} and the className=\"capitalize\" that caused \"Linkedin\"/\"Youtube\" is gone. Other display sites go through the same map (asset-card.tsx:228, asset-detail-modal.tsx:244, clients/[id]/agents/[agentId]/page.tsx:872). grep for `capitalize` across src/components and src/app returns no platform or provider use.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "123": {
   "n": 123,
   "verdict": "done-differently",
   "evidenceNow": "src/components/client-analytics.tsx:65-69",
   "detail": "The sentence was not reworded in place \u2014 the whole one-line panel became the fifth tile in the counter row: <StatCard label=\"Agent runs\" value={jobs.length} hint={lastRun ? `Last run ${relativeTime(lastRun.createdAt)}` : \"No runs yet\"} /> (client-analytics.tsx:65-69), which reads \"Agent runs / 20 / Last run 9h ago\" and adds an honest empty state. Equivalent to the requested \"20 runs \u00b7 last run 9h ago\" and it also settles F99. Mounted on the client's own dashboard at src/app/(app)/clients/[id]/page.tsx:193 (ClientAnalyticsStats) and for staff via ClientAnalytics with hideStats=false (page.tsx:77-86).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "143": {
   "n": 143,
   "verdict": "fixed",
   "evidenceNow": "src/components/notification-bell.tsx:406-415, 335-355, 23",
   "detail": "Every row now leads with a subject-and-verb line and carries the record's own wording underneath: task rows print \"A task is ready for your review\" / \"A task is waiting on you\" then the title at line-clamp-3 (TaskAlertRow, lines 404-415), review rows print \"{agentName} finished a draft\" then the title at line-clamp-2 (lines 247-259) \u2014 wrapping instead of the single-line clip. The footer is per-kind rather than always meetings: \"View workspace \u2192\" only when task or review rows are present, \"View all meetings \u2192\" only when action items are (lines 335-355). Age-out is visual: STALE_MS = 7 days (line 23) drives opacity-60 on task rows (line 390), review rows (line 269) and action items (line 293). The badge/header vocabulary was also unified to \"unread\" (lines 88, 179).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "146": {
   "n": 146,
   "verdict": "fixed",
   "evidenceNow": "src/lib/data.ts:788",
   "detail": "listTranscripts now sorts `(b.meetingDate ?? b.createdAt ?? 0) - (a.meetingDate ?? a.createdAt ?? 0)` (src/lib/data.ts:782-788), so a Fireflies backfill can no longer push old meetings to the top. The display touch matches the sort key on both surfaces: the list row stamps relativeTime(t.meetingDate ?? t.createdAt) (src/components/meetings-client.tsx:230) and the detail page formatDateTime(t.meetingDate ?? t.createdAt) (src/app/(app)/transcripts/[id]/page.tsx:72); the timeframe filter uses the same expression (meetings-client.tsx:57), so a meeting can no longer be filtered by one date and ordered by another. Both the staff and the client fetch paths go through listTranscripts (transcripts/page.tsx:14-19), so the client Meetings list is ordered too.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "40": {
   "n": 40,
   "verdict": "fixed",
   "evidenceNow": "src/components/custom-agents.tsx:1195-1201",
   "detail": "The staff dial is now labelled \"Runs per week\" (custom-agents.tsx:1200) and the cost box gained the resulting volume \u2014 \"{postsPerWeek * outputsPerRun} new draft(s) a week.\" at custom-agents.tsx:1285-1288. The client (paceOnly) face labels the same dial \"Posts per week\" only when outputsPerRun === 1 (literally true) and \"Posting days a week\" otherwise (1196-1200), and its cost sentence matches (1273-1277), so no surface now contradicts itself. The activity-log copy at src/lib/actions/planned-run-actions.ts:337 was reworded to \"Set <agent>'s pace: 3 posting days a week\" rather than the instructed \"N runs per week (M drafts)\" \u2014 a deliberate deviation citing the A3/A4 churn rule (no batch decomposition to clients); it no longer misstates volume, which was the defect.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "41": {
   "n": 41,
   "verdict": "fixed",
   "evidenceNow": "src/components/x-agent-intake.tsx:708-720",
   "detail": "All three feedback run lists now render <JobStatusBadge status={r.status} /> instead of `\u00b7 {r.status}` \u2014 x-agent-intake.tsx:720, linkedin-agent-intake.tsx:678, reddit-agent-intake.tsx:345 \u2014 and JOB_STATUS_META maps review\u2192\"In review\", failed\u2192\"Failed\" (src/components/job-status.tsx:12-20). Raw ISO dates are gone: staff get `Run ${formatDate(...)}` with the /jobs link, clients get `Worked on your content \u00b7 ${relativeTime(...)}` (x:708-710, li:666-668, reddit:333-335). Feedback-history dates use relativeTime (x:759, li:717, reddit:377). The isStaff gate is real and comes from the route's role check (x-agent/page.tsx:21, linkedin/page.tsx:23, reddit/page.tsx:24) through buildXAgentIntakeView, and toRunRowViews only attaches href when isStaff (src/lib/agent-intake-views.ts:82).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "42": {
   "n": 42,
   "verdict": "fixed",
   "evidenceNow": "src/components/x-agent-intake.tsx:498-508",
   "detail": "<RequiredMark /> now sits on every \"Anything we must never post\" label the server rejects empty: X SeatCard (x-agent-intake.tsx:500), X AddSeatForm (596), LinkedIn SeatCard (linkedin-agent-intake.tsx:438), LinkedIn AddSeatForm (569), alongside the pre-existing company marks (x:280/293). Placeholders were softened to read as a required choice \u2014 'Write \"nothing\" if everything is fair game.' (x:507/603, li:445/575). The server rejections that make the mark truthful are still in place (x-agent-actions.ts:140/187/308, linkedin-agent-actions.ts:141/219), and the genuinely-optional LinkedIn company field keeps its \"(optional)\" label with no server rejection (linkedin-agent-intake.tsx:172, action at linkedin-agent-actions.ts:101 checks length only).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "43": {
   "n": 43,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/agents/page.tsx:385-406",
   "detail": "`nothingToShow = enabledAgents.length === 0 && staffRuns.length === 0` (line 275) now drives a real EmptyState titled \"No agents available for this client yet\" that names both causes and links out: \"Import or enable an agent \u2192\" to /agents, plus a conditional \"Set the lab-repo slug \u2192\" to /clients/<id>/settings when client.agentsRepoSlug is unset (399-403) \u2014 exactly the filter (agentKeyMatchesClientSlug, line 249) that drops per-client instances. The service-unconfigured branch keeps its own separate empty state (379-384), so the two conditions no longer share one message.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "44": {
   "n": 44,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/agents/page.tsx:209-212",
   "detail": "One heading survives: PageHeader title=\"AI agents\", description=\"Your always-on AI team. Open an agent to see what it makes and to start a post.\" The duplicate section header is gone entirely \u2014 the client branch now renders <ClientAgentRoster> (page.tsx:226), and roster.tsx emits a bare <section> with a grid and no <h2>/<p> of its own. The wording differs from the instruction's suggested description because the run gesture moved to the detail page. NOTE: the instruction's \"keep the credits badge unconditionally\" was NOT honoured \u2014 the client's \"N credits available\" badge no longer appears anywhere on the agents page (spendable is computed at page.tsx:97 but only feeds creditBlockReasons). The balance is still reachable via CreditsPanel on /clients/<id>/settings and inside the pace dialog (custom-agents.tsx:1291-1295), so it is a downgrade rather than a loss.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The client's credit balance is no longer visible on the AI Agents page at all; confirm that is intended, since the finding's fix explicitly asked to keep that badge.",
   "newBug": false
  },
  "45": {
   "n": 45,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/external-job-actions.ts:10-12",
   "detail": "The retire direction was taken cleanly. src/components/managed-products.tsx and src/components/client-managed-agents.tsx no longer exist; external-job-actions.ts now exports only cancelManagedJobAction (line 27) and cancelClientAgentJobAction (line 41), with a comment recording that submitManagedJobAction went with the UI. The submitManagedJob CORE is untouched and still called by the task engine (src/lib/execution-engine.ts:317) and the runway sweep (src/app/api/runway/route.ts:183). The jobPreviews block is gone from the staff branch, with the reason recorded at src/app/(app)/clients/[id]/agents/page.tsx:252-257. `npx tsc --noEmit` is clean, so nothing dangles. Minor stale text: src/lib/jobs/submit-managed.ts:18 still says the core is \"Called by BOTH the web server action (`submitManagedJobAction`\u2026)\" \u2014 a comment referencing a deleted symbol.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "127": {
   "n": 127,
   "verdict": "fixed",
   "evidenceNow": "src/components/custom-agents.tsx:68-73",
   "detail": "The manifest string no longer crosses the client boundary at all: RunnableAgentSummary is Pick<CustomAgent, \"id\"|\"key\"|\"name\"|\"clientBlurb\"|\"icon\"|\"color\"> and deliberately omits `description` (custom-agents.tsx:68-73), so it is not even in the RSC payload. Every client surface resolves copy through clientAgentBlurb (src/lib/agent-blurbs.ts:157-176): curated clientBlurb first, then a keyed fallback list of buyer-language sentences, then a contentless honest default \u2014 the manifest is not in the chain. clientBlurb exists on the type (src/lib/types.ts:257) and in the admin editor (custom-agents.tsx:1943-1957). The requested import guard exists as LAB_JARGON_RE (src/lib/agent-service/custom-agent-import.ts:214) and is enforced on what an admin types (src/lib/actions/custom-agent-actions.ts:117-119); the importer goes further than asked and never promotes a manifest blurb at all \u2014 every import lands with clientBlurb: null and is flagged (custom-agent-actions.ts:284-297), surfaced as a \"No client blurb\" badge on the staff library card (custom-agents.tsx:541). The calendar path is guarded too (src/app/(app)/calendar/calendar-body.tsx:208-220, 236-247). Remaining work is the data backfill (scripts/backfill-agent-blurbs.ts), which is what OPS-PENDING refers to \u2014 but because the fallback is keyed, no client sees the manifest even un-backfilled.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "scripts/backfill-agent-blurbs.ts still has to be run with --apply against the live customAgents so the seven agents carry curated blurbs rather than the keyed fallbacks; verify on screen afterwards.",
   "newBug": true,
   "newBugDetail": "Stale, now-false helper copy in the admin agent editor: src/components/custom-agents.tsx:1953-1956 tells staff \"Leave empty and the card falls back to the internal description.\" That is no longer true \u2014 agentBlurb/clientAgentBlurb never reads `description` (src/lib/agent-blurbs.ts:163-176); an empty blurb falls back to the keyed blurb map. The same wrong claim is in the comment at custom-agents.tsx:539-540 (\"the client's card is still falling back to the lab manifest below\"). Staff-facing only, but it misdescribes the very guarantee this finding was about."
  },
  "128": {
   "n": 128,
   "verdict": "done-differently",
   "evidenceNow": "src/components/custom-agents.tsx:183-216",
   "detail": "The mid-word character cut is gone, but not the way the fix asked. Root cause first: the client card no longer renders the 600-char-truncated manifest at all (that slice is still at src/lib/agent-service/custom-agent-import.ts:283, now feeding the internal `description` shown only on the staff library card at custom-agents.tsx:549-551). Where a client reads a blurb it is a short curated/keyed sentence clamped on a LINE boundary: the run dialog uses a new AgentBlurb component with line-clamp-3, measured overflow and a More/Less toggle (custom-agents.tsx:183-216, mounted at 1642), and the detail page prints the blurb unclamped in the PageHeader (src/app/(app)/clients/[id]/agents/[agentId]/page.tsx:521-523). What is missing versus the instruction: the roster card \u2014 the surface the screenshot was taken on \u2014 uses a plain `line-clamp-2` with NO More control (src/components/client-agents/roster-card.tsx:75-77), so a 300-char curated blurb (MAX_CLIENT_BLURB_CHARS) still gets an ellipsis there with no in-place expansion; the full text is one click away on the detail page. He also wrote his own toggle instead of reusing seo-geo/disclosure.tsx. Better than asked on the root cause, slightly weaker on the disclosure.",
   "agreesWithLedger": true,
   "confidence": "medium",
   "needsHumanEyes": false,
   "newBug": false
  },
  "129": {
   "n": 129,
   "verdict": "fixed",
   "evidenceNow": "src/lib/client-agents.ts:473-511",
   "detail": "The constant is deleted \u2014 `grep -rn \"Ready to build your weekly\" src/` returns nothing. The status slot is now derived. On the roster, rosterStatus() (src/lib/client-agents.ts:473-511) resolves one word with the precedence the fix asked for: a schedule refusal outranks Live (488), then launchState, then an active schedule, then hasDelivered \u2192 \"Runs on request\" vs \"Not set up yet\". Staff rows carry a second derived line (review count \u2192 empty intake \u2192 pace) built server-side at src/app/(app)/clients/[id]/agents/page.tsx:315-322 and painted as `note` (roster-card.tsx:78-83). The full ladder the fix specified lives on the detail surface: StaffAgentControls' live-state slot walks refusal \u2192 setup missing \u2192 next fire \u2192 \"N drafts waiting for review\" (linked) \u2192 last run \u2192 \"No runs yet.\" (custom-agents.tsx:729-778). No card can now print readiness under a Setup-needed chip: the chip and the strip read the same resolved state.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "130": {
   "n": 130,
   "verdict": "fixed",
   "evidenceNow": "src/lib/types.ts:271",
   "detail": "`creditCost?: number | null` is on the CustomAgent record (src/lib/types.ts:271), admin-editable (\"Credits per run\", custom-agents.tsx:2001-2013), and read with the flat default as the fallback everywhere \u2014 agentRunCost() at custom-agents.tsx:172-174, the client detail page at src/app/(app)/clients/[id]/agents/[agentId]/page.tsx:200, the card footer copy (\"{cost} credits per post\", agent-detail-panel.tsx:301; legacy-agent-panel.tsx:186), the per-agent credit-block reason (agents/page.tsx:107), the schedule cost (planned-run-actions.ts:288) and \u2014 critically \u2014 the actual charge (src/lib/jobs/submit-custom.ts:322, src/lib/actions/client-agent-run-actions.ts:105), so the quoted price and the billed price cannot diverge. The schedule dialog's derived weekly total was left correct, as instructed. Copy also improved from the inaccurate \"per output\" to \"per client run\"/\"per post\". This is exactly what the fix asked for; the OPS-PENDING part is that no agent has a value typed in yet, so all seven still resolve to the 25-credit default on screen.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Until an admin types per-agent prices into the new \"Credits per run\" field, every card still reads 25 credits \u2014 the on-screen symptom the finding described persists as a data task."
  },
  "131": {
   "n": 131,
   "verdict": "fixed",
   "evidenceNow": "src/lib/client-agent-runs.ts:141-150",
   "detail": "Readiness is now a server-evaluated gate that every run control reads, and no surface renders an enabled Run beside a blocked chip. The pure gates return a `setup_missing` code with a client-worded reason: evaluateTemplateRunGate (src/lib/client-agent-runs.ts:141-150) and evaluateLegacyRunGate (338-346), both fed the same resolved AgentSetupState \u2014 and Reddit is now in that map, so it no longer computes ready:true by omission (src/lib/client-agent-rows.ts:229-242, clientLabel \"Your Reddit details\"). Client detail: the button is `disabled={!runnableTemplate || running}` with the reason and a setupHref link PAINTED, not a title (src/components/client-agents/agent-detail-panel.tsx:182-216); the legacy panel does the same (legacy-agent-panel.tsx:142-165). Staff detail: `disabled={Boolean(blockedSetup)}` on Run now with the reason painted below (custom-agents.tsx:787, 814-818). The roster card carries no Run button at all (roster-card.tsx). The gates are re-checked by the actions, so nothing can be pressed that the server refuses.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "132": {
   "n": 132,
   "verdict": "fixed",
   "evidenceNow": "src/lib/client-agent-rows.ts:103-112",
   "detail": "The raw prompt is stripped server-side rather than hidden at render: toRunRows only attaches `prompt` and `href` when staff (src/lib/client-agent-rows.ts:110-111), and CustomAgentRunRow documents prompt as staff-only (custom-agents.tsx:108-114). The row label is now the resolved agent identity plus what the run produced \u2014 `run.label` with `relativeTime(createdAt) \u00b7 N drafts`, and the prompt appended only when present i.e. only for staff (custom-agents.tsx:900-909). Beyond the fix: the client branch of the agents page mounts no AgentRunHistory at all \u2014 only <ClientAgentRoster> (src/app/(app)/clients/[id]/agents/page.tsx:225-233) \u2014 so a client never meets these rows; `runs` there is used solely to decide whether to mount <AutoRefresh> (line 158-163).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "134": {
   "n": 134,
   "verdict": "fixed",
   "evidenceNow": "src/lib/custom-agent-launch.ts:358",
   "detail": "The chip now reads \"Focus this batch on one person's seat.\" (src/lib/custom-agent-launch.ts:358) \u2014 the reword option in the fix, so nothing needs substituting and clicking it inserts no literal placeholder. The requested regression test exists and is real: src/lib/__tests__/agent-launch-ui.test.ts:115-140 sweeps every launch profile's eyebrow, intro, estimate, quickStarts, deliverables, attachment label/hint and every field label/placeholder/helper/option label against /\\[[a-z_]+\\]/ and fails on any hit. `grep -rn \"\\[person\\]\" src/` finds it only inside comments and that test's rationale.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "135": {
   "n": 135,
   "verdict": "fixed",
   "evidenceNow": "src/components/custom-agents.tsx:1280-1284",
   "detail": "The cost line now pluralises from the value: \"{postsPerWeek} run{s} \u00d7 {outputsPerRun} output{s} \u00d7 {costPerOutput} credits.\" (src/components/custom-agents.tsx:1281-1283), and the added draft-volume line does the same (1285-1288). Note the QA doc's evidence pointed at the wrong file \u2014 src/components/schedule-run-modal.tsx never held this string (confirmed against ddcef3e); the \"1 outputs\" text was AgentScheduleModal in custom-agents.tsx (ddcef3e:795), which is where the fix landed.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "97": {
   "n": 97,
   "verdict": "done-differently",
   "evidenceNow": "src/components/client-home-overview.tsx:121-138 (non-link status row); src/components/progress-view.tsx:73-93 (URL-driven tab)",
   "detail": "Edit (1) landed exactly as asked: ProgressView seeds `view` from `?tab=` (progress-view.tsx:73), follows a changing param on same-route navigation (:79-84) and writes it back with history.replaceState (:86-93), so /tasks?tab=archive is deep-linkable \u2014 and it is mounted (tasks-body.tsx:156). Edit (2) landed differently: the count now comes from draft ASSETS (client-home-overview.tsx:47; `jobs` is no longer even a prop), the label/hint were rewritten to the spec's wording, and no Approve control was added \u2014 but the href was deliberately dropped rather than repointed to /tasks?tab=archive, so the row renders as a plain status line with no arrow and no hover (AttentionRow's href is optional, :231-268). His deviation is correct and the instruction was wrong: isInClientArchive returns false for drafts (src/lib/asset-visibility.ts:100-107) and the Archive tab is fed getClientArchiveAssets (src/app/(app)/tasks/tasks-body.tsx:72-74), so the instructed link would have landed the client on a screen that provably excludes the counted items. Approval stays staff-only. Net: better than asked.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The row now reads \"N deliverables in review\". The fix instruction dictated that wording, but the house copy rule lists \"review\" as an internal status word not to show clients. The paired hint (\"Your Karos team is reviewing these \u2014 they'll appear in your archive when ready\") carries the meaning in plain English, so this is a copy judgement call, not a code defect.",
   "newBug": false
  },
  "99": {
   "n": 99,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/page.tsx:180-209 (client branch: one-line welcome \u2192 Overview \u2192 AI Insights \u2192 tabs); src/components/client-dashboard-tabs.tsx:35-84",
   "detail": "The client branch is now: a single small welcome line (page.tsx:180-184, em dash, no PageHeader banner), the Overview section (counters + attention/recent, :190-204), the AI Insights briefing (:205-208), then ClientDashboardTabs holding Performance and Search & AI visibility (:209). The tab control mirrors ProgressView's segmented markup and keeps the active tab in `?tab=` via history.replaceState (client-dashboard-tabs.tsx:44-53); duplicate rendering is prevented with hideStats/hideScores/hidePlan (page.tsx:84, :102-104; seo-geo-panel.tsx:554, :734). The standalone \"Agent activity\" Card is gone and its run count is the fifth StatCard with a \"Last run \u2026\" hint (client-analytics.tsx:62-69), grid widened to lg:grid-cols-5 (:57). Staff keep the plain single stack (page.tsx:110-133). Three documented deviations: the counter row was lifted ABOVE the briefing rather than left in the Performance tab (CD-H1); the fix list is the second section inside the visibility tab (page.tsx:162-166) rather than a section under the briefing, because nothing behind the tab may also render outside it; and the old card's \"Open AI Agents\" link was lost when it became a stat tile.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "100": {
   "n": 100,
   "verdict": "fixed",
   "evidenceNow": "src/components/transcript-tools.tsx:179 (\"Analyzing\u2026\" / \"Ingest & analyze\"), :108 (\"summarize it\"); src/app/(app)/transcripts/page.tsx:30, :59; src/components/asset-card.tsx:745 (\"pick favorites\")",
   "detail": "Every cited surface is now US spelling: transcript-tools.tsx:179 and :108, transcripts/page.tsx:30 (\"auto-summarized\") and :59 (\"are summarized and matched\"), asset-card.tsx:745 (\"Expand to read and pick favorites\"), ai-insights.tsx:109 (\"Analyzing\u2026\"), archive-view.tsx:182 (\"organized per agent\"), client-analytics.tsx:111 (\"summarized here\"). The x-drafts-review \"Pick your favourites\" line was first corrected (d0f9224) then removed entirely when that intro was rewritten (4a4021c) \u2014 a repo-wide grep for \"favo\" now returns only asset-card's \"favorites\". Remaining British spellings are all non-user-facing: code comments (layout.tsx:134, notification-bell.tsx:10, integrations-tab.tsx:451, fireflies route:15) and LLM-facing prompt text (chat route:328 \"summarising\" in a tool description).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "124": {
   "n": 124,
   "verdict": "done-differently",
   "evidenceNow": "src/components/client-analytics.tsx:52-71 (four baseline tiles restored plus a fifth); src/app/(app)/clients/[id]/page.tsx:190-209",
   "detail": "Not implemented as written, by ruling: the tile row was collapsed and then reverted after Albert struck the finding (CD-G6), and the code carries an explicit do-not-collapse-again comment at client-analytics.tsx:52-56. There are now FIVE tiles (Published / Scheduled / Channels / Deliverables / Agent runs), not the two thin ones asked for. What did change addresses the duplication complaint indirectly: \"Needs your attention\" sits immediately under the counters (page.tsx:198-202), and the two restating cards (\"Content by status\", \"Connected channels\") moved into the Performance tab below the briefing (page.tsx:209 \u2192 ClientDashboardTabs, default tab performance), so they no longer occupy the first screen. The duplication itself still exists further down the same scroll.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The struck finding's other half \u2014 \"lift the three score tiles above the fold\" \u2014 went the opposite way: F99 moved SeoGeoScores inside the visibility tab (page.tsx:155-161), so a client must now click \"Search & AI visibility\" to see any score, where at the baseline the scores always rendered on the page. Albert's revert covered the tiles, not this.",
   "newBug": false
  },
  "125": {
   "n": 125,
   "verdict": "fixed",
   "evidenceNow": "src/app/api/clients/[id]/insights/route.ts:100-110 (non-staff get needs-connection, no model call); :71-75 (digest scoped to connected platforms); src/components/ai-insights.tsx:46-50, :124-137 (empty state + connect link)",
   "detail": "Both halves are in. (1) When every engagement row is mock/stale the route returns X-Insights-State: needs-connection with no LLM call for anyone who is not staff (route.ts:100-110); AiInsights reads that header, cancels the body and renders the connect-a-channel empty state linking to the client's own settings page (ai-insights.tsx:46-50, :124-137 \u2014 a CLIENT_USER can reach /clients/[id]/settings, where Channels is their first tab since the Profile section is null and dropped, settings-tabs.tsx:30-33). The gate is correct: isStaff is admin/employee only (src/lib/auth.ts:348-350) and a real impersonation session resolves to CLIENT_USER, so \"view as client\" gets the empty state while staff keep the badged demo prose. (2) The digest is scoped to platforms the client actually has (route.ts:71-75) and the system prompt forbids naming or recommending spend on any channel absent from the data (:184-189), so the \"cut LinkedIn spend, move to Instagram\" shape can no longer be produced for a client without Instagram. Expired channels are readmitted and flagged stale so a dead LinkedIn is reported rather than silently dropped (route.ts:65-92; src/lib/analytics.ts:101-104, tests at src/lib/__tests__/analytics.test.ts:179-219, green).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Residual, outside the instruction and unchanged from baseline: the gate is all-or-nothing (analytics.ts:103 \u2014 every row mock or stale). A client with one live channel plus other connected platforms whose provider fell back to mock (analytics-providers.ts:480, :497) still gets a full briefing built partly from invented figures, and unbadged, since dataSourceHeaders uses the same all-or-nothing test (route.ts:92-93). Worth a product call on per-row provenance.",
   "newBug": false
  },
  "126": {
   "n": 126,
   "verdict": "fixed",
   "evidenceNow": "src/components/ai-insights.tsx:200-201 (INLINE_EMPHASIS_RE), :218-254 (renderInline emits <em>); src/lib/__tests__/ai-insights-render.test.ts",
   "detail": "A render-time guard, not an output observation: the split regex covers ***both***, **bold** (with nested single stars), *emphasis*, __bold__ and _emphasis_ (ai-insights.tsx:200-201); unwrap() re-checks each part's shape so bare delimiter runs stay text (:209-216); single-delimiter spans render as <em> (:243-250). The asked-for test exists and passes (13 tests, run green here) including a corpus sweep asserting no \"*\" or \"#\" survives into rendered text. He correctly went past the instruction: the regex the doc supplied \u2014 /(\\*\\*[^*]+\\*\\*|\\*[^*\\n]+\\*|_[^_\\n]+_)/g \u2014 would have failed on \"**bold with *nested* inside**\" ([^*]+ cannot cross the inner star) and would have eaten the middle of snake_case asset labels; the shipped pattern uses (?:[^*]|\\*(?!\\*))+? plus \\w boundary guards for underscores, both pinned by tests (:88-121).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "145": {
   "n": 145,
   "verdict": "partial",
   "evidenceNow": "src/components/client-analytics.tsx:164-193 (all integrations listed, warning badge + settings link for dead tokens); still unfixed at src/components/client-analytics.tsx:60 (`<StatCard label=\"Channels\" value={activeChannels.length} />`)",
   "detail": "The card half is done well: the list iterates ALL integrations rather than only usable ones (client-analytics.tsx:164), a dead token renders the warning \"Reconnect needed \u2192\" badge wrapped in a link to Settings (:173-182) reusing the integrations-tab treatment, only usable rows keep the neon Connected badge (:183-191), the empty state keys off integrations.length (:156), and the icon was corrected to CircleCheck \u2014 I confirmed CheckCircle2 does not exist in the installed lucide while CircleCheck does (:185-189). integrationIsUsable and integrationNeedsReconnect are exact complements (src/lib/integration-status.ts:15-22), so no third state falls through to a false green badge. A \"3 working \u00b7 1 needs attention\" sub-line was added, but on the CARD header (:145-150), not where the fix asked. Missing: the stat half. The Channels tile still counts only usable rows and has no hint (:48, :60) even though StatCard supports one (src/components/ui.tsx:216, :229; the Agent runs tile uses it at :65-69), so a dead channel still silently drops out of the headline count. Minor: the reconnect link omits ?tab=channels \u2014 harmless for a client (Channels is their first tab) but staff land on Profile.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "New user-visible inconsistency introduced by the fix: the Channels tile and the card beneath it now disagree. With 3 healthy + 1 expired integration the tile reads \"Channels 3\" while the list below it shows 4 rows; at baseline both showed 3. The card's sub-line explains it one card over, but the tile is the number a client reads first. One-line fix on client-analytics.tsx:60: hint={staleChannels.length > 0 ? `${staleChannels.length} needs attention` : undefined}."
  },
  "63": {
   "n": 63,
   "verdict": "fixed",
   "evidenceNow": "src/components/icon.tsx:11-18",
   "detail": "All eight dead lucide names are gone from the repo: a repo-wide grep for AlertCircle/AlertTriangle/CheckCircle/CheckCircle2/CheckSquare/Loader2/BarChart2/Wand2 as icon names returns zero hits, and I cross-checked every `name=\"\u2026\"` literal in src/ against the installed lucide-react 1.21.0 `icons` map (1737 keys) \u2014 every one resolves (the only three misses are `name=\"message\"`, `name=\"subject\"`, `name=\"viewport\"`, which are form-field and <meta> attributes, not Icon calls). The specific call sites now read correct-info-modal.tsx:148 `name=\"CircleAlert\"`, sidebar.tsx:60 `icon: \"SquareCheck\"`, onboarding-wizard.tsx:211 `CircleCheck`, notification-bell.tsx:191 `CircleCheck`, tasks-board.tsx:720 `SquareCheck`. The dev-loud fallback was added as asked, deduped one-warning-per-name via a module-level `warned` Set (icon.tsx:23).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "64": {
   "n": 64,
   "verdict": "fixed",
   "evidenceNow": "src/components/notification-bell.tsx:382 and src/components/tasks-board.tsx:499-543",
   "detail": "TaskAlertRow now builds `/tasks?owner=<client|karos>&task=<id>` (notification-bell.tsx:381-382), and TasksBoard reads both params with useSearchParams (tasks-board.tsx:499-508): it finds the linked task, derives initialTab from inferOwner of that task rather than the old hardcoded \"karos\", and seeds selectedTaskId (line 516) so TaskTicketModal opens on mount (rendered at line 889-891). He also handled the case the fix instruction did not mention \u2014 a same-route click from the bell while already on /tasks does not remount, so a linkSignature guard re-applies the params on change (lines 537-543). He deliberately keyed it `owner` rather than `tab` because `tab` is already the Workspace board/activity/archive toggle; the collision is documented at lines 495-498. Unknown values degrade to the default board.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "65": {
   "n": 65,
   "verdict": "partial",
   "evidenceNow": "src/app/api/clients/[id]/chat/route.ts:685",
   "detail": "Two of the three surfaces are fixed. ingestCustomUserTaskAction now returns the routed title (src/lib/actions/task-actions.ts:368 in the return type, :466 `return { ok: true, taskId, owner: parsed.owner, title: parsed.title }`); the copilot quick-add renders `Added \u201c<title>\u201d` plus a \"View\" Link to `/tasks?owner=\u2026&task=\u2026` (src/components/chatbot-widget.tsx:322-331, 395-402); quick-add-task-bar.tsx:40-45 names the created card (no link, deliberately, since the board is on screen); and the War Room's done footer got a \"View task map \u2192\" Link with the 1.6s auto-close removed (src/components/strategy-war-room.tsx:178-182 now only calls onComplete(), and 298-304 is the link). Still missing: the third surface named in the finding. Content Dispatch's closing line is still bare prose with nothing to click \u2014 the persist-tasks tool returns `Created ${count} task${\u2026} in your task board${skipNote}.` at src/app/api/clients/[id]/chat/route.ts:685, and the copilot chat renders that model-paraphrased text with no affordance (chatbot-widget.tsx:242 only sniffs the chunk for \"Created\"+\"task\" to trigger a router.refresh()). The fix instruction never explicitly asked for that one, so this is an under-scoped instruction faithfully followed rather than a botched implementation \u2014 but the defect described in the finding survives on that surface.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "66": {
   "n": 66,
   "verdict": "fixed",
   "evidenceNow": "src/components/archive-view.tsx:191-231",
   "detail": "ArchiveView now has the same `flex flex-wrap items-center gap-2 rounded-lg border border-border bg-surface-2 p-2` control strip AssetsView has: a status select (lines 195-207, labelled in client vocabulary via STATUS_LABEL so \"published\" reads \"Posted\"), an agent select shown only when there is more than one agent (208-222), and a title search input (223-229). `status` seeds from `?status=` and is re-read on same-route param change (lines 105-122). Each agent group caps at GROUP_PAGE_SIZE = 12 behind a \"Show all N \u00b7 M more\" button (67, 243-244, 300-309), the group heading is a real `<button aria-expanded>` that folds its grid (247-251, 288), and a no-match empty state was added (233-238). Group ordering and per-tile stamps use clientDeliveryStamp for clients rather than createdAt.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "67": {
   "n": 67,
   "verdict": "partial",
   "evidenceNow": "src/app/api/users/resume/route.ts:36-46",
   "detail": "Only the interim half of the fix landed. The captions are honest now: settings-form.tsx:188 reads \"Stored for your Karos team \u2014 they use it when writing your LinkedIn advocacy posts.\" and resume-uploader.tsx:88 reads \"Stored for your Karos team\". The dead URL branch is gone \u2014 src/lib/ai/prompts/proactive-assistant.ts:540-547 now emits the background block only when `employeeAdvocacy.resumeText` is present, and the test was updated to assert a URL never reaches the prompt (src/lib/__tests__/artifact-prompt.test.ts:49-56). What did NOT land is the actual fix: the upload route still only stores the file and mirrors `resumeUrl` onto the seat (route.ts:36-46) with no PDF/DOCX/TXT text extraction, so the resume is still never read by a model. And `backgroundContext` \u2014 the one field src/lib/execution-engine.ts:371 maps into `resumeText` \u2014 is still written by no UI and no code path (the only writers are seat-actions.ts:104/116, whose input nothing supplies), so the advocacy prompt now carries zero background instead of an unusable link. Net effect on generation quality is unchanged; only the lie was removed.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "68": {
   "n": 68,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/layout.tsx:52-101",
   "detail": "Both feeds are populated for staff before the shell renders. `isStaffViewer` scopes clients with `listClients()` for admins and `listClients({ employeeId: user.uid })` for employees (layout.tsx:52-56), then reviews come from a new `listReviewJobsForClients([...ids], { limit: 15 })` (src/lib/data.ts:1715-1734, which filters the review-status jobs to the allowed id set) and tasks from one cross-client `listClientTasks({ status: [\"pending\",\"review_pending\"], limit: 200 })` (layout.tsx:88). Rows are annotated with the client name and re-fenced to the viewer's scope, capped at 20 (layout.tsx:93-101), and both the task rows (notification-bell.tsx:413) and the review rows (notification-bell.tsx:254) print that name. The bell itself moved into the sidebar account menu (AppHeader was retired in CD-G9c) and receives scopedReviewJobs/scopedTaskAlerts at layout.tsx:229-230 \u2192 sidebar.tsx:323-326/628-631/711-714. The empty state at notification-bell.tsx:188-195 is now only reachable when all three feeds are genuinely empty. The employee fence matches the one /jobs and /assets use, so an employee cannot see another employee's clients in the bell.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "69": {
   "n": 69,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:1083-1089 and src/lib/actions/_shared.ts:80-101",
   "detail": "Part 1: the Documents empty state now branches three ways \u2014 isAiProcessing \u2192 \"Karos Agents are writing your documents now \u2014 this takes a few minutes.\", aiProcessingFailed \u2192 \"Generation stopped early. Your Karos team is on it.\", else the onboarding line (client-documents.tsx:1083-1089). The new prop is a BOOLEAN, not the raw provider error, and is derived through hasAiProcessingFailure() (src/lib/client-visibility.ts:63-66) at all four mount points (sidebar.tsx:550, 673; client-rail.tsx:164, 266) \u2014 so a client's RSC payload never carries the internal error string. Part 2: the reassurance is now true in both directions. A new logGenerationFailure() writes an INTEL_GENERATION activity entry on failure (_shared.ts:80-101) and is called alongside every one of the five releaseAiProcessingLock failure paths (intel-actions.ts:295, onboarding-actions.ts:121, client-actions.ts:96, api/intel-report-schedule/route.ts:78, api/tasks/generate-swarm/route.ts:76), and a \"Generation failed\" danger badge now appears on the staff /clients grid (clients-grid.tsx:443-445). The banner copy was corrected to match what actually happens \u2014 \"Your Karos team can see it and is on it \u2014 nothing for you to do.\" (ai-processing-banner.tsx:65-71) \u2014 with an explicit comment that no email or push is sent so it must not claim one. No Resend mail was added, which is fine because the copy no longer promises it.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "70": {
   "n": 70,
   "verdict": "fixed",
   "evidenceNow": "src/lib/draft-lane-label.ts:32-48, src/components/x-drafts-review.tsx:139, src/components/li-drafts-review.tsx:155",
   "detail": "The shared helper exists exactly as specified: laneLabel() strips a leading \"Avenue N \u00b7 \"/\"Post N \u00b7 \"/\"Draft N \u00b7 \" prefix, peels the \"(live)\" freshness flag off, looks the remainder up case-insensitively in LANE_COPY (build-in-public \u2192 \"Building in public\", knowledge/explainer \u2192 \"Explainer\", news-reaction \u2192 \"Reacting to the news\", quote-comment \u2192 \"Quote reply\", pov thread \u2192 \"Your point of view (thread)\", \u2026) and sentence-cases anything unmapped, with \"Draft\" as the empty-string floor. Used at the render boundary only \u2014 x-drafts-review.tsx:139 `{laneLabel(draft.avenue)}` and li-drafts-review.tsx:155 `{laneLabel(draft.lane)}` \u2014 while the raw heading is preserved for the feedback draftRef (x-drafts-review.tsx:79, li-drafts-review.tsx:101), which is what the fix required. He went further than asked and applied it to two other leak paths the finding did not name: reddit-agent-intake.tsx:377 (feedback history) and src/lib/client-agent-rows.ts:374 (the slot's picked-direction fallback, where the stored optionRef tail was lab vocabulary). asset-card.tsx no longer renders headings itself \u2014 it delegates to XDraftsBatch/LiDraftsBatch (asset-card.tsx:698, 735). Covered by src/lib/__tests__/draft-lane-label.test.ts.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "71": {
   "n": 71,
   "verdict": "partial",
   "evidenceNow": "src/components/reddit-drafts-review.tsx:308 and :435, src/components/x-agent-intake.tsx:411",
   "detail": "The high-value sweep landed. All three agent-page promises now use em dashes \u2014 x-agent/page.tsx:32 \"Drafts only \u2014 nothing posts without a human.\", linkedin-agent/page.tsx:37 \"Drafts only \u2014 a person always posts.\", reddit-agent/page.tsx:35 \"Drafts only \u2014 we never post to Reddit, you post the reply yourself.\" \u2014 and so do \"Your final version \u2014 the wording you will actually post.\" (reddit-drafts-review.tsx:304), \"Logged \u2014 the reason tunes \u2026\" (reddit-drafts-review.tsx:440), the onboarding wizard, ai-processing-banner.tsx (the JSX-text-node trap the fix warned about is handled at lines 41/65/76), and the seo-geo REC_COPY strings. chatbot-widget.tsx has zero \" - \" left. Three client-facing spaced hyphens survive: reddit-drafts-review.tsx:308 \"{n} characters - Reddit comments cap at \u2026\" (the over-cap warning, one line below a string he DID fix); reddit-drafts-review.tsx:435 \"Change requested - it feeds the agent's next pass on this account.\" (five lines above the \"Logged \u2014\" string he fixed); and x-agent-intake.tsx:411 where the takes list joins date/topic to the take with ` - `. Outside the grep scope the fix declared but still client-visible, two server-action refusals rendered verbatim in the UI also use it: src/lib/actions/asset-actions.ts:265 \"placeholder - put it on the calendar\u2026\" and :294 \"being published right now - give it a moment.\", plus the copilot tool results at src/app/api/clients/[id]/chat/route.ts:466/577/683.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The residue is pure copy judgement across surfaces a grep cannot fully separate from arithmetic; a human should do one final read-through of the Reddit drafts reader and the X intake takes list, plus decide whether server-action refusal strings count as in-scope client copy."
  },
  "72": {
   "n": 72,
   "verdict": "fixed",
   "evidenceNow": "src/components/sidebar.tsx:60, src/app/(app)/tasks/tasks-body.tsx:191, src/components/notification-bell.tsx:346, src/components/client-home-overview.tsx:153",
   "detail": "All four renames landed exactly as specified. The staff sidebar nav item is now `{ href: \"/tasks\", label: \"Workspace\", icon: \"SquareCheck\" }` (sidebar.tsx:60); the staff cross-client page header is `<PageHeader title=\"Workspace\" description=\"Every client's board in one place.\" />` (tasks-body.tsx:191), matching the two scoped headers already titled \"Workspace\" (tasks-body.tsx:40, 153); the bell footer reads \"View workspace \u2192\" (notification-bell.tsx:346); and the dashboard attention hint reads \"Open items on your workspace board.\" (client-home-overview.tsx:153). The client rail item was already \"Workspace\" (client-rail.tsx:86). The copilot prompt text and chat-route strings were left alone, which is what the instruction said to do \u2014 so \"task board\" still appears in copilot tool output (chat/route.ts:466, 577, 685) and \"Task Map\" persists as the copilot chip and banner vocabulary; both were explicitly out of scope.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "73": {
   "n": 73,
   "verdict": "fixed",
   "evidenceNow": "src/components/activity-timeline.tsx:504-515, :93, :156, :184",
   "detail": "Empty state now reads \"Every agent run, brand update, and competitor change shows up here as your team works.\" with no mention of notes or Intel Reports (lines 501-506), and a \"Run an agent\" link to `/clients/<clientId>/agents` renders when `!isStaff` (507-515) \u2014 a route the client rail itself uses (client-rail.tsx:84), so the target is reachable for a CLIENT_USER. \"campaign drafted\" is gone: staff rows read `${j.agentName} delivered a draft` (line 93) and client rows go further than the fix asked, collapsing to one row per agent per day titled `${day.agentName} worked on your content` (line 156) with failures as `${j.agentName} couldn't finish a run` (line 136) \u2014 a better answer than the instruction's \"delivered a draft\", since a draft is exactly what a client never receives. \"Intel Report generated\" is now \"Research report ready\" (line 184). EVENT_CONFIG.label was correctly left alone; I confirmed `cfg.label` is never read (only cfg's icon/tone are, at line 336).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "136": {
   "n": 136,
   "verdict": "fixed",
   "evidenceNow": "src/components/tasks-board.tsx:867-868 and :309-317",
   "detail": "Real status columns now exist: the board renders `grid grid-cols-1 gap-3 @3xl:grid-cols-2` widening to `@5xl:grid-cols-3` or `@5xl:grid-cols-4` depending on whether the Review Pending column is present (lines 866-869), one BoardColumn per status (BOARD_COLUMNS at line 38, visibleColumns at 685-688), with a stacked single column below the tablet breakpoint as the fix asked. The card is compact \u2014 status dot + label + priority badge + optional bot/client chips on one row (lines 256-284), a two-line clamped title, and an age line \u2014 with the description explicitly moved out to the ticket modal and the `title` hover tooltip (comment and code at 309-317). Breakpoints are container queries (@3xl/@5xl) rather than viewport ones, which is the right call here because the copilot rail steals horizontal space the viewport width does not know about. Drag-and-drop between columns is wired (commitStatusChange at 603, onDragEnd at 660-681), and client-owned tasks stranded in review_pending surface in Pending rather than vanishing (lines 700-707).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Whether four columns are actually legible at the real staff width with the copilot rail out \u2014 and how many cards now fit on screen \u2014 is a visual claim that only a screenshot at 1280 and 1440 can settle; the code proves the layout exists, not that it reads well."
  },
  "149": {
   "n": 149,
   "verdict": "fixed",
   "evidenceNow": "src/lib/asset-visibility.ts:97-107 and src/app/(app)/tasks/tasks-body.tsx:72-74",
   "detail": "The archive is filtered at the server boundary, not in the browser: tasks-body.tsx:72-74 wraps a client viewer's assets in `getClientLibraryAssets(getClientArchiveAssets(rawAssets), { forClient: true })` while staff keep the full library, so nothing unposted crosses into the RSC payload at all. `isInClientArchive` (asset-visibility.ts:97-107) is the single predicate: launch deliverables out, drafts out, future-dated out, and published work aged out at CLIENT_ARCHIVE_WINDOW_MS = 30 days (line 44). \"Mark as posted\" exists as one shared control mounted in both places the fix named \u2014 the asset detail modal (asset-detail-modal.tsx:374) and the calendar day card in compact form (run-calendar.tsx:546) \u2014 via src/components/mark-posted-row.tsx, which refuses locked/placeholder/unapproved assets client-side (lines 32-36) while markAssetPostedAction re-checks every one of those server-side (src/lib/actions/asset-actions.ts:263-295), including a future-date guard so a client cannot attest their way through the pre-generated batch and end redaction early. Rows are stamped and sorted by clientDeliveryStamp rather than createdAt, which closes the batch tell the generation timestamp leaked. One deliberate deviation from the instruction: approved/scheduled/delivered work does NOT age out (line 106 returns true unconditionally for those), documented as keeping \"Mark as posted\" and the per-draft reader reachable.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The 30-day window applies only to published work, so an approved post a client never marks posted stays in their archive forever \u2014 that is the deliberate deviation from \"age it out after ~30 days\" and is a product call, not a code question.",
   "newBug": false
  },
  "150": {
   "n": 150,
   "verdict": "partial",
   "evidenceNow": "src/components/asset-detail-modal.tsx:271-280, src/lib/types.ts:452-459",
   "detail": "The playback half shipped, the ingestion half did not \u2014 which is what the ledger itself claims (DEFERRED-TOMER, CD-D1 seam), so his status is honest. What exists: `Asset.videoUrl?: string | null` (types.ts:452-459, documented as GCS-backed with the agent service writing the URL), an `assetVideos()` resolver that also picks clips out of meta.videos / meta.files / meta.artifacts (src/lib/asset-images.ts:102-128), a `<video controls preload=\"metadata\">` player with the cover image as poster in the asset detail panel (asset-detail-modal.tsx:271-280) alongside the existing copy-caption path, and a play-badged archive tile (archive-view.tsx:335, 346-350, 354). What is still missing: any upload/ingestion path \u2014 no GCS write, no video upload endpoint, nothing that populates videoUrl from the portal side; and the calendar day card has no clip indicator (no assetVideos reference in run-calendar.tsx), so \"show clips on calendar like posts\" is only half done. He also declined the fix's literal instruction to add a `kind: \"video\"` variant \u2014 note the finding's evidence cited the wrong field there, types.ts:545 is ContextItem.kind, not Asset's (Asset has `type: AssetType`, which is still the five post types at types.ts:419-424). Treating a clip as media attached to a deliverable rather than a new asset type is the better modelling choice.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The remaining half is infrastructure Tomer owns (GCS bucket, agent-service upload contract); someone needs to confirm the agent service will in fact write to Asset.videoUrl in the shape assetVideos() expects before the email workaround can retire."
  },
  "17": {
   "n": 17,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1557-1575",
   "detail": "buildPromptViews now reads insights.intentPrompts and sets the chip when intent is \"brand\" || \"navigational\" \u2014 the same classifier the comparison split uses (src/lib/intel/seo-geo.ts:823-826 isCategoryPrompt), so the display-name match is gone. The chip is no longer inert: src/components/seo-geo-panel.tsx:766-774 renders tagLabel with an InfoTip carrying tagExplainer (\"\u2026We leave it out of the competitor comparison to keep that like-for-like\"), and the per-question answer grid asked for as the alternative also landed (panel:100-167, presenter.ts:1445-1491). Two caveats, neither user-visible on realistic data: (a) the presenter comment and the pin at src/lib/__tests__/seo-geo-presenter.test.ts:783-808 claim \"chip count equals brandPresence.total by construction\", which was true under the old measured-only denominator but not under methodology v2 \u2014 presenceCounts/computePresence (src/lib/seo-geo.ts:652-688) now make total the PLANNED count, so the measured-scoping at presenter.ts:1567 makes chips \u2264 total on a partial run; (b) when a grid exists but no engine measured anything, buildAnswerGridViews returns null (presenter.ts:1459) so the flat list renders, yet the measured set is empty and no chip renders at all while the comparison subtitle still cites the planned branded count. In that path the intent group heading plus basisLabel \"names you\" (panel:757-763) still carries the same information, so nothing contradicts on screen.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "18": {
   "n": 18,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo-panel.tsx:743-878",
   "detail": "The single card is now three: \"The {n} questions we asked\" (panel:744-790, still a collapsed Disclosure, which the fix allowed), \"Who we compare you against\" (panel:793-830, a plain Card with no disclosure, so open by default) and \"Who the engines quote as sources\" (panel:842-878, same). Questions are grouped under plain-English intent headings via buildIntentPromptViews (presenter.ts:1508-1526) or the grid's own groups (presenter.ts:1477-1484), reusing INTENT_VIEW ordering and never the DISC/COMP codes (presenter.ts:1391-1399). Typography: formatPrompt (presenter.ts:1432-1437) wraps each prompt in curly quotes and appends \"?\" only when the text opens interrogatively \u2014 a documented, deliberate narrowing of the spec's \"append when no terminal punctuation\", since \"karoslabs.com?\" would have been a new defect. sanitizePromptSet (src/lib/intel/seo-geo.ts:297) was left alone, which the fix marked optional.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "19": {
   "n": 19,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo-panel.tsx:855-877",
   "detail": "The `.slice(0, 8)` is gone \u2014 panel:856 maps every row the data layer returns (computeCitationLeaderboard still defaults to limit 12, src/lib/seo-geo.ts:1697), and panel:849-851 states the count and denominator (\"How many times each of these N domains was cited across the {category answers} we measured\"). The client's own citation sentence now renders unconditionally at panel:877, OUTSIDE the quotedInstead.length gate, and the zero branch renders citation.emptyLine (\"No engine cited any source domain on the answers we measured this run\", presenter.ts:362) with a separate absent-vs-zero variant for snapshots predating citationSummary (presenter.ts:346-352). The only suppression left is the F23 degraded-run gate at panel:841, which is correct \u2014 there were no answers to count citations across.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "20": {
   "n": 20,
   "verdict": "done-differently",
   "evidenceNow": "src/components/seo-geo/presenter.ts:471-493; src/components/seo-geo-panel.tsx:557-599",
   "detail": "Parts 1 and 3 landed as asked: formatCaptured emits a locale date (presenter.ts:205-213), snapshotAge adds a relative age with a 45-day staleness flag (presenter.ts:220-237), buildContextLine interpolates both (presenter.ts:251-262) and the panel paints the strip warning-toned when stale (panel:558-563). isRefreshing/intelScheduleEnabled/intelScheduleNextRunAt are all passed from src/app/(app)/clients/[id]/page.tsx:98-100, giving the in-place \"Refreshing this snapshot now\" state (panel:566-571), and ai-processing-banner.tsx:40-42 now branches on isClientViewer so the Regenerate / Refresh Task Map names are staff-only (layout.tsx:183 passes isClientViewer for the client shell). Pre-cutoff captures are marked via buildSnapshotTrust + SNAPSHOT_TRUST_CUTOFF = 2026-07-23 (presenter.ts:396-438, src/lib/seo-geo.ts:1821-1829). Part 2 was done differently: rather than rewriting the six bare \"next snapshot\" phrases with the real date, he states the schedule truth once on the capture strip \u2014 \"Next snapshot: {date}\" when one will fire, otherwise \"No refresh is scheduled yet\u2026\" plus a FlagButton \"Ask us to schedule refreshes\" (presenter.ts:482-491, panel:572-587). One authoritative statement is arguably better than six interpolations, but the phrases themselves are untouched (panel:596, 686, 826; presenter.ts:572, 669).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "The new strip line and the old copy now contradict each other inside the SAME card: panel:580 renders \"No refresh is scheduled yet, so this snapshot won't update on its own.\" and panel:596, three elements below it in that card, still renders \"We'll retry on the next snapshot.\" for the no-engines case. The roster chip \"\u00b7 next snapshot\" (panel:826) and the drift line \"are measured on the next snapshot\" (panel:686) promise the same thing on a client whose schedule is off. Copy-only, but it is a client-visible contradiction the fix created."
  },
  "22": {
   "n": 22,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1295; src/lib/seo-geo.ts:882-898",
   "detail": "Both halves landed. Sort: buildGapViews now orders `severityRank(a.severity) - severityRank(b.severity) || b.scoreLift - a.scoreLift` with SEVERITY_RANK beside SEVERITY_VIEW (presenter.ts:1148-1153, 1295), and buildRecommendations applies the identical rule at capture time (src/lib/seo-geo.ts:1334-1347) so the client plan's impact badges agree with its row order. Scales: the new visibilityLift(shortfall, target, weight) (src/lib/seo-geo.ts:895-898) puts the three visibility gaps in the same 0\u201310 band as computeCheckGaps \u2014 GEO-27 weight 10 (line 1086), GEO-35 weight 10 so a total miss is 10 \u2192 critical (line 1102), GEO-11 weight 6 so a total miss is 6 \u2192 high (line 1120) \u2014 and every gap's severity is now severityFromLift(lift) rather than a hardcoded string (lines 1092, 1108, 1126). The <7-weight-can-never-be-urgent ceiling the finding noted is documented as deliberately out of scope (comment at 878-881). Residual on legacy data: healRecommendations (presenter.ts:945-947) re-resolves title/description but not order, so a plan persisted before this merge still renders in its frozen lift-only order under the same \"Ordered by expected impact\" header (panel:389-391) \u2014 those snapshots do carry the legacy-measurement banner.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "23": {
   "n": 23,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo-panel.tsx:500; src/components/seo-geo/presenter.ts:247-256",
   "detail": "capturedNothing(insights) (presenter.ts:247-249) is computed once at panel:500 and used to suppress the \"You vs competitors\" card (panel:664), the buyer-questions disclosure (panel:743) and the citation card (panel:841). buildContextLine falls back to \"Snapshot from {date} ({age}) \u00b7 AI answer capture did not complete this run\" (presenter.ts:254-256), so the \"0 real buyer questions \u00b7 0 of 5 AI engines measured\" string can no longer render. The existing blue banner survives in the card's place: with an empty probe set every engine resolves to status \"no-data\" (presenter.ts:686-687), so noEnginesMeasured is true and panel:593-598 renders it. The presence tiles fall back to their emptyLine rather than 0-of-0 fractions (presenter.ts:822-837), and the score-tile guard at presenter.ts:176 is intact.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "133": {
   "n": 133,
   "verdict": "fixed",
   "evidenceNow": "src/lib/seo-geo.ts:1127; src/components/seo-geo/presenter.ts:353-361",
   "detail": "One unit and one scope family now, with the denominator stated on both sides. The per-engine gap reads \"Cited in {n} of {m} {Engine} category answers\" (src/lib/seo-geo.ts:1127) instead of a bare percentage, and the leaderboard footer reads \"Your site was cited as a source in {n} of {m} category answers across every engine we measured\" (presenter.ts:360, rendered at panel:877) \u2014 answers cited, not raw citation occurrences (computeCitationSummary counts probes with brandCited, src/lib/seo-geo.ts:1728-1739). Crucially the summary and the leaderboard are now computed over categoryProbes, not all probes (src/lib/intel/seo-geo.ts:867-871), so the two figures differ only by engine scope, which each sentence names. The \"cited 11 times\" phrasing is gone. The numeric gap card is additionally staff-only now (panel:406-413); the client's plan row for GEO-11 carries no number at all (src/lib/seo-geo.ts:1239).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "144": {
   "n": 144,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/gap-list.tsx:19-23; src/components/seo-geo/presenter.ts:1170-1174",
   "detail": "One word set in all three places: FILTERS is now All | Search results | AI answers (gap-list.tsx:19-23), LEVER_LABELS is \"search results\" / \"AI answers\" / \"search + AI answers\" typed Record<Lever,\u2026> so a new lever is a compile error (presenter.ts:1170-1174), CHANNEL_VIEW reads those same strings (presenter.ts:1183-1187), and the client action plan's own copy of the map matches (src/components/seo-geo-action-plan.tsx:31-35) \u2014 previously that badge rendered the raw lever code r.vertical, which the fix instruction did not mention and which was a worse instance of the same defect. He chose \"Search results\" over \"Google search\" because the underlying checks cover Bing/Brave too (documented at presenter.ts:1156-1161). No \"Search engines\" label survives outside comments; the only remaining lowercase uses are prose disambiguated by \"like Google\" (presenter.ts:149, panel:464).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "152": {
   "n": 152,
   "verdict": "fixed",
   "evidenceNow": "src/lib/__tests__/seo-geo-mounting.test.ts:55-89",
   "detail": "Restoration: seo-geo-panel.tsx:37 imports SeoGeoActionPlan and SeoGeoPlan renders it at panel:400-404 with recommendations + approvedRecIds, with GapList demoted to a staff-gated disclosure beneath it (panel:406-413). Prevention: the fix offered \"a CI check that fails when an exported component has zero importers (or a panel smoke test asserting recommendations render)\" \u2014 the second option landed as seo-geo-mounting.test.ts, which walks src/, fails on any zero-importer module in the seo-geo family, asserts the panel both imports and renders <SeoGeoActionPlan> with both props, asserts the plan sorts above <GapList> behind !isClientViewer, and asserts the pipeline still calls buildRecommendations. It runs on every PR to main (.github/workflows/ci.yml:34, `npm test`); all 170 seo-geo tests pass at HEAD. Two scope notes: the orphan sweep covers only components/seo-geo* rather than the whole repo, and the process rule (re-run the v1 sign-off checklist on any merge touching the panel) is not encoded anywhere I can verify in the repo.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "57": {
   "n": 57,
   "verdict": "fixed",
   "evidenceNow": "src/components/task-ticket-modal.tsx:411",
   "detail": "The <pre> is gone: the guide now renders through the shared renderer (`dangerouslySetInnerHTML={{ __html: renderAssetBody(plan) }}`), and it is wrapped in a Show/Hide toggle that starts folded for an already-stored plan (task-ticket-modal.tsx:353, 383-395) with a \"N sections\" summary line (countPlanSteps, :428). He used renderAssetBody rather than the instructed renderFullDoc \u2014 the better choice, since renderAssetBody strips no preamble so a leading heading or `---` in agent output survives. The Generated Deliverable block was correctly left on <pre> (:142) exactly as the instruction asked.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "74": {
   "n": 74,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/intel-actions.ts:544",
   "detail": "applyTargetedDocCorrection now returns `{ changed: boolean }`; on the `corrected.trim() === doc.content.trim()` branch it refunds via refundDocCorrection \u2192 creditClientCredits({ kind: \"refund\", chargedAt }) (intel-actions.ts:66-84, 544-555), guarded by `chargedAt !== null` which is only non-null for isBillableClientActor sessions (:47-64). The wrapper returns the exact refusal string on !changed (:499-504) and correct-info-modal.tsx:67-70 renders it in the red box and skips onSuccess, so the modal no longer closes the document on a discarded correction. applyCredit honours the refund kind and hands back window spend (src/lib/credits.ts:393-401), and applyDocCorrections still has the `return currentContent` path (src/lib/intel/pipeline.ts:705) so the branch is genuinely reachable.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "75": {
   "n": 75,
   "verdict": "fixed",
   "evidenceNow": "src/lib/active-client-context.tsx:54",
   "detail": "ClientContextSync now derives a content signature `contextDocs.map(d => `${d.id}:${d.version}`).join(\"|\")` plus a processing signature from client.isAiProcessing/aiProcessingStartedAt and a competitor signature, and the effect depends on those instead of counts (:54-69). Because updateContextDocContent bumps version (src/lib/data.ts:1335-1338) and replaceClientContextDocs writes fresh doc ids, both a correction and a full regenerate change the signature, so router.refresh() re-seeds the sidebar; the Regenerate button's greyed state now also re-syncs because the lock fields are in the dependency list.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "76": {
   "n": 76,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/layout.tsx:119",
   "detail": "All three edits landed and the gates are right: the client shell fetches `listClientContextDocs(user.clientId, \"client\")` (layout.tsx:119) so internal and internal-only text never enters the RSC payload ClientRail serializes; pickDoc's internal fallback is behind `allowInternalFallback`, defaulted false (client-documents.tsx:80-104, 1007/1028) and passed only from the staff sidebar (sidebar.tsx:553, 676) \u2014 client-rail.tsx:159/261 omits it; and the chat route filters `contextDocs.filter(d => d.tier === \"client\")` for CLIENT_USER before building the prompt (chat/route.ts:124-132), with copilot-context.ts:85-91 also fixed to genuinely prefer internal rather than \"first row seen\".",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "The new `kind: \"rebuilding\"` row (client-documents.tsx:91, rendered at :1113-1122) is dead code and the client-facing half of the instruction is therefore unmet in effect. It only fires when allowInternalFallback is false AND an internal-tier twin is present, but the only mount that passes false (client-rail) is now handed client-tier rows exclusively, so internalTier is always undefined and the pick resolves to `{kind:\"none\"}`. Net effect for a client whose client-tier copy is missing or empty: the document silently disappears from the nav with no explanation, instead of the promised \"This document is being rebuilt\" row."
  },
  "77": {
   "n": 77,
   "verdict": "fixed",
   "evidenceNow": "src/lib/intel/pipeline.ts:775",
   "detail": "Part 2 is real: runOnboardPipeline loads listClientDocCorrections(clientId) (non-fatal on error), maps them to DocCorrection with global scope applying to every doc (pipeline.ts:775-788), and buildCorrectionsBlock injects them into each generation prompt under \"\u25c8 VERIFIED CLIENT CORRECTIONS \u2014 ABSOLUTE GROUND TRUTH\" ranked above CLIENT CONTEXT (:471-481, 494, and the hierarchy at :513). Part 1 landed as different, now-truthful copy \u2014 \"This rebuilds every document from scratch. Corrections you've applied are carried into the new versions.\" (client-documents.tsx:708-711) \u2014 rather than the instructed \"\u2026will be lost\", which would have been false after part 2. The optional third part was not done: replaceClientContextDocs still deletes every row and writes version 1 (src/lib/data.ts:1308-1317), so there is still no version history or diff view.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The carry-forward is prompt-mediated, not deterministic, yet the modal states it as a fact to an admin. Worse, listClientDocCorrections returns up to 100 rows with no de-duplication or supersession logic (data.ts:1576-1586), so a fact corrected twice injects BOTH versions as \"absolute ground truth\" in the same prompt, and every correction ever made is re-injected on every future run forever.",
   "newBug": false
  },
  "78": {
   "n": 78,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/intel-actions.ts:265",
   "detail": "generateIntelReportAction keeps requireAdmin + tryAcquireAiProcessingLock synchronous (so a second click still gets the immediate \"already running\" error) and moves runIntelReportPipeline / updateClient / logActivity / releaseAiProcessingLock into after(), returning straight away (intel-actions.ts:262-299); the finally block passes the failure reason to releaseAiProcessingLock so the reason survives now that nothing throws to the caller. The modal matches: the button reads \"Starting\u2026\" and onSuccess fires as soon as the action resolves (client-documents.tsx:662-673, 762), the body says the run \"continues in the background \u2014 you can close this and keep working\" (:718-721), and the lock disables both Regenerate entry points (:1064, regenerate-workspace-button.tsx:35, both admin-gated at clients/[id]/page.tsx:122).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The fix now depends on infrastructure that is not in place: cloudbuild.yaml:139-142 sets --memory/--cpu/--timeout/--concurrency but no CPU-allocation flag, so under Cloud Run request-based billing the CPU is throttled once the response is sent and the multi-minute after() pipeline is best-effort \u2014 it can be starved or killed while holding the lock for the full 20-minute stale window. The implementer flagged this himself (docs/qa-sweep-2026-07/TOMER-HANDOVER.md:495-508: \"Add --no-cpu-throttling to the deploy args\"); agent-service/cloudbuild.yaml:92 already has it, the portal's does not.",
   "newBug": false
  },
  "79": {
   "n": 79,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:73",
   "detail": "All three edits landed: hasBody() requires stripDocPreamble(content).length > 40 and is applied inside pickDoc's tier lookups (client-documents.tsx:73-85), so an empty row can no longer produce a nav item; DocOverlay branches on an empty render with \"This document has not been generated yet \u2014 ask your Karos team to regenerate it.\" (:531-534); and the pipeline filters blank condensed entries before writing, with a warn log (src/lib/intel/pipeline.ts:917-924), so the empty row is never stored in the first place.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "80": {
   "n": 80,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:434",
   "detail": "The drawer now calls parseDocSections(doc.content) and renders a two-pane layout \u2014 a sticky `w-44` Contents column of anchor buttons (with a horizontally scrolling chip row under md) beside `<section id=\u2026>` bodies rendered through renderSectionBody, keeping the pre-first-heading lead-in via leadIn() (client-documents.tsx:434-435, 536-592, 396-407); both previously-dead helpers now have a caller, so placeholder-only sections are dropped for free by parseDocSections' PLACEHOLDER_RE (doc-render.ts:247-249). One instructed element was not implemented: nothing folds \u2014 seo-geo/disclosure.tsx is not used and every section renders expanded; the index is the whole navigation story. Documents with fewer than two sections keep the old single-pass renderFullDoc render (:435, 594-599).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "81": {
   "n": 81,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/intel-actions.ts:565",
   "detail": "After the write, applyTargetedDocCorrection computes the sibling tier (client\u2194internal, null for internal-only) and, inside after(), re-applies the same corrections to that twin with getClientContextDocByTier + applyDocCorrections + updateContextDocContent, swallowing errors so the landed correction can't fail (intel-actions.ts:557-586). The tier argument is now required on getClientContextDoc (src/lib/data.ts:1262-1268) and every caller names one \u2014 x-agent-actions.ts:252-253 now asks for \"client\", so the X agent reads the corrected copy; branding.ts:780-781, branding-actions.ts:74-75, transcripts/ingest.ts:196 and chat/route.ts:297 name \"internal\"/\"internal-only\" deliberately. copilot-context.ts:85-91 also now really prefers internal instead of keeping whichever row Firestore returned first.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false,
   "newBugDetail": "Not a new bug, but noting: the propagation block was re-implemented inline rather than lifted, so applyDocCorrectionAction (intel-actions.ts:621) is still dead \u2014 no component in src/components calls it \u2014 and the same internal\u2192client propagation now exists in two places."
  },
  "82": {
   "n": 82,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:128",
   "detail": "renderForPrint now opens with `let out = esc(stripPipelineMarkers(markdown))` before any tag is generated (client-documents.tsx:124-133), so angle-bracketed template placeholders print as text instead of being parsed as markup and dropped, and a stray script/img tag in a document can no longer execute in the print window. The dependent rules were adjusted correctly for the escape: the blockquote rule matches the escaped `^&gt;` (:203) rather than a `^>` that could never fire, and the table/bold/link passes all run on pipes and asterisks that esc() leaves alone.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "83": {
   "n": 83,
   "verdict": "fixed",
   "evidenceNow": "src/lib/doc-render.ts:275",
   "detail": "All three rules landed in renderSectionBody and are mirrored in the print renderer: `^#{4,6}\\s+` renders as a small bold label ABOVE the ### rule (doc-render.ts:275-282), the bullet and ordered-list rules accept leading whitespace and mark indented items with a \\x06 sentinel for an `ml-4` class (:348-381), and a link pass with an isSafeHref scheme guard runs last, refusing javascript:/data:/protocol-relative targets (:55-72, 410-414). renderForPrint carries the same three (client-documents.tsx:135, 179-198, 215-217). src/lib/__tests__/doc-render.test.ts:200-230 pins the four-hash heading, the nested bullet, the link and the javascript: refusal; all 56 tests pass.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "84": {
   "n": 84,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:843",
   "detail": "ScheduleModal derives `edited` by comparing enabled/intervalMonths/dayOfMonth against the incoming schedule prop and shows the SAVED schedule.nextRunAt labelled \"Next run\" while clean, switching to computeFirstIntelScheduleRun(dayOfMonth) labelled \"Next run after saving\" once edited (client-documents.tsx:843-855, rendered :954-959). A saved schedule with a null stored next run is folded into the preview branch so it also gets the preview label rather than a bare date, and the preview correctly ignores intervalMonths because updateIntelScheduleAction re-anchors to computeFirstIntelScheduleRun on save (intel-actions.ts:321).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "86": {
   "n": 86,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:491",
   "detail": "The viewer header carries \"Updated {formatDate(doc.updatedAt)} \u00b7 v{doc.version}\" (client-documents.tsx:491-493), the sticky Contents index answers the navigation half (:537-579), and generateDocSummaryAction \u2014 which had no caller anywhere \u2014 is now invoked on open and rendered as an \"In short\" bullet block above the body (:460-472, 516-530). The action's own gate is sound: a CLIENT_USER's tier argument is ignored and forced to \"client\" with no cross-tier fallback (intel-actions.ts:402-415), and the cached summary is served without a model call while summaryVersion === version (:418-420).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Someone should decide whether client-triggered summary generation should cost credits like every other client-facing AI action does.",
   "newBug": true,
   "newBugDetail": "Wiring generateDocSummaryAction to the client-facing drawer created an unmetered, client-triggerable LLM call: the effect at client-documents.tsx:460-472 fires on every DocOverlay open and the action makes a Haiku call with only logger.logUsage \u2014 no chargeClientCredits, no isBillableClientActor branch (intel-actions.ts:422-476), unlike the 1-credit chat message and 2-credit correction. Each new document version re-triggers it for every document a client opens, and because the cache write happens in after() (:462-474), rapid repeat opens before that write lands each cost a fresh call."
  },
  "138": {
   "n": 138,
   "verdict": "fixed",
   "evidenceNow": "src/lib/intel/templates.ts:25",
   "detail": "The literal numbers are out of the templates \u2014 every `## 1. Voice in one line` style heading is now `## Voice in one line` (templates.ts:25-92 and throughout; confirmed against ddcef3e by diff) \u2014 and numbering is positional at render time: the drawer prints `{i + 1}. {stripHeadingNumber(s.heading)}` in both the index and the section heads (client-documents.tsx:555, 576, 587) and buildPrintWindow numbers from the same parseDocSections list so the PDF cannot disagree (:228-236). stripHeadingNumber (doc-render.ts:50-52) strips a baked-in \"3. \"/\"3) \" prefix so documents generated before the change cannot show two numbers.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false,
   "newBugDetail": "Minor residue, not introduced by the fix: the two paths that bypass parseDocSections keep legacy baked-in numbers untouched \u2014 a stored doc with fewer than two sections renders via renderFullDoc (client-documents.tsx:594-599), and downloadMarkdown (:302-318) exports the raw stored markdown, so a pre-change document still exports \"2. Five voice adjectives\"."
  },
  "139": {
   "n": 139,
   "verdict": "fixed",
   "evidenceNow": "src/components/correct-info-modal.tsx:120",
   "detail": "The placeholder is now 'e.g. \"Our pricing is $49/mo, not $99/mo\" or \"Our head office is in Tel Aviv, not London\"' \u2014 the teammate's name and the founder framing are gone, replaced by exactly the neutral example the fix asked for.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "140": {
   "n": 140,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-documents.tsx:916",
   "detail": "The label is now \"Repeat every\" with the unit sitting beside the number input and pluralising off the value (`{intervalMonths === 1 ? \"month\" : \"months\"}`), so the field reads \"Repeat every [3] months\" (client-documents.tsx:914-931); the three-underscore fill-in-the-blank string no longer exists anywhere in the file.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "1": {
   "n": 1,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo-panel.tsx:400-405",
   "detail": "SeoGeoActionPlan is now imported (seo-geo-panel.tsx:37) and rendered as the primary \"What we're fixing\" view inside the new SeoGeoPlan component with clientId / healed recommendations / approvedRecIds; GapList is demoted behind `!isClientViewer` into a \"Technical detail \u2026 (staff only)\" Disclosure (seo-geo-panel.tsx:406-413). SeoGeoPlan is mounted for clients at src/app/(app)/clients/[id]/page.tsx:165 (isClientViewer) and for staff through SeoGeoPanel (seo-geo-panel.tsx:734). Approved state survives reload: it is seeded from insights.approvedRecIds, persisted in a transaction (src/lib/data.ts:983-995) and carried across re-captures (src/lib/data.ts:973), and the action's role gate is correct \u2014 CLIENT_USER only for their own clientId, otherwise staff/admin (src/lib/actions/intel-actions.ts:197-203). A `trust.planPending` empty state covers snapshots with no persisted plan (seo-geo-panel.tsx:393-398). src/lib/__tests__/seo-geo-mounting.test.ts:67-80 pins the mount and the plan-before-GapList order.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "3": {
   "n": 3,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1307-1321",
   "detail": "All three sub-defects are addressed. (a) The audit model's evidence string is markdown-stripped, whitespace-collapsed, sentence-cased and terminally punctuated at the server boundary by normalizeEvidence (src/lib/seo-geo.ts:908-925), applied in sanitizeChecks (src/lib/intel/seo-geo.ts:186); the raw-evidence line is additionally now staff-only, since GapList sits behind the isClientViewer gate. (b) goalLine is dropped when the benchmark equals either the raw registry label or the resolved title (presenter.ts:1321), so the \"what good looks like\" duplicate is gone for every site check. (c) The card title resolves through REC_COPY[id.split(\":\")[0]] and the registry/model label is demoted to a secondary `technicalLabel` mono line shown only when expanded (presenter.ts:1307-1312, gap-list.tsx:48-52); the client-facing plan uses resolveRecCopy, which returns REC_FALLBACK rather than a model label for invented ids (src/lib/seo-geo.ts:1304-1316).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "4": {
   "n": 4,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1208",
   "detail": "FIX_ROUTES[\"agent-direct\"] now reads \"Karos drafts this fix for your approval.\" and a header comment forbids restoring \"automatically\" until an apply path writes artifactRef (presenter.ts:1200-1206). A repo grep finds \"automatically\" nowhere else in the SEO/GEO surfaces except the competitor-pool sentence at seo-geo-panel.tsx:715, which describes real behaviour. The promise is also now backed by a real control: the route sentence itself is staff-only (GapList), and the client's view of the same list is the Approve row on SeoGeoActionPlan, which calls approveSeoGeoRecommendationAction and logs to the activity timeline. The plan additionally states, before the click, that approval is authorization and nothing on the site changes (seo-geo-action-plan.tsx:152-155).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "7": {
   "n": 7,
   "verdict": "done-differently",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1250-1262, 1302-1327",
   "detail": "He did not re-point the chip at the agents page as instructed. Instead: (1) the chip is permanently retired \u2014 GapView.agentChip is hardcoded null (presenter.ts:1327) and pinned null by seo-geo-presenter.test.ts, with the linked chip removed from gap-list.tsx (see the comment at gap-list.tsx:88-94); (2) the id map was rebuilt as REC_PRODUCTS keyed off the rec id (not `delivery`) onto nine ids the producers actually emit, and the executing product's catalog name is folded into the plain fixRoute sentence \u2014 \"\u2026Produced by the {product} managed product.\" (presenter.ts:1326); (3) the handoff a client can act on is the Approve control on the newly-mounted action plan, not a link. His reasoning holds at HEAD: /clients/[id]/agents renders only granted custom agents and their umbrellas plus a \"No active agents yet\" empty state, so a \"Handled by your Blog agent\" link would still be a dead end. Better than the instruction on the link question. Not done: the \"Draft a reply about this\" runCustomAgentAction button for LinkedIn/Reddit-shaped gaps \u2014 deliberately skipped and documented (presenter.ts:1243-1248) because the panel is not passed client.customAgentIds.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "The requested agent handoff was deliberately dropped rather than built: no card in \"What we're fixing\" can start a run, only an approval request. That is a product decision (the 27 Jul call asked for handoff into the agents), so Daniel should confirm the approval-only route is acceptable for launch.",
   "newBug": false
  },
  "9": {
   "n": 9,
   "verdict": "fixed",
   "evidenceNow": "src/lib/seo-geo.ts:1222-1263",
   "detail": "REC_COPY is now exported and covers every registry id: I enumerated SEO_CHECKS + GEO_READINESS_CHECKS at HEAD (35 distinct ids) and found zero uncovered, matching the \"coverage is the contract\" comment at seo-geo.ts:1215-1220 and the unit pin in seo-geo.test.ts:789. The card title in the staff gap list resolves through it (presenter.ts:1307) and the client's plan resolves through resolveRecCopy with REC_FALLBACK for model-invented ids (seo-geo.ts:1304-1316), so no engineering label such as \"LCP p75 under 2.5s\" can become a headline. The orphan component was mounted rather than deleted (seo-geo-panel.tsx:400). The instruction's optional extra \u2014 a `description` field on GapView \u2014 was not added, but GapView now carries technicalLabel plus the normalized found line and is staff-only, so the client-prose requirement is met by the plan's REC_COPY descriptions.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "10": {
   "n": 10,
   "verdict": "fixed",
   "evidenceNow": "src/lib/seo-geo.ts:493-502",
   "detail": "Both defects and the ghost-chip mismatch are fixed. engineVisibilityScore now reads categoryMetrics(e) for every term, so the headline index shares the denominator with the cards below (seo-geo.ts:493-502); computeVisibilityIndex only scores engines whose category set has measured prompts (seo-geo.ts:533-537). The sentiment term is gated on `c.mentionRate > 0` (seo-geo.ts:500), so zero presence now scores 0 instead of the old 5/100 floor. The ghost chip reads cat.ghostCitationRate (presenter.ts:760-762), the same scope as the \"cited as a source\" line in its card. The tile explainer states the denominator and the engine count (presenter.ts:176) and drops the \"fixes below move this\" claim on unscoped legacy records (presenter.ts:177). Residual, disclosed: a pre-merge snapshot's stored geoVisibilityIndex was computed under the old maths while the \"Score by engine\" breakdown recomputes with today's function (presenter.ts:187-193) \u2014 both pipeline and methodology stamps were bumped to 2026-07-29, so every such snapshot renders under the buildSnapshotTrust legacy banner telling the client not to read it as current.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "11": {
   "n": 11,
   "verdict": "partial",
   "evidenceNow": "src/components/seo-geo/presenter.ts:945-947",
   "detail": "The dedupe landed twice for gaps: dedupeGapsByRecId collapses registry duplicates at the source (src/lib/seo-geo.ts:1038-1058, applied at src/lib/intel/seo-geo.ts:878-884, keeping the higher scoreLift and promoting the survivor to lever \"BOTH\") and again at render in buildGapViews (presenter.ts:1291) so the staff list cannot regress. What is missing is the same guard on the surface F11 was actually observed on: the client's \"What we're fixing\" is now the action plan, which renders insights.recommendations straight through healRecommendations (presenter.ts:945-947, seo-geo-panel.tsx:386) with no dedupe. For any snapshot captured before this merge \u2014 i.e. every existing client until a refresh \u2014 the stored plan still contains the pre-dedupe rows, and re-resolving their copy through today's REC_COPY makes them textually identical while keeping their contradictory impact and channel badges. src/lib/seo-geo-import.ts:235-236 passes imported gaps/recommendations through unfiltered, so imported snapshots can carry duplicates too.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "healRecommendations (presenter.ts:945-947) heals copy without deduping, so a legacy snapshot shows the same action twice on the client's plan with contradictory chips. Reproduced against HEAD: for BOTH-09 (no REC_COPY entry at ddcef3e, so the two registries stored two rows titled \"Valid XML sitemap, referenced in robots.txt, no noindexed entries\" and \"Sitemap valid + referenced\"), healRecommendations returns two rows both titled \"Publish a clean map of your site\" with the same description, one badged high/search results and one medium/AI answers. Same for BOTH-02, BOTH-03 and GEO-01. Both rows share recId \"BOTH-09\", so approving one flips both \u2014 visually it is exactly the two-cards-one-defect symptom F11 reports, now on the client-facing plan the F1 fix just mounted. Fix: dedupe by recId (and by resolved title) inside healRecommendations, keeping the higher-impact row."
  },
  "12": {
   "n": 12,
   "verdict": "fixed",
   "evidenceNow": "src/components/seo-geo/presenter.ts:1445-1491",
   "detail": "buildAnswerGridViews reads insights.answerGrid, maps every CellState through the closed CELL_VIEW map (\"Named first\" / \"Named\" / \"Used your site, didn't name you\" / \"Not named\" / \"Not measured\", presenter.ts:1377-1384), orders columns by ENGINE_ORDER, drops engines with nothing but unavailable cells, groups rows under the plain-English INTENT_VIEW headings with a category/names-you basis label, and returns null for pre-grid snapshots. It is genuinely mounted: seo-geo-panel.tsx:490 builds it and the questions Card renders <AnswerGrid> inside the disclosure at seo-geo-panel.tsx:746-751, falling back to the grouped flat list when the grid is absent. The table itself lives in an overflow-x-auto container with sr-only per-cell labels and a legend (seo-geo-panel.tsx:100-167). Raw answer text is still not surfaced, as the instruction required.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "16": {
   "n": 16,
   "verdict": "fixed",
   "evidenceNow": "src/lib/seo-geo.ts:942-944",
   "detail": "leverFromId now treats the registry's own lever as authoritative and only overrides on a literal \"BOTH-\" prefix, so GEO-01, GEO-02, GEO-17 and GEO-20 scored in SEO_CHECKS come out lever \"SEO\" instead of AI-only; and because all four sit in both registries, dedupeGapsByRecId promotes them to \"BOTH\" (seo-geo.ts:1054-1058), which gap-list.tsx:104-105 shows under both tabs. Row counts were added to the filter tabs (gap-list.tsx:119-137). Note the tab itself is now staff-only (GapList behind isClientViewer), but the same lever feeds the client plan's channel badge (seo-geo-action-plan.tsx:116), so the fix matters on both surfaces. Residual dependency: the promotion to BOTH only happens when the audit model returns the shared id in both `seoChecks` and `geoChecks` \u2014 the prompt says each id must appear \"exactly once in the JSON\" (src/lib/intel/seo-geo.ts:253), and the F11 screenshot evidence shows it does emit both today, but a run that emits a shared id only once would file it by that registry's lever alone.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "The lever change silently empties the markdown briefs of the four promoted checks. buildSeoBrief filters `g.lever === \"SEO\"` (src/lib/intel/seo-geo.ts:637) and buildGeoBrief filters `g.lever === \"GEO\"` (src/lib/intel/seo-geo.ts:734); neither accepts \"BOTH\". At ddcef3e, GEO-01/GEO-02/GEO-17/GEO-20 carried lever \"GEO\" and appeared in the GEO brief's prioritized gap list. At HEAD they are promoted to \"BOTH\" by the dedupe and are therefore excluded from BOTH briefs \u2014 the gaps vanish from the markdown fed to downstream doc generation and the client report (src/lib/intel/report.ts also reads insights.gaps). No other consumer of `lever` was missed (a repo grep finds only these two equality filters). Fix: make the two filters `g.lever !== \"GEO\"` / `g.lever !== \"SEO\"`, or include \"BOTH\" explicitly."
  },
  "5": {
   "n": 5,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/layout.tsx:180, src/app/(app)/layout.tsx:242, src/components/tasks-board.tsx:867-868",
   "detail": "Both shells now wrap page content in `@container` (client shell layout.tsx:180, staff shell layout.tsx:242) and every named grid was converted to container variants: tasks-board.tsx:867-868 (`@3xl:grid-cols-2` + `@5xl:grid-cols-3/4`), seo-geo-panel.tsx:355/692/699 (`@xl`/`@2xl`/`@4xl`), archive-view.tsx:290, and integrations-tab.tsx:185 \u2014 the two duplicated platform grids were refactored into one shared ChannelSection grid, so the single `@2xl:grid-cols-2 @4xl:grid-cols-3` covers both former call sites. Tailwind v4 (package.json:39) needs no plugin.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": true,
   "newBugDetail": "The `@container` wrappers set `container-type: inline-size`, which applies layout containment and therefore makes those wrappers the containing block for `position: fixed` descendants. `TaskTicketModal` (mounted from src/components/tasks-board.tsx:890, i.e. INSIDE the wrapper) is the one dialog in the repo that is NOT portaled: src/components/task-ticket-modal.tsx:631 renders `className=\"fixed inset-0 z-[9999] flex items-center justify-center p-4\"` directly in the React tree. Its inset-0 now resolves against the max-w-6xl content column (whose height is the whole scrolled page) instead of the viewport, so the backdrop no longer covers the rail/dock and the max-h-[90vh] panel is centred against the full page height \u2014 opening a ticket from the top of a long board can place the dialog far below the fold. Every other overlay in the repo uses createPortal for exactly this reason (see the note at src/components/contact-us-modal.tsx:96-97, \"containing block doesn't offset this fixed overlay\"). Same hazard, smaller impact, at src/components/seo-geo/score-popover.tsx:72, whose `fixed inset-0` click-catcher now only covers the content column.",
   "needsHumanEyes": true,
   "humanEyesWhy": "Open a task ticket on /tasks at 1280px after scrolling down the board \u2014 confirm whether the dialog is still viewport-centred. The containment/fixed-positioning interaction is a CSS-spec consequence I could only verify by reading, not by rendering."
  },
  "46": {
   "n": 46,
   "verdict": "fixed",
   "evidenceNow": "src/components/asset-detail-modal.tsx:122-137, src/components/asset-detail-modal.tsx:285-315",
   "detail": "The preferred fix landed exactly as specified. AssetDetailModal now runs the three sniffs in the load-bearing order (LinkedIn \u2192 Reddit \u2192 X, asset-detail-modal.tsx:122-137, with a comment explaining why LinkedIn/Reddit must precede X) and renders LiDraftsBatch / RedditDraftsBatch / XDraftsBatch in place of the plain paragraph, passing clientId, optional jobId and assetId (lines 288-314). The modal is what archive-view.tsx:317 opens for every tile, so the loop is reachable for a client; none of the three reader components carries a role gate, and their actions (addXDraftFeedbackAction, addLiDraftFeedbackAction, addRedditDraftFeedbackAction) authorize via requireClientAccess (x-agent-actions.ts:137, linkedin-agent-actions.ts:135, reddit-agent-actions.ts:188). The intake copy was also updated to name the approval step and deep-link the archive (x-agent-intake.tsx:686-691, linkedin-agent-intake.tsx:644-649, reddit-agent-intake.tsx:309-314), and that `?tab=archive` link now works because progress-view.tsx:70-81 seeds and follows the tab param.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "47": {
   "n": 47,
   "verdict": "fixed",
   "evidenceNow": "src/components/asset-detail-modal.tsx:394-404, src/lib/asset-visibility.ts:101-109",
   "detail": "Both halves are closed. (1) Rendering: parsed batches get the per-draft reader (asset-detail-modal.tsx:285-315) and everything else goes through AssetContentBody (lines 394-404), which routes Markdown-shaped content through `renderAssetBody` (HTML-escaped first) and keeps whitespace-pre-wrap only for plain captions. The residual the ledger logged (literal asterisks in X-reader metadata) is also closed \u2014 stripInlineMarkdown is applied at every metadata render point in x-drafts-review.tsx:158/197/351 and its LinkedIn/Reddit twins. (2) The approval lie: isInClientArchive returns false for `status === \"draft\"` (asset-visibility.ts:106), applied at the server boundary in tasks-body.tsx:73, and the calendar filters drafts for clients too (calendar-body.tsx:184). The run-dialog sentence was rewritten to match (custom-agents.tsx:1498-1500).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "48": {
   "n": 48,
   "verdict": "fixed",
   "evidenceNow": "src/components/tasks-board.tsx:103-112, src/lib/actions/settings-actions.ts:31-38",
   "detail": "He took option (b) and took it cleanly. `updateAutopilotAction` is gone; `runPendingTasksBatchAction` (settings-actions.ts:39) is documented and implemented as one-shot and persists nothing, and the control is now `RunPendingTasksButton` labelled \"Run up to 5 pending tasks now\" (tasks-board.tsx:108, 148-151) with no switch and no stored state. `ClientSettings.autopilot` survives only as an `@deprecated` field on the type (src/lib/types.ts:1352-1359) with nothing reading or writing it \u2014 grep confirms zero other references \u2014 and `autopilotEnabled` is no longer threaded from tasks-body.tsx.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "50": {
   "n": 50,
   "verdict": "partial",
   "evidenceNow": "src/components/chatbot-widget.tsx:65-74, src/lib/agent-swarm.ts:456 and :487",
   "detail": "The copy half (option a) is done: the sublabel now reads \"Rebuild your task map from calendar gaps and past performance\" (chatbot-widget.tsx:73), the icon is hard-coded to ListTodo with the hasGoogleIntegration/Globe branch removed (line 72; the prop is kept on the chain and explicitly voided at line 494), and the unreachable trigger string is deleted (the scan_inbox entry has no `trigger`, so line 436 falls through to onRefreshTaskMap()). Option (b) was correctly not attempted \u2014 agent-swarm.ts has no `tools:` array and no webSearch. What is still missing is the fix's mandatory rider (\"Either way, give the onboarding/re-auth coverage contract a reachable control\"): persistSwarmTasks still hardcodes `owner: \"karos_managed\"` at agent-swarm.ts:456 and :487, and the coverage contract still lives only in the chat system prompt (src/lib/ai/prompts/proactive-assistant.ts:207-262, 377), so the non-negotiable onboarding / re-authenticate checklist still cannot be produced by this button. Separately, the War Room's own claim that the panel is \"stress-testing against your analytics\" is untouched (strategy-war-room.tsx:226-227) \u2014 it was in the finding's evidence but not in the fix instruction.",
   "agreesWithLedger": false,
   "confidence": "high",
   "newBug": false
  },
  "51": {
   "n": 51,
   "verdict": "done-differently",
   "evidenceNow": "src/components/notification-bell.tsx:242-284, src/components/client-rail.tsx:201-206",
   "detail": "Rather than repointing the row at /tasks?tab=archive, he made the row non-navigable for clients: `viewerIsClient` (notification-bell.tsx:50) turns the row body into a plain div and the /jobs link is rendered only for staff (lines 272-283), with the copy changed from \"Waiting for your review\" to \"Your Karos team is reviewing it\" (lines 255-257). The gate is correct \u2014 client-rail.tsx:206 and :314 pass `viewerIsClient`, the three staff Sidebar mounts do not. This is BETTER than the instructed fix given F149: the archive now excludes drafts (asset-visibility.ts:106), so /tasks?tab=archive would have been a link to a screen that provably does not contain the row's subject \u2014 the same reasoning client-home-overview.tsx:126-138 applies to the identical count. The deep link the fix asked for was built anyway and is used elsewhere (progress-view.tsx:70-81, `?tab=` not `?view=`). Dead link eliminated either way; app-header.tsx no longer exists (the staff header strip was retired), so that half of the threading is moot.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "53": {
   "n": 53,
   "verdict": "fixed",
   "evidenceNow": "src/components/linkedin-seats-workspace.tsx:155, src/lib/credits.ts:131",
   "detail": "Both strings state the charge as it behaves. The add-seat form reads \"This is beyond your {seatLimit}-seat plan \u2014 adding a seat is a one-time {seatCost}-credit charge\" (linkedin-seats-workspace.tsx:155) and the removal warning matches (line 272). The blocked-state `reason` in evaluateSeatAddition now reads \"...is a one-time {cost}-credit charge \u2014 top up credits or upgrade your plan to continue.\" (credits.ts:131). The comment that seeded the error was rewritten to say the charge \"is not a subscription and does not recur\" (credits.ts:64-70), and the test was updated to pin it (src/lib/__tests__/seat-architecture.test.ts:30). No \"$29/mo\" survives anywhere in src/. Em dashes, not spaced hyphens.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "54": {
   "n": 54,
   "verdict": "fixed",
   "evidenceNow": "src/components/task-ticket-modal.tsx:55-59, src/lib/actions/task-actions.ts:127-129, src/components/tasks-board.tsx:704-707",
   "detail": "All three layers of the instructed fix landed. (1) The footer no longer reads the flat map directly: `nextStatusFor` (task-ticket-modal.tsx:55-59) returns \"completed\" when the flat next would be review_pending and inferOwner(task) === \"client_managed\", and the footer button uses it (line 581, used at 764). (2) Server belt-and-braces: updateTaskStatusAction refuses the move outright \u2014 \"Tasks you own go straight to Done \u2014 Review Pending is for Karos drafts.\" (task-actions.ts:127-129), keyed on inferOwnerEngine. (3) Already-stuck tasks resurface: tasksByColumn folds client_managed review_pending rows into the pending bucket while activeTab === \"client\" (tasks-board.tsx:704-707), so the count and the cards agree again.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "55": {
   "n": 55,
   "verdict": "fixed",
   "evidenceNow": "src/app/api/auth/social/[provider]/route.ts:29-47, src/lib/integrations/oauth-popup.ts:44-59, src/components/integrations-tab.tsx:449-457",
   "detail": "The popup shell was extracted to a shared module (src/lib/integrations/oauth-popup.ts, with successPage/errorPage/htmlPage plus an `esc`/`js` pair that also closes a `</script>` injection through the reflected provider segment), and all five bare `new Response(...)` returns in the authorize route are now errorPage() calls with client-readable wording and correct status codes (route.ts:29, 31, 33, 37, 41-46) \u2014 so the parent's karos_oauth_error listener fires and the card leaves \"Connecting\u2026\". The admin-only hint was also un-gated: integrations-tab.tsx:452-457 now renders for everyone, with the env-var detail for admins and \"This channel isn't connectable yet \u2014 ask your Karos team to finish setting it up.\" for everyone else; the Google card has the same treatment at lines 833-840.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "56": {
   "n": 56,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/settings/page.tsx:212-222, src/lib/actions/client-actions.ts:113-124",
   "detail": "The card is gated: `client.clientKeyId && (isStaff || user.isGroupAdmin)` (settings/page.tsx:214, with isStaff at :63 and user at :49), and because the Team tab is filtered out when its content is null (line 231) an ordinary client user does not even see the tab. The rotate control is mounted: ClientKeyInline takes `canRotate` and renders a confirm-then-replace button wired to regenerateClientKeyAction (client-key-inline.tsx:78-90, 100-118). The server action re-checks independently \u2014 staff, or a CLIENT_USER who isGroupAdmin AND whose clientId matches, with an explicit empty-id guard so a null-clientId group admin can't satisfy `null === null` (client-actions.ts:114-121). The dead ClientKeyPanel file was deleted (only client-key-inline.tsx remains).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "58": {
   "n": 58,
   "verdict": "fixed",
   "evidenceNow": "src/components/tasks-board.tsx:114-131 and :148-195, src/lib/actions/settings-actions.ts:113-143",
   "detail": "The bare toggle is gone. `previewPendingTasksBatchAction` (settings-actions.ts:113) is a read-only preview that mirrors the runner's selection exactly (both go through the shared `pendingTasksBatch` at settings-actions.ts:26) and returns count + credits + billable without claiming or charging; the button now runs askToRun \u2192 a panel printing \"Runs N pending tasks for X credits\" \u2192 a \"Run & charge X credits\" confirm (tasks-board.tsx:114-131, 161-195). Standing helper text under the switch states that it charges credits per task and that agents still being set up are skipped (lines 155-158).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": true,
   "newBugDetail": "The preview and the run diverge for a staff member using \"View as client\". Under impersonation getCurrentUser returns a CLIENT_USER, so previewPendingTasksBatchAction evaluates clientTaskRunRefusal and SUBTRACTS guard-rail-blocked tasks from `count` (settings-actions.ts:129-135) \u2014 but isBillableClientActor is false, so runPendingTasksBatchAction falls to the staff branch at settings-actions.ts:104-106 and calls runAutopilotBatch(clientId), which re-selects the batch itself (execution-engine.ts:465-471) and applies no refusal filter. The panel therefore quotes a smaller task count than what actually runs, and agents whose umbrella is not live get executed on that path. The same branch also reports `started: staffBatch.length` from an unfiltered selection.",
   "needsHumanEyes": true,
   "humanEyesWhy": "Whether the \u00a72 guard rail is supposed to bind an impersonating admin is a product call (clientAgentRunRefusal deliberately returns null for non-CLIENT_USER roles) \u2014 but the preview and the runner should at least agree on the number."
  },
  "60": {
   "n": 60,
   "verdict": "fixed",
   "evidenceNow": "src/components/client-context-bar.tsx:18-52, src/app/(app)/layout.tsx:235",
   "detail": "He built exactly what was asked and nothing more \u2014 importantly, no second role switch, so the pre-existing real impersonation in src/lib/auth.ts was not duplicated. ClientContextBar is a client component reading useActiveClient, renders nothing when no context is active, and otherwise shows \"Client context: <name> \u00b7 staff view, full visibility, runs are free\" with a labelled \"Exit client view\" button calling setActiveClient(null) then router.push(\"/clients\") \u2014 the same body as the sidebar's clear. It is mounted in the staff shell at layout.tsx:235, beside the ImpersonationBanner, so it renders for any staff member the moment a context is active. Both features were renamed: sidebar.tsx:135 is \"Client context\", team-manager.tsx:154 is \"Sign in as\".",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "61": {
   "n": 61,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/task-actions.ts:75-97, :428-441",
   "detail": "Refund, not reorder \u2014 as instructed. chargeTaskAssist now returns `chargedAt` (task-actions.ts:45-66) and a new refundTaskAssist calls creditClientCredits with kind \"refund\", operation \"task_execution\" and the original chargedAt, guarded by isBillableClientActor and swallowing its own errors so a refusal can't become a crash (lines 75-97). Both early returns refund: capacity at line 429 (\"Refund \u00b7 task queue at capacity\") and duplicate at line 440 (\"Refund \u00b7 duplicate task not created\"). The UI split also landed: the action returns `duplicate: true` (line 444) and both surfaces render it as a neutral info pill rather than red danger (chatbot-widget.tsx:337 + 379/388, quick-add-task-bar.tsx:53).",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "62": {
   "n": 62,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/competitor-actions.ts:476 and :515, src/components/client-context-sections.tsx:99-135, src/lib/active-client-context.tsx:60-69",
   "detail": "The preferred fix landed. addCompetitorByNameAction now returns `{ id, company, url?, created }` (signature at competitor-actions.ts:476, returned at :515 with a comment naming F62), and CompetitorTrack appends the row to local `addedRows` state on success (client-context-sections.tsx:107-125) \u2014 merged into the displayed list at lines 60-68, keyed on clientId so an optimistic row can't leak into another client's context across a switch. It is source \"manual\" with createdAt = now, so computeTrackedCompetitors puts it in the first slot (competitor-priority.ts:51-54). The already-tracked / report\u2192manual promotion case is surfaced explicitly instead of looking like nothing happened (line 129). ClientContextSync's dependency was also widened from competitors.length to a content signature `id:source:company` (active-client-context.tsx:60-69), so a promotion or rename re-syncs the rail.",
   "agreesWithLedger": true,
   "confidence": "high",
   "newBug": false
  },
  "147": {
   "n": 147,
   "verdict": "fixed",
   "evidenceNow": "src/lib/agent-identity-map.ts:117-150",
   "detail": "The single shared resolver the fix asked for exists and is wired everywhere. `resolveContentIdentity` resolves, in order, an explicit umbrella link \u2192 the job/schedule's customAgentId \u2192 the content FAMILY read off `asset.type` / `asset.meta.taskType` / `job.external.taskType` (FAMILY_BY_TASK_TYPE maps social_post\u2192\"social\") mapped onto the client's live umbrella \u2192 today's fallback labels. That family rung is exactly what collapses the double identity: a managed job recorded as \"Social posts (IG/TikTok)\" now prints the umbrella's displayName (\"Instagram Agent\"), which also kills the two-system \"TikTok Agent\" duplicate. Call sites all import the one module: calendar-body.tsx:268 (scheduleRowLabel) and :333 (runRowLabel) for the exact stacked-chip scenario in the screenshot, jobs/page.tsx:33, tasks-body.tsx:137/142 (archive grouping + activity rows), lib/client-agent-rows.ts:106 (agent run history), lib/data.ts:1703, lib/agent-detail-archetypes.ts:76. The umbrellas are loaded once per page (calendar-body.tsx:165-173 listClientAgents, indexed by identitiesByClient) so there is no per-row read. CalendarBody is mounted by both /calendar and /clients/[id]/calendar. Two deliberate non-conversions are documented rather than missed: client-analytics.tsx:9-17 (the analytics stack never names an agent, so there was no label map to convert) and credits-panel.tsx:28-39 (the rate card is billing vocabulary, not content attribution \u2014 resolving it would leave a client unable to find the social-post rate). A surface-wiring test pins the four call sites and their imports (src/lib/__tests__/agent-identity-surfaces.test.ts:330-345), and the copilot prompt was updated to stop emitting the catalog label (src/lib/ai/prompts/proactive-assistant.ts:247).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "148": {
   "n": 148,
   "verdict": "partial",
   "evidenceNow": "src/components/client-agents/live-card.tsx:49-321",
   "detail": "Almost all of the product model landed, and it is genuinely mounted. Types: ClientAgentLaunchState / ClientAgentTemplate / ClientAgent (with templates[] + rotation[]) / AgentSlot (templateKey + note) / ClientAgentFeedback at src/lib/types.ts:1649-1900. UI: /clients/[id]/agents/[agentId]/page.tsx mounts ClientAgentLaunchCard (launch-card.tsx) and AgentDetailPanel, which in turn mounts TemplateRows/WeekStrip/OptionsRow/StaffSlotNotes (live-card.tsx), SlotNoteModal, OptionPicker and ClientAgentFeedbackModal \u2014 I checked the import chain rather than trusting the components existed. Per-template Run at the agent price is live-card.tsx:222-231 (`Run now \u00b7 {agent.runCost} credits`), backed server-side by runClientAgentTemplateAction (src/lib/actions/client-agent-run-actions.ts:82-135) which re-runs the same pure gate the button was painted from, and role-gates via requireClientAccess + isCustomAgentGrantedToClient. Two-level feedback is real: agent-detail-panel.tsx:88-89 carries `{scope:\"agent\"} | {scope:\"template\",\u2026}`, the global control is at :292 and the per-template one at :260-262. Rotation reorder is live-card.tsx:124-137. Launch-vs-recurring cost split exists as Job.runType (types.ts:396, stamped in submit-custom.ts:302/384, honoured by the webhook at route.ts:226/264) and is bucketed for the economics card by src/lib/credit-reporting.ts:127-165 + the launch-price calibration at :204-223, rendered by AgentEconomicsCard. WHAT IS MISSING: the fix asked for a slot note \"consumed at run time\". Notes are captured and guarded (src/lib/actions/slot-note-actions.ts:60-80) and surfaced to staff, but nothing in a run path ever consumes one \u2014 `consumedAt` is only ever set by the manual staff markSlotNoteAppliedAction (slot-note-actions.ts:125-128), and src/app/api/run-scheduled/route.ts contains no slot or note handling at all, so the client's echo \"Your Karos team applied this to that day's post\" (src/lib/slot-notes.ts:73-74) is truthful only because a human does it. Also unbuilt in this branch: the run-scheduled cron is not slot-aware, so day-of generation per slot is not automated. Both gaps are honestly recorded as the CD-A3 Tomer seam in LEDGER.md:176 (RESOLVED-PARTIAL) even though F148 itself is marked RESOLVED, so the F148 row overstates by exactly that leg.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "F148 is the document's biggest work item and its ledger row reads RESOLVED while the sub-directive that owns the run-day note consumption (CD-A3) reads RESOLVED-PARTIAL. Daniel should decide whether shipping per-slot notes that only a human ever applies is acceptable for the demo, since the client-facing echo copy asserts the note was applied.",
   "newBug": false
  },
  "24": {
   "n": 24,
   "verdict": "partial",
   "evidenceNow": "src/lib/client-agents.ts:488 + src/lib/client-agent-rows.ts:147-152 + src/components/scheduled-runs.tsx:100-109",
   "detail": "The core lie is gone: toScheduleRows now projects lastError/lastErrorAt (client-redacted through clientSafeRefusal), and rosterStatus gives a stored refusal precedence over \"Live\" (client-agents.ts:488 returns tone \"attention\"/\"Needs attention\"), consumed by the roster cards (agents/page.tsx:194, :335) and by the detail page's badge and status strip (agents/[agentId]/page.tsx:257, :561). Staff also get the refusal SENTENCE plus the matching action \u2014 setup link or ContactUsButton \u2014 and a \"Last tried \u2026\" stamp in StaffAgentControls (custom-agents.tsx:689, 735-757). Two asks did not land. (1) A CLIENT still gets no reason and no action: schedule.lastError is only read by rosterStatus and by the staff-only StaffAgentControls; neither AgentDetailPanel nor LegacyAgentPanel paints it, so the client sees the words \"Needs attention\" and nothing that says why or what to do \u2014 which is the exact complaint in the finding (\"no way to learn why\"). (2) The admin card the fix named, ScheduledRunsCard, still renders label/cadence/next-run only and never surfaces run.lastError (types.ts:342 holds it, /api/scheduler/route.ts:66,90 writes it), so the legacy scheduledRuns collection keeps its silent-failure shape.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": true,
   "humanEyesWhy": "Whether a client must see the refusal text (and a support route) or whether the one-word \"Needs attention\" is the deliberate client face is a product call; the implementer's handover logs a different F24 residual and does not mention dropping the client-side sentence.",
   "newBug": false
  },
  "25": {
   "n": 25,
   "verdict": "fixed",
   "evidenceNow": "src/lib/credits.ts:264-303 + src/app/(app)/clients/[id]/agents/page.tsx:104-109 + src/components/client-agents/legacy-agent-panel.tsx:151-166",
   "detail": "bindingCreditLimit/creditBlockReason mirror the server's own denial ladder and return the per-case line (\"Weekly limit reached \u2014 resets Monday.\" / \"Monthly limit reached \u2014 resets on the 1st.\" / \"Not enough credits \u2014 ask your Karos team for a top-up.\"); the reason is computed PER AGENT because agents have distinct prices (agents/page.tsx:104-109, [agentId]/page.tsx:201-204) and only for billable client actors. Every run control now PAINTS the reason in a visible warning block instead of a title on a pointer-events-none button, with ContactUsButton on the credits rung: legacy-agent-panel.tsx:151-166, agent-detail-panel.tsx:196-215, live-card.tsx:303-313. No title attribute survives on any disabled client run button. Note: this finding's `fix` field ends with an unrelated paragraph about removing Perplexity/Copilot from the client report \u2014 a QA-doc contamination from an SEO/GEO finding, correctly ignored here.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "27": {
   "n": 27,
   "verdict": "partial",
   "evidenceNow": "src/lib/scheduled-runs.ts:52-68 + src/lib/actions/planned-run-actions.ts:238-264 + src/components/custom-agents.tsx:1197-1225",
   "detail": "The billing half landed properly: scheduleLimitsFor() returns 5 runs/week and 1 output/run for isRedditAgentIdentity, the dialog's dropdowns are built from it (countOptions(limits.maxRunsPerWeek/maxOutputsPerRun) at custom-agents.tsx:1206, 1220) and clamp stored values on the way in (1079-1090), and configureClientAgentScheduleAction enforces the same ceilings server-side (planned-run-actions.ts:239, 261-265) so a stale page or a direct POST cannot buy 7x5. The outputs selector is not hidden but offers a single option, which is equivalent. The WORDING half did not land: nothing is Reddit-aware in AgentScheduleModal's copy \u2014 the label is still \"Posts per week\" (custom-agents.tsx:1196-1200) and the client description still reads \"How often this agent posts for you\" (1152-1156) for a product where a human posts every reply by hand.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "New in this merge (CD-H8): LegacyAgentPanel hardcodes post vocabulary and is mounted for ANY unbound agent with a schedule or delivered work, including the Reddit agent (agents/[agentId]/page.tsx:640 has no archetype exclusion, and agentArchetype maps Reddit to daily_finder only for the hero). So a Reddit page renders DailyFinderPanel's correct \"reply\" language and, directly under it, \"Create a new post\", \"credits per post\", \"How often it posts\" and \"This agent is already posting for you\" (legacy-agent-panel.tsx:174-186, 138-192) \u2014 a draft-only product being sold as a posting one. AgentDetailPanel already solves this with OUTPUT_NOUN[archetype]; LegacyAgentPanel takes no archetype at all."
  },
  "28": {
   "n": 28,
   "verdict": "fixed",
   "evidenceNow": "src/components/asset-detail-modal.tsx:286-317 + src/components/x-agent-intake.tsx:684-693",
   "detail": "Option A of the fix landed: LiDraftsBatch / RedditDraftsBatch / XDraftsBatch are mounted behind the parser check inside AssetDetailModal (asset-detail-modal.tsx:288, 299, 309), which is the client's own path \u2014 archive-view.tsx:317 mounts the modal, and ArchiveView is reached at /tasks?tab=archive via ProgressView (progress-view.tsx:103, 138) from tasks-body.tsx:156. The per-draft actions are client-authorized (x-agent-actions.ts:412 uses requireClientAccess, not requireStaff), and RedditDraftsBatch carries the per-reply posted / posted-with-edits / not-posted controls the Reddit copy promises (reddit-drafts-review.tsx:315-374). The three Feedback paragraphs were also rewritten to name the approval step and LINK the archive instead of describing a screen (x-agent-intake.tsx:685-693, linkedin-agent-intake.tsx:643-651, reddit-agent-intake.tsx:306-315).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "29": {
   "n": 29,
   "verdict": "fixed",
   "evidenceNow": "src/lib/actions/asset-actions.ts:205-238",
   "detail": "approveAssetAction now calls closeProducingJobIfReviewed(asset) after the asset patch (asset-actions.ts:205); that helper loads the producing job by asset.jobId, checks every sibling in job.assetIds is past \"draft\", and writes updateJob(job.id, { status: \"approved\" }) \u2014 best-effort, wrapped so a failure cannot undo the approval (222-238). Pinned by src/lib/__tests__/approve-asset-action.test.ts:71. The stuck counter itself is recomputed from jobs with status \"review\" on each render (agents/page.tsx:298-306 for the roster note, [agentId]/page.tsx:349-358 for the per-agent count), so it now drops to zero once drafts are approved, and \"Approved\"/\"Delivered\" in JOB_STATUS_META are reachable states rather than dead rows.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "30": {
   "n": 30,
   "verdict": "fixed",
   "evidenceNow": "src/lib/types.ts:216-222 + src/app/api/agent-service/webhook/route.ts:38-45 + src/lib/actions/external-job-actions.ts:41-65",
   "detail": "\"cancelled\" is a first-class JobStatus (types.ts:222), STATUS_MAP maps cancelled to itself while failed/dead_letter stay failed (webhook/route.ts:42-45), JOB_STATUS_META gives it a neutral \"Cancelled\" badge (job-status.tsx:19), and ManagedJobProgress treats it as a neutral stop pinned to the working step rather than \"Run failed\" (managed-job-progress.tsx:23-29, 43-45). The client-side cancel exists as cancelClientAgentJobAction (external-job-actions.ts:41), authorized on the JOB's own clientId via requireClientAccess with a uniform not-found line so ids cannot be probed, and refusing anything not queued/running. It is mounted where a client meets an in-flight run: agent-detail-panel.tsx:164 and legacy-agent-panel.tsx:122 (CancelRunControl, custom-agents.tsx:961).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": true,
   "newBugDetail": "custom-agents.tsx:935 mounts <CancelRunControl runId={run.id} /> inside AgentRunHistory without passing `refunds`, so it defaults to true \u2014 and AgentRunHistory is staff-only (rows are built only when isStaff). A staff member cancelling a staff-fired run therefore reads \"Stop this run? Credits for it are returned.\" for a run that was never charged, which the component's own doc block (custom-agents.tsx:968-974) says must not happen. Both client mount sites pass the flag correctly; only this one omits it."
  },
  "31": {
   "n": 31,
   "verdict": "done-differently",
   "evidenceNow": "src/app/(app)/clients/[id]/agents/page.tsx:158-163,205 + src/app/(app)/clients/[id]/agents/[agentId]/page.tsx:511 + src/components/client-agents/legacy-agent-panel.tsx:107-123",
   "detail": "The defect is gone but not via the route the fix named, because CD-G1 deleted the client's \"Recent agent runs\" rows entirely. <AutoRefresh /> is now mounted in the client branch of the roster page whenever anything is in flight (runInFlight covers queued/running runs, in-flight launches and row.activeRun \u2014 page.tsx:158-163, rendered at :205) and on the agent detail page (:511). In place of a frozen \"Queued\" row the client gets an explicit in-flight banner with the expected duration and a Cancel control (agent-detail-panel.tsx:145-168; legacy-agent-panel.tsx:107-123, which does mount ManagedJobProgress at :119). Equivalent-or-better than the instruction: the page now moves on its own and states the wait. Residual: the umbrella panel's banner has no three-step strip, and the roster card shows no \"working now\" for a client (only the detail page's status strip does, via `running`).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "32": {
   "n": 32,
   "verdict": "fixed",
   "evidenceNow": "src/components/modal.tsx:112-140",
   "detail": "Modal is now three flex rows inside the panel: a shrink-0 header holding title/description (126-130), a min-h-0 flex-1 overflow-y-auto body that keeps the scrollRef target (134-139), and an optional shrink-0 border-t footer fed by the new `footer?: ReactNode` prop (140). The hard 720px cap became max-h-[min(calc(100dvh-3rem),1100px)] (112) \u2014 a deliberate deviation from the instruction's uncapped calc, documented in the comment. Callers adopted it: the run dialog's estimate + \"Start run\" row now lives in the footer and is suppressed on the data pane (custom-agents.tsx:1573-1587), the schedule dialog's pause/save row likewise (1155-1175), plus the intake seat modals. scrollRef survives on purpose for the pane swap (1382, 1411, 1570).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "33": {
   "n": 33,
   "verdict": "fixed",
   "evidenceNow": "src/lib/job-title.ts:13-29 + src/app/api/agent-service/webhook/route.ts:388",
   "detail": "One shared module now owns both halves: JOB_TITLE_CLIENT_SEPARATOR = \" - \" with jobTitleForClient() used by all three submit paths (submit-custom.ts:306, submit-managed.ts:122, run-custom-agent.ts:165) and assetTitleFromJobTitle() used by the delivery handler (webhook/route.ts:388), so the em-dash/hyphen mismatch cannot recur. The strip matches only the exact `agentName + separator` prefix rather than splitting blindly, and submit-managed's agentName and title are built from the same `label`, so the prefix always matches. The stale comment was rewritten (webhook/route.ts:383-387). Existing asset titles still need the one-off backfill the finding called for \u2014 consistent with the OPS-PENDING ledger status.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "34": {
   "n": 34,
   "verdict": "fixed",
   "evidenceNow": "src/app/(app)/clients/[id]/agents/page.tsx:218-233 + :366-384",
   "detail": "The shared condition is split. The never-set-up EmptyState now fires only on an empty roster (rosterEntries derives 1:1 from `agents`, so it means agents.length === 0 \u2014 page.tsx:225-233), and an unconfigured service instead shows a distinct warning banner above an otherwise intact roster: \"Agent runs are paused right now \u2014 starting a new run will not work until this clears. Your Karos team has been notified. Everything below is unaffected.\" (218-224). Staff got the same treatment in their own wording (366-372), and the staff \"Agent service not configured\" EmptyState is now reached only when there is genuinely nothing to show (379-384). Run controls stay honest during the outage because the gate ladder checks serviceConfigured first (evaluateLegacyRunGate, [agentId]/page.tsx:392-398).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "35": {
   "n": 35,
   "verdict": "partial",
   "evidenceNow": "src/components/custom-agents.tsx:154-158 (intakeDrivenLabel) vs :530-538",
   "detail": "The binding half landed: each card computes perClientAgentSlug(agent.key) and prints a \"<slug> only\" badge (custom-agents.tsx:507, 538), the run dialog receives only eligible clients (600-602), Run is disabled with an explanatory title when none are eligible (571-582), and the hub is fed agentsRepoSlug from the page (agents/page.tsx:62-66). The setup half is incomplete: intakeDrivenLabel(key) only recognises X and LinkedIn and returns null for Reddit (custom-agents.tsx:154-158), so the Reddit agent's card carries no \"Needs Reddit agent data\" badge even though submitCustomAgentJob gates on hasRedditAgentIntake and refuses with REDDIT_SETUP_REQUIRED_PREFIX \u2014 for that agent the gate is still discoverable only by writing the brief and reading the refusal, exactly the reported symptom. The alternative arm (thread xSetup/linkedinSetup/redditSetup through the hub so the dialog gets its in-dialog pane) was not taken either; the hub's refusal still offers a plain link that navigates away (custom-agents.tsx:1806-1814).",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "36": {
   "n": 36,
   "verdict": "fixed",
   "evidenceNow": "src/components/custom-agents.tsx:1445-1460",
   "detail": "submit() now falls back instead of refusing: when buildCustomAgentPrompt returns empty AND no profile field is required, it rebuilds the prompt with profile.quickStarts[0] injected into the primary field \u2014 the same text the suggestion chips insert, so an untouched form runs identically to clicking a chip (custom-agents.tsx:1452-1456). Required-field validation is untouched and still fires first with its own message (1430-1434), and the server's empty-prompt guard was not relaxed. Both affected profiles have non-empty quickStarts (custom-agent-launch.ts:253-257 LinkedIn, :392 Reddit), so the fallback always resolves. The misleading copy is gone too: the residual error now reads \"Add at least one line to the brief before starting the run.\" (1459).",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "37": {
   "n": 37,
   "verdict": "partial",
   "evidenceNow": "src/components/jobs-list.tsx:35-51,148-161 + src/components/clients-grid.tsx:364-365 + src/app/(app)/clients/page.tsx:12-14",
   "detail": "Most of it landed. /jobs moved into a client component with a status select, a client select and a search over agent name plus title, then 50 rows at a time with a \"Load N more\" button and a \"Showing X of Y\" line (jobs-list.tsx:35-51, 88-114, 148-161); the page now ships only the fields a row prints and fences out jobs of invisible clients (jobs/page.tsx:25-40). /clients gained a search box and a name / most-recent-run / most-deliverables sort (clients-grid.tsx:364-365). loading.tsx now exists for all five named routes (jobs, clients, admin/analytics, clients/[id]/settings, clients/[id]/agents). Still missing: the instruction to replace the full-collection scans \u2014 clients/page.tsx:13-14 still calls listAssets() and listJobs() over the whole database and only reduces them to per-client counts afterwards (35-48); the comment at :24-34 admits the server-side count() is a handover item.",
   "agreesWithLedger": false,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "38": {
   "n": 38,
   "verdict": "fixed",
   "evidenceNow": "src/components/custom-agents.tsx:501-503,571-582,600-602,1676-1694",
   "detail": "Implemented as written. agents/page.tsx:62-66 widens the clients prop with agentsRepoSlug; the hub computes `eligible = clients.filter(c => agentKeyMatchesClientSlug(agent.key, c.agentsRepoSlug))` per card (custom-agents.tsx:501-503), disables Run when none are eligible with a title naming the binding (571-582), and passes only the filtered list into RunCustomAgentModal (600-602). The one-eligible-client case drops the dropdown for a fixed chip showing the client name and \"\u00b7 this agent's own client\" (1676-1694). Same predicate the client page filters on (agents/page.tsx:249) and the submit core enforces, so the hub can no longer assemble a pair the server refuses.",
   "agreesWithLedger": true,
   "confidence": "high",
   "needsHumanEyes": false,
   "newBug": false
  },
  "39": {
   "n": 39,
   "verdict": "done-differently",
   "evidenceNow": "src/components/managed-products.tsx deleted (commit fbecbbf) + src/lib/execution-engine.ts:307-320 + src/components/seo-geo/presenter.ts:1326-1327",
   "detail": "He took the delete arm, but not with the copy strip the instruction asked for \u2014 and the capability is not actually dead. managed-products.tsx and submitManagedJobAction are gone (external-job-actions.ts:10-12 records why), and the jobPreviews block that fed the unmounted component was removed from the agents page (agents/page.tsx:252-257). The submitManagedJob CORE is still reachable from a browser: moving a content_generation task on the task board calls updateTaskStatusAction (tasks-board.tsx:609) \u2192 runTaskExecution \u2192 submitManagedJob for the resolved catalog product (execution-engine.ts:307-320), which is what the Connect page's revised sentence now claims (\"the catalog products run from the client's own task board, not over MCP\"). Given that, keeping the four product names in client copy is defensible rather than a broken promise, and the dead /clients/[id]/agents link was removed from the chip (presenter.ts:1245-1248, agentChip: null). So: better than the letter of the instruction, but the client-facing wording is now wrong in a different way (see newBug).",
   "agreesWithLedger": true,
   "confidence": "medium",
   "needsHumanEyes": true,
   "humanEyesWhy": "Worth confirming on screen that a client can actually get a Social/Newsletter/Blog/Landing run started from the task board (the dispatch path is a status transition plus resolveTaskProduct, not a visible \"run this product\" control), since the whole finding is about there being no front door.",
   "newBug": true,
   "newBugDetail": "presenter.ts:1326-1327 now renders \"Produced by the <name> managed product.\" into the client-facing SEO/GEO fix route. \"Managed product\" is internal portal vocabulary (the catalog's own word for the agent-service task types), which the house copy rule bars from client-facing text \u2014 a client should be told which agent or team does the work, not the internal product class."
  }
 },
 "rechecks": {}
}