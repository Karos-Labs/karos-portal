# QA sweep findings — pages 161–199

Source: Karos-portal-QA-sweep-FULL-2026-07-27.pdf, extracted 2026-07-28.
Section headers in this range: tail of a prior section (F145 fix tail + F100), then
"Settings, credits & billing" (6 findings: #101, 102, 103, 104, 105, 141), "Calendar"
(8 findings: #107, 108, 151, 109, 110, 111, 112, 142), "Shell: navigation,
notifications, roles" (12 findings: #113, 115, 116, 117, 118, 119, 120, 143, 146, 121,
122, 123), then "Killed in verification" and the closing colophon.

F99 → see findings-p123-160.md
F145 → see findings-p123-160.md

## F100 · LOW · Track A
**Title:** British and American spellings sit side by side — "Analysing" on one screen, "Analyzing" on another, "favourites" in client draft copy
**Where:** src/components/transcript-tools.tsx:179; src/components/transcript-tools.tsx:108; src/app/(app)/transcripts/page.tsx:30; src/app/(app)/transcripts/page.tsx:59; src/components/ai-insights.tsx:97; src/components/archive-view.tsx:68; src/components/client-analytics.tsx:60; src/components/asset-card.tsx:762; +1 further reference in the verification record. Surfaces: Meetings (/transcripts), client dashboard AI insights, Assets / Workspace archive, X drafts reader. (No screenshot — absence finding, evidence is the code references.)
**What you see:** Meetings page: "Fireflies transcripts, auto-summarised and routed to clients", "Karos will summarise it", a button reading "Ingest & analyse" / "Analysing...". Client dashboard: "Analyzing...". Client analytics: "will be summarized here". Archive: "organized per agent". Draft cards: "Expand to read and pick favourites" and "Pick your favourites".
**Why wrong:** The same verbs are spelled two ways inside one product, and "favourites" appears in client-facing draft copy while "organized" and "summarized" appear two screens away. It reads as unproofed. The two spellings of analyse/analyze are two clicks apart for a staff user who also views as a client.
**Fix per doc:** Standardize on US spelling (the majority). src/components/transcript-tools.tsx:179 — `{loading ? "Analysing..." : "Ingest & analyse"}` becomes "Analyzing..." / "Ingest & analyze". transcript-tools.tsx:108 "Karos will summarise it" → "summarize it". src/app/(app)/transcripts/page.tsx:30 "auto-summarised" → "auto-summarized". Same file line 59 "are summarised and matched" → "are summarized and matched". src/components/asset-card.tsx:762 "pick favourites" → "pick favorites". src/components/x-drafts-review.tsx:325 "Pick your favourites" → "Pick your favorites". Prioritize the last two: they are the only client-facing hits.
**Systems touched:** copy/i18n, meetings/transcripts, client dashboard, assets/archive, X drafts

---
# Section: Settings, credits & billing
"Prices exist in code for every action and are shown to the client nowhere except the ledger, after the charge." 6 findings: #101, 102, 103, 104, 105, 141.

## F101 · HIGH · Track A
**Title:** One misclick on a trash icon destroys a LinkedIn seat — no confirm, no undo, no feedback if it fails
**Where:** src/components/linkedin-seats-workspace.tsx:211 — onClick={() => remove(seat)} on the trash button (216), adjacent to the pause button (202); src/components/linkedin-seats-workspace.tsx:89 — `await removeEmployeeSeatAction(...)` with no result check and no try/catch, then router.refresh(); src/lib/actions/seat-actions.ts:124 — "Remove a seat (no charge / no refund)."; src/lib/actions/seat-actions.ts:129 — requireSeatAccess throws (seat-actions.ts:30-34) rather than returning an error result; src/lib/credits.ts:62 — employeeSeat = 100 credits; src/lib/credits.ts:107 — withinPlan = currentSeatCount < seatLimit, so re-adding after a removal charges again. Surface: Client settings /clients/[id]/settings — LinkedIn Employee Advocacy roster. (No screenshot — absence finding.)
**What you see:** Each employee seat row ends in a small trash icon sitting right next to the pause icon. Clicking it deletes the seat immediately — no confirmation, no undo, no toast. The seat's LinkedIn sign-in goes with it. If the deletion fails on the server the row simply reappears after the refresh with no message at all.
**Why wrong:** Seats beyond the plan limit cost 100 credits each and removal is explicitly documented as no charge and no refund. Because the paid-seat test is "current count is at or above the limit", removing any seat from an over-limit roster means re-adding a person costs another 100 credits. So a stray click on a 16-pixel icon, two pixels from the pause control, can burn 100 credits with no prompt. The handler also awaits the action without inspecting the result or catching a throw, so an authorization or write failure produces no visible feedback whatsoever.
**Fix per doc:** In src/components/linkedin-seats-workspace.tsx: gate remove() behind a two-step inline confirm (mirror the existing `gate` warning strip at lines 142-151 — set a `confirmingId` state, show "Remove Jane Doe? Seats beyond your plan cost 100 credits and re-adding will charge again." with Cancel / Remove) rather than deleting on first click. Then handle failure: wrap the call at line 89 in try/catch and also check `res.ok`, writing the message into the existing `error` paragraph at line 137 — note the action currently only ever returns ok:true or throws, so the catch is the part that matters. Separately, in src/lib/actions/seat-actions.ts:125-133 consider recording the charged amount on the seat at add time (line 98) so the confirm text can be accurate per seat, and/or refunding a charged seat on removal.
**Systems touched:** LinkedIn seats, credits, seat-actions, client settings

## F102 · HIGH · Track B
**Screenshot:** screenshots/F102.png
**Title:** The client's headline credit number is labelled "credits available" but is not what they can spend — three surfaces disagree
**Where:** src/components/credits-panel.tsx:119 — renders {credits.balance}; src/components/credits-panel.tsx:120 — labelled "credits available" directly beneath it; src/app/(app)/layout.tsx:96 — creditBalance={credits.balance} passed to ClientRail; src/components/client-rail.tsx:171 — desktop pill renders that raw balance; src/components/client-rail.tsx:207 — mobile pill, with aria-label "... credits remaining" (client-rail.tsx:203); src/lib/credits.ts:324 — availableCredits() clips balance by weekLeft/monthLeft; src/app/(app)/clients/[id]/agents/page.tsx:207 — the Agents page uses the clipped figure; src/lib/credits.ts:257 — assessCharge enforces the same caps at charge time. Surface: Client settings /clients/[id]/settings (Credits & usage card) and the client rail credits pill on every page. Screenshot: live portal 27 Jul 2026, red box 102.
**What you see:** The left rail pill reads "Credits 180". The settings card shows "180" in large type directly above the words "credits available". Then on the AI Agents page a badge says "0 credits available" and every Run button is dead. Same workspace, same second, two different "available" numbers.
**Why wrong:** The rail and the settings card render the raw stored balance. The number that actually gates spending is the balance clipped by whatever remains under the weekly and monthly caps — that is what the Agents page shows and what the charge transaction enforces. So the two most prominent credit numbers in the product are labelled with a promise the system will refuse to honour whenever a cap is binding.
**Fix per doc:** Compute the spendable figure once server-side and use it on all three surfaces. In src/app/(app)/layout.tsx:96 pass `availableCredits(credits)` (import from src/lib/credits.ts) instead of `credits.balance`. In src/components/credits-panel.tsx:119-120 make the spendable figure the headline under "credits available" and demote the raw balance to a secondary line ("180 on balance · 0 spendable until Monday") — the panel already receives the full ClientCredits doc, so it can call availableCredits itself. Leave the Agents-page badge (custom-agents.tsx:466) as-is; it is already correct.
**Systems touched:** credits, client rail, settings credits panel, agents page

## F103 · MEDIUM · Track A
**Screenshot:** screenshots/F103.png
**Title:** Client Settings is a nine-section single-column stack ending in a panel whose only content is a sign-out button
**Where:** src/app/(app)/clients/[id]/settings/page.tsx:98-220; src/app/(app)/clients/[id]/settings/page.tsx:77-79; src/components/integrations-tab.tsx:858-886; src/components/integrations-tab.tsx:744-756; src/components/account-menu.tsx:114; src/app/(app)/clients/[id]/settings/page.tsx:212-220; src/components/settings-form.tsx:114 — "Profile Picture"; src/components/settings-form.tsx:127 — "Personal Information"; +7 further references in the verification record. Surface: Client Settings (/clients/[id]/settings). Screenshot: live portal 27 Jul 2026, red box 103.
**What you see:** An admin scrolls past a brand-profile form, a credits panel with a ledger, an agent-access checklist, a scheduled-runs table, then two sections of channel cards — nine of them, most reading "Not connected" — then an auto-schedule toggle, then a meetings list, then an invite key, then a panel containing nothing but a Sign out button that also lives in the rail. No tabs, no anchors, no index. A client sees six of those nine sections.
**Why wrong:** Nothing is grouped by task and nothing is collapsed, so "where do I top up credits" or "where do I disconnect LinkedIn" is a scroll-and-scan exercise. The channels grid is the worst of it: connected and disconnected platforms render as identical full-height cards, so the one or two channels that matter are lost among the ones that are off. The page heading also mis-describes the page — it mentions channels, meetings and teammates, and says nothing about credits, agent access or scheduled runs.
**Fix per doc:** Split src/app/(app)/clients/[id]/settings/page.tsx into tabs (Profile / Channels / Credits / Automation / Team) using the segmented-control markup from src/components/progress-view.tsx:42-67 with the active tab in a `?tab=` param so it is linkable, and rewrite the PageHeader description at line 79 to name what is actually on the page. Inside src/components/integrations-tab.tsx partition each section's platforms on `integrationIsUsable` and render connected ones first as full cards, disconnected ones as a compact add-a-channel row that expands into the full card on click. Delete the Account card at settings/page.tsx:212-220 — LogoutButton already renders in the rail's AccountMenu (src/components/account-menu.tsx:114).
**Also reported as:** Client-facing account settings are written in Title Case throughout, against the house copy rule.
**Systems touched:** client settings IA, integrations tab, account menu, copy

## F104 · MEDIUM · Track A
**Screenshot:** screenshots/F104.png
**Title:** Hitting a spend cap never explains itself and offers no way forward
**Where:** src/components/credits-panel.tsx:12 — UsageMeter renders label, spent/limit and a bar; no reset or explanatory text; src/components/credits-panel.tsx:115 — subtitle names the spending actions but no amounts; src/components/credits-panel.tsx:125 — the two meters, with no warning state at 100% beyond the bar turning red (line 28); src/lib/credits.ts:263 — the Monday reset sentence, reachable only via a denial; src/lib/credits.ts:272 — the 1st-of-month reset sentence, same; src/lib/credits.ts:36 — CREDIT_COSTS, the full price list, never rendered as a list anywhere; src/app/(app)/clients/[id]/settings/page.tsx:109 — the card is shown to client users too, so this is a client-facing surface. Surface: Client settings /clients/[id]/settings — "Credits & usage" card. Screenshot: live portal 27 Jul 2026, red box 104.
**What you see:** Two thin meters: "Used this week 150 / 150" with a red bar, and "Used this month 150 / 400". No sentence anywhere. No reset date. No button to ask for more. The card's subtitle says AI actions spend credits without ever saying how many. The client is capped and the page acts as if everything is normal.
**Why wrong:** The wording the client needs already exists in code and is only ever shown after an action has already failed: the reset guidance ("It resets on Monday", "It resets on the 1st") lives inside the denial messages. The card itself never states a reset date, never says what is now blocked, and offers no route to request more — even though a support flow exists elsewhere in the shell. So a client cannot plan around a cap or understand why they hit one.
**Fix per doc:** In src/components/credits-panel.tsx: give UsageMeter (line 12) an optional reset line and render it under each bar — compute the next Monday / the 1st with the existing creditWeekKey / creditMonthKey helpers in src/lib/credits.ts. At pct >= 100, render a warning strip (same shape as the seat gate strip in src/components/linkedin-seats-workspace.tsx:142-151) naming what is blocked, with the existing ContactUsButton (variant="row") as "Request more credits" — it needs userName/userEmail, so thread them from src/app/(app)/clients/[id]/settings/page.tsx:109. Add a collapsible "What actions cost" table rendered from CREDIT_COSTS and TASK_EXECUTION_COSTS so pricing is visible before the wall.
**Systems touched:** credits panel, credits lib, client settings, support/contact flow

## F105 · MEDIUM · Track B
**Title:** The auto-publish switch snaps back with no explanation, and Disconnect silently does nothing when it fails
**Where:** src/components/integrations-tab.tsx:212-223 — handleAutoPublishToggle: `catch { setAutoPublish(!next); }`; src/components/integrations-tab.tsx:225-236 — handleDisconnect: `catch { // revalidation corrects state }`; src/components/integrations-tab.tsx:543-553 — handleDisconnectSub: same empty catch; src/components/integrations-tab.tsx:163 — the existing `formError` state; src/components/integrations-tab.tsx:447-450 — where formError is already rendered for the manual form; src/lib/actions/integration-actions.ts:89-96 — deleteIntegrationAction starts with `await requireStaff()`, which throws "Forbidden" for a client; src/components/integrations-tab.tsx:362-371 — the Disconnect button is not gated on isAdmin, so clients see and can click it; src/app/(app)/clients/[id]/settings/page.tsx:33-37 — a client user reaches their own settings page; +1 further reference in the verification record. Surface: Client Settings → Integrations → connected platform card. (No screenshot — absence finding.)
**What you see:** You toggle "Auto-publish scheduled content". It flips, then flips back on its own with no message, so you cannot tell a refusal from a network blip. Separately, you click Disconnect: the spinner runs, stops, and the card still says Connected — no error, no reason. For a client user this is guaranteed every time, because disconnecting is a staff-only operation on the server and the refusal is thrown away.
**Why wrong:** All three handlers swallow their errors. The auto-publish toggle catches everything and just reverts the optimistic state, so the user sees the switch move twice and is told nothing. Disconnect and the Google sub-service disconnect catch and discard with a comment claiming revalidation corrects the state, but nothing revalidates on the failure path — and since a client is never permitted to disconnect, they get a spinner and silence forever. The card already has an error slot that the manual-credentials form writes into; these three paths just don't use it.
**Fix per doc:** src/components/integrations-tab.tsx: set `formError` from the caught error in handleAutoPublishToggle (line 218), handleDisconnect (line 231) and handleDisconnectSub (line 548), and render it in each card's action area the way the manual form already does at lines 447-450 (the Google card needs its own error slot — it has none). Keep the optimistic revert, just say why. Separately, either hide the Disconnect button for non-staff (add the isAdmin/isStaff condition already available in that component at line 735) or let clients disconnect their own integrations by relaxing deleteIntegrationAction in src/lib/actions/integration-actions.ts:93 to the same self-scoped check setIntegrationAutoPublishAction uses at lines 80-83.
**Systems touched:** integrations tab, integration-actions, roles/permissions, error handling

## F141 · LOW · Track A
**Screenshot:** screenshots/F141.png
**Title:** The credit ledger is the only place that tells a client what anything costs
**Where:** src/components/credits-panel.tsx:115 — subtitle names the actions but no amounts; src/lib/credits.ts — CREDIT_COSTS holds every price (chatMessage: 1, and the rest). Surface: Client settings (/clients/[id]/settings) → Credits & usage → Recent activity. Screenshot: live portal 27 Jul 2026, red box 141.
**What you see:** Ledger rows: "Copilot chat message -1", "LinkedIn employee seat: Test Test -100". The card subtitle above says only "AI actions (agent runs, copilot messages, task executions, doc corrections) spend credits", with no amounts.
**Why wrong:** A client can only learn the price of an action by performing it and then reading the ledger. Every price exists in code already — it is simply never displayed before the charge.
**Fix per doc:** Render CREDIT_COSTS as a short price list in the Credits card ("Copilot message 1 · doc correction 2 · agent run 25") and put the cost on each action's own button, so the price is visible before the click, not after.
**Systems touched:** credits panel, credits pricing, client settings

---
# Section: Calendar
"Read-only for clients, with a seven-column grid at every width that truncates every chip to three words." 8 findings: #107, 108, 151, 109, 110, 111, 112, 142.

## F107 · HIGH · Track B
**Title:** Choosing "Manual push" when approving tells you to publish from the calendar. There is no Publish Now button on the calendar — or anywhere else
**Where:** src/components/asset-card.tsx:146 — approve-panel hint: "On the calendar, you push it live with Publish Now"; src/components/asset-card.tsx:500 — `canPublishNow = canApprove && compatibleConnected.length > 0 && ...`; src/components/asset-card.tsx:498 — compatibleConnected filters against the `connectedPlatforms` prop; src/components/assets-view.tsx:113 — `<AssetCard asset={asset} canApprove={canApprove} />` — connectedPlatforms is never passed; src/app/(app)/jobs/[id]/page.tsx:61 — `<AssetCard key={a.id} asset={a} canApprove />` — also no connectedPlatforms; these are the only two AssetCard call sites in the repo; src/components/asset-card.tsx:1017 — the Publish Now button, gated on canPublishNow; src/components/run-calendar.tsx:578 — the calendar opens AssetDetailModal; src/components/asset-detail-modal.tsx:291 — MarkPostedRow is the only action in that modal; +4 further references in the verification record. Surface: Approve panel on a draft (/assets, staff) → then Calendar (/calendar) → day → post detail. (No screenshot — absence finding.)
**What you see:** When approving a deliverable you pick a publishing tier. Choosing "Manual push" shows the hint "On the calendar, you push it live with Publish Now." You go to the Calendar, click the scheduled post, and the detail panel offers only "Mark as posted". There is no Publish Now on the calendar. In fact the Publish Now button never appears anywhere in the app, because the list that renders it is never given the client's connected channels — the condition that reveals it can never be true. The integrations tooltip ("Publish through the Publish Now button only") and the task ticket ("lands on the calendar for a manual Publish Now") point at the same button.
**Why wrong:** Three separate pieces of user-facing copy name a control that has no reachable location. The calendar opens the read-only asset detail panel, which has no publish path at all. And the one place the button is coded — the staff assets list — never satisfies its own visibility condition, so the whole "manual" tier is instruction-only: the app tells you to push it live and gives you nothing to push.
**Fix per doc:** Two parts. (1) Make the button reachable for staff: pass the client's connected platforms into AssetCard from src/components/assets-view.tsx:113 and src/app/(app)/jobs/[id]/page.tsx:61 (listClientIntegrations + integrationIsUsable, as src/lib/actions/asset-actions.ts:284-285 does), otherwise canPublishNow at asset-card.tsx:500 can never be true. (2) Fix the three copy strings so they name a real location: asset-card.tsx:146, integrations-tab.tsx:323, task-ticket-modal.tsx:235 should say the post is pushed live from the Assets list (or, for clients, that they post it by hand and use "Mark as posted"). Do NOT add Publish Now to src/components/asset-detail-modal.tsx for clients — publishAssetNowAction calls requireStaff() (asset-actions.ts:279), so a client-facing button there would always error; if it is added to that modal, gate it on a staff viewer prop and pass connectedPlatforms in from src/components/run-calendar.tsx.
**Systems touched:** assets/publish flow, calendar, asset detail modal, integrations copy, task tickets

## F108 · HIGH · Track B
**Title:** Scheduled runs are previewed in your timezone but stored and printed in the server's
**Where:** src/lib/scheduled-runs.ts:5; src/lib/scheduled-runs.ts:36; src/components/schedule-run-modal.tsx:53; src/lib/actions/planned-run-actions.ts:99; src/lib/actions/planned-run-actions.ts:91; src/components/run-calendar.tsx:90; src/components/run-calendar.tsx:188; src/components/run-calendar.tsx:85; +1 further reference in the verification record. Surface: Calendar → "Schedule a run" modal, then the day-detail card for that run. (No screenshot — absence finding.)
**What you see:** In the schedule modal you pick Weekly / Monday / 09:00 and the green preview says it fires Monday at 9:00 AM. After saving, the card for that run on the calendar says "Weekly · Mon 09:00" and, in the same sentence, a different clock time — and the chip can land in a day cell either side of the one you expected.
**Why wrong:** The next fire time is worked out twice in two different timezones: the browser computes the preview, then the server throws that away and recomputes with the same helper in the server's own timezone (the helper file states this outright). The card then prints the stored hour and minute raw next to a time converted into whoever is looking. Nothing in the deployment pins a timezone, so a hosted container runs on UTC while the team is not — every recurring schedule fires hours away from the time the person picked, and the day bucketing is computed from the runtime's timezone on both the server render and the browser, so chips and the "today" highlight can move on hydration. A one-off run is worse: the instant is stored correctly from the browser but the printed hour is re-derived in the server's timezone.
**Fix per doc:** Persist the intent, not a server-local instant. Add an IANA timezone to PlannedScheduledRun (src/lib/types.ts:489 — the sibling ScheduledRun type already carries RunCadence.timezone at src/lib/types.ts:264) and compute nextRunAt with the timezone-aware helper that already exists for the other scheduler, computeNextRunAt in src/lib/run-cadence.ts:91, instead of computeNextRun from src/lib/scheduled-runs.ts. Have ScheduleRunModal send the browser zone (Intl.DateTimeFormat().resolvedOptions().timeZone) so createPlannedRunAction (src/lib/actions/planned-run-actions.ts:62) stores the same zone the preview used, and pass that zone as timeZone into the toLocaleTimeString call in timeStr (src/components/run-calendar.tsx:90) and into dayKey (line 85) so server render and browser agree. Minimum shippable version: print the zone next to the time in both the modal preview and ScheduledRunCard so the two strings cannot silently contradict each other.
**Systems touched:** scheduled runs, timezones, calendar, planned-run-actions, types

## F151 · HIGH · Track A
**Screenshot:** screenshots/F151.png
**Title:** The calendar day detail prints the raw run record — internal status, product code, job hash, markdown and all
**Where:** src/components/run-calendar.tsx:229 area — the day-detail run card renders the run summary text verbatim; the status label map already exists (finding 120's evidence) and is unused here. Surface: Calendar → click a day → Completed runs. Screenshot: live portal 27 Jul 2026, red box 151.
**What you see:** Photographed on 22 Jul's detail: "# Karos X — full draft batch across every avenue (company page + seat) status: pending_review · product e13 · 2026-07-22 · job e52ffe1e · draft-only, nothing posted This batch drafts **one post in every avenue** for both managed accounts:". Literal #, literal **, the internal status word, the lab product code and a job hash — in the panel a client opens to see what ran. This is the "too much text / messy" calendar the call complained about, in one block.
**Why wrong:** It is the run record's internal summary dumped verbatim into a client surface — four leak classes at once (markdown syntax, raw enum, internal product id, database hash). The portal already has a status label map that would render "In review".
**Fix per doc:** Compose the line instead of echoing the record: agent name + what it produced + status through the existing label map ("X Agent · drafted 8 posts · In review"). Strip markdown from any model-written summary at render, and keep product/job ids to a staff-only tooltip. Cross-ref findings 57/89 (same leak class elsewhere).
**Systems touched:** calendar day detail, run records, status labels, client-facing copy

## F109 · MEDIUM · Track A
**Screenshot:** screenshots/F109.png
**Title:** A calendar run badged "Ready to review" has nothing to click
**Where:** src/components/run-calendar.tsx:229; src/components/run-calendar.tsx:208; src/app/(app)/calendar/calendar-body.tsx:159; src/components/notification-bell.tsx:197; src/app/(app)/jobs/[id]/page.tsx:20; src/components/run-calendar.tsx:287. Surface: Calendar → click a day → "Completed runs". Screenshot: live portal 27 Jul 2026, red box 109.
**What you see:** A completed run shows an orange "Ready to review" badge, the agent name, the time and thumbnails. There is no link to the run, no Review button, nothing that gets you to where you would approve it. You have to leave the calendar and hunt for it on the Jobs or Assets page. The post cards immediately below it do open a detail view, which makes the run cards look broken by comparison.
**Why wrong:** The card already holds the identifier of the run it is describing, and the app has a working run detail page that the notification bell links to. Telling someone something is ready to review and then giving them no way to review it is the same hand-off gap that was called out on the SEO/GEO cards.
**Fix per doc:** In PastRunCard (src/components/run-calendar.tsx:208) make the header actionable. For staff, wrap it in a Link to `/jobs/${run.id}` (calendar-body.tsx:158-159 sets CalendarRun.id to the job id for past runs) and add an explicit "Review deliverable →" button when run.jobStatus === "review". Do not point client viewers there — src/app/(app)/jobs/[id]/page.tsx:20 is guarded to staff and silently redirects a client to /dashboard; for clients call the setOpenAssetId handler already wired in this component (line 352, used by PostCard at line 287) with run.assets[0].id. Pass a canOpenJob-style flag down from RunCalendar's existing canSchedule prop rather than inferring role in the card.
**Systems touched:** calendar, jobs page, review flow, roles

## F110 · MEDIUM · Track B
**Title:** Pause and Cancel on a scheduled run fail silently, and Cancel deletes it forever with no confirmation
**Where:** src/components/run-calendar.tsx:163; src/components/run-calendar.tsx:169; src/components/run-calendar.tsx:196; src/lib/actions/planned-run-actions.ts:253; src/lib/actions/planned-run-actions.ts:299; src/components/custom-agents.tsx:717. Surface: Calendar → click a day → "Upcoming runs" card → Pause / Cancel. (No screenshot — absence finding.)
**What you see:** "Cancel" sits next to "Pause" and permanently deletes the schedule on one click — no confirm step, no undo. If the call is refused, for example because the schedule was already removed in another tab, the button spins forever, no message appears, and the card stays on screen as if nothing happened.
**Why wrong:** Both handlers ignore the error the server hands back and never clear the busy flag, so a refused delete looks like a hang and a refused pause looks like a no-op — the person walks away believing the run is stopped when it will still fire. And labelling an irreversible delete "Cancel", right next to a card you can dismiss, invites the one mis-click that cannot be taken back. The same file already proves the right pattern is available elsewhere in the app: the agent schedule dialog surfaces the returned error inline.
**Fix per doc:** In ScheduledRunCard (src/components/run-calendar.tsx:153) capture the result of both setPlannedRunStatusAction and deletePlannedRunAction, render res.error inline, and clear busy in a finally so the button always recovers — mirror the handler in src/components/custom-agents.tsx:717 (togglePause), which already does exactly this. Rename the danger button to "Delete schedule" and gate it behind a confirm step. Optionally pass canManage for client viewers too (it is currently hardcoded to canSchedule at run-calendar.tsx:543, and calendar-body.tsx:71 only sets that for staff) so clients can pause from the calendar as well as from the AI Agents page.
**Systems touched:** calendar, planned-run-actions, error handling, destructive actions

## F111 · MEDIUM · Track A
**Screenshot:** screenshots/F111.png
**Title:** The month calendar is a fixed seven-column grid at every screen size, with chips well under the minimum touch target
**Where:** src/components/run-calendar.tsx:448-451; src/components/run-calendar.tsx:455; src/components/run-calendar.tsx:484; src/components/run-calendar.tsx:139-147; src/components/run-calendar.tsx:508. Surface: Calendar (/calendar and /clients/[id]/calendar). Screenshot: live portal 27 Jul 2026, red box 111.
**What you see:** On a phone the month grid still renders seven columns across roughly 340 pixels — about 48 pixels per day, each holding chips whose text truncates to nothing, so the month reads as rows of coloured slivers with no labels. On a client's laptop with the copilot dock open the cells are about 78 pixels. The chips themselves are around seventeen pixels tall, and tapping one is how you open a post.
**Why wrong:** The calendar has no responsive alternative at all — not a single width-dependent class anywhere in the component — so the same seven-column layout and the same ten-pixel chips render on a phone as on a desktop. Seventeen pixels is well under the twenty-four pixel minimum for a control, and this control is the way into a post's detail.
**Fix per doc:** In src/components/run-calendar.tsx render the seven-column grid and its day-label row (448-455) only from `sm:` up (`hidden sm:grid`) and add a mobile sibling listing only days that have content as an agenda — a `<ul>` of date headers with full-width rows reusing RunChip and PostChip at `text-xs` and `py-2`. Bump the desktop PostChip at 139-142 to `py-1 text-[11px] min-h-[24px]` and give RunChip the same, and raise the "+N more" line at 508 from `text-[9px]` to `text-[11px]`. Cell `min-h-[84px]` at 484 can stay.
**Systems touched:** calendar, responsive layout, accessibility/touch targets

## F112 · LOW · Track A
**Screenshot:** screenshots/F112.png
**Title:** A client's Calendar is read-only and its empty state offers no next step
**Where:** src/app/(app)/calendar/calendar-body.tsx:41; src/app/(app)/calendar/calendar-body.tsx:71; src/app/(app)/calendar/calendar-body.tsx:46; src/components/run-calendar.tsx:429; src/components/run-calendar.tsx:470; src/components/run-calendar.tsx:417; src/lib/actions/planned-run-actions.ts:141. Surface: Client portal → Calendar. Screenshot: live portal 27 Jul 2026, red box 112.
**What you see:** A brand-new client opens Calendar and gets a full month of empty squares, a legend, and the words "No runs yet". There is no "Schedule a run" button, clicking a day does nothing unless it already has something on it, and nothing on the page says where schedules come from.
**Why wrong:** The page header promises what the agents will run and everything they have produced, then offers no way to make either happen. Clients can set up a weekly agent schedule — just on the AI Agents page, one item up the same rail — so this is a missing pointer rather than a missing capability, and the calendar dead-ends instead of handing off.
**Fix per doc:** In src/app/(app)/calendar/calendar-body.tsx, when runs.length + posts.length === 0, render the EmptyState primitive above the grid using its action slot (src/components/ui.tsx:188-198, already imported at line 5) with a button to `/clients/${clientId}/agents` — "Set up an agent schedule" — since configureClientAgentScheduleAction (src/lib/actions/planned-run-actions.ts:141) lets clients do exactly that. Separately, for staff, make empty day cells clickable in src/components/run-calendar.tsx:470 and pass the clicked date into ScheduleRunModal as a prefill (the modal currently exposes only a datetime-local field for one-offs, line 153, and ignores the day you clicked).
**Systems touched:** calendar, empty states, agent schedules

## F142 · LOW · Track A
**Screenshot:** screenshots/F142.png
**Title:** The calendar's primary button wraps to two lines
**Where:** src/components/run-calendar.tsx — calendar header row. Surface: Calendar (/clients/[id]/calendar). Screenshot: live portal 27 Jul 2026, red box 142.
**What you see:** The orange primary button reads "Schedule a / run" across two lines, next to a two-line "Agent / Calendar" heading.
**Why wrong:** The header row has no minimum widths, so the primary action breaks first. It looks broken at exactly the width a laptop uses.
**Fix per doc:** Add whitespace-nowrap to the button and let the heading shrink, or drop the header to a stacked layout under the tablet breakpoint.
**Systems touched:** calendar, responsive layout

---
# Section: Shell: navigation, notifications, roles
"The frame around everything — the sidebar, the bell, role handling, and one icon fallback that silently replaces eight common glyphs with a sparkle." 12 findings: #113, 115, 116, 117, 118, 119, 120, 143, 146, 121, 122, 123.

## F113 · HIGH · Track A
**Screenshot:** screenshots/F113.png
**Title:** Opening any client page swallows the whole staff sidebar — and employees have no way to get it back
**Where:** src/app/(app)/clients/[id]/layout.tsx:24 — isStaff includes KAROS_EMPLOYEE; src/app/(app)/clients/[id]/layout.tsx:36 — ClientContextSync mounted unconditionally for staff; src/components/sidebar.tsx:314 — items = (isStaff && activeClient) ? clientViewNav(...) : adminItems; src/components/sidebar.tsx:435 — ClientContextPicker rendered only when user.role === "KAROS_ADMIN"; src/components/sidebar.tsx:81 — setActiveClient(null) exists only inside that admin-only picker; src/lib/active-client-context.tsx:39 — "Context intentionally persists when navigating elsewhere; only cleared by the picker X"; src/app/(app)/layout.tsx:162 — StaffCopilotDock also appears for any staff with an active client (src/components/staff-chatbot-widget.tsx:15). Surface: Staff sidebar, everywhere after visiting /clients/[id] (any sub-route). Screenshot: live portal 27 Jul 2026, red box 113.
**What you see:** An employee clicks a client from the Clients grid. The sidebar instantly stops showing Clients, Agents, Jobs, Meetings, Assets, Calendar, Tasks, Connect and Analytics, and shows only four client tabs (Dashboard / AI Agents / Calendar / Workspace). A client copilot panel also appears on the right, uninvited. There is no close control and no "back to workspace" link. The nav stays that way as they keep clicking around. The only escapes are the logo — which lands on the staff dashboard with a sidebar still showing the four client tabs and nothing highlighted — or a full browser reload of a non-client page.
**Why wrong:** Nobody asked to enter client view — merely opening a client page seeds it, because the nested client layout mounts the context sync for all staff, employees included. The nav swap is keyed on staff-or-not, but the only control that clears the context lives inside the admin-only client picker at the bottom of the rail. So for every employee this is a one-way door, escapable only by a full page reload. The context is deliberately built to persist across navigation, which makes the trap sticky rather than transient.
**Fix per doc:** In src/components/sidebar.tsx: (a) move ClientContextPicker out of the `user.role === "KAROS_ADMIN"` guard at line 435 so employees get the same labelled exit, or add an always-visible "← Back to workspace" row above the client tabs inside `clientSections` (line 356) that calls setActiveClient(null) and routes to /clients — the same body as clearClient at lines 79-85; (b) better, stop swapping the nav wholesale at line 314 — keep adminItems and render clientViewNav(activeClient.client.id) as a labelled sub-group under the client header in clientSections, so no staff member can lose the workspace nav at all.
**Systems touched:** sidebar/navigation, active-client context, roles, staff copilot dock

## F115 · MEDIUM · Track B
**Title:** An approved user is never told they were approved; the pending screen is a static dead end
**Where:** src/app/pending/page.tsx:9 — already redirects to /dashboard the moment the account is enabled, so a refresh is all that is missing; src/app/pending/page.tsx:31 — LogoutButton is the only control on the page; src/lib/actions/user-actions.ts:13 — the import block pulls in data + auth + adminAuth only; no email helper; src/lib/actions/user-actions.ts:112 — approveRegistrationAction ends at revalidatePath("/team"); no mail, no notification; src/lib/actions/user-actions.ts:118 — rejectRegistrationAction deletes the Firestore doc and the auth account silently; src/components/auto-refresh.tsx:11 — AutoRefresh(intervalMs) already exists; src/lib/email.ts:16 — sendEmail via Resend, soft-failing, ready to use. Surface: /pending (disabled / awaiting-approval landing). (No screenshot — absence finding.)
**What you see:** "Awaiting approval — your account dh@example.com is registered as a client for Acme. An agency admin needs to approve it..." and one Sign out button. That is the entire screen. It never updates. When an admin approves the account minutes later, nothing happens: no email, no notification, no page change — the user has to guess and reload. If the admin rejects them, their account and sign-in record are deleted and they are told nothing; their next sign-in attempt simply fails.
**Why wrong:** The approve and reject actions flip the flags and revalidate the Team page; neither sends mail — the file imports no email helper at all — nor writes any notification, even though Resend is wired up and used elsewhere for support mail and deliveries. The pending page has no polling either, although a small auto-refresh component already exists in the codebase for exactly this pattern, and the page already redirects to the workspace as soon as the account is enabled. There is also no support link on the screen, so a user stuck here has no in-product way to ask what is happening — and this is the very first impression the portal makes on a new client.
**Fix per doc:** Three additions. (1) In src/lib/actions/user-actions.ts, after the upsert at line 110 send a Resend mail with sendEmail from src/lib/email.ts ("You're in — sign in here"), and a neutral decline mail in rejectRegistrationAction before the delete at line 118 (capture the email first); reuse the existing emailShell helper for markup. (2) In src/app/pending/page.tsx mount `<AutoRefresh intervalMs={15000} />` from src/components/auto-refresh.tsx — the existing redirect at line 9 then flips the user into the workspace with no manual reload. (3) Add an expected-timeframe line and a support mailto beside the Sign out button at line 30-32 so the screen has one forward action.
**Systems touched:** onboarding/pending flow, user-actions, email (Resend), auto-refresh

## F116 · MEDIUM · Track A
**Screenshot:** screenshots/F116.png
**Title:** Clients get no notification badge on desktop — the bell is buried in the account menu
**Where:** src/components/client-rail.tsx:174; src/components/client-rail.tsx:210; src/components/account-menu.tsx:95; src/components/notification-bell.tsx:70; src/components/app-header.tsx:19. Surface: Client portal, any page: desktop left rail (bottom) versus mobile top bar. Screenshot: live portal 27 Jul 2026, red box 116.
**What you see:** On a phone there is a bell with a count in the top bar. On a desktop the bell exists only as a "Notifications" row inside the account dropdown at the bottom of the rail, and the count is only visible once that dropdown is open. Nothing on the visible page hints that drafts are waiting for review.
**Why wrong:** The point of a badge is ambient signal. A client with items sitting in "Ready for review" sees nothing until they happen to click their own avatar, so approvals stall and the portal looks idle. Staff get a permanently visible bell in the workspace header, so the two shells behave differently for no reason a client can see.
**Fix per doc:** Render the icon variant of NotificationBell in the client rail's bottom block (src/components/client-rail.tsx:161, next to the Credits pill at line 162) with panelPlacement="up", passing the actionItems / reviewJobs / taskAlerts already handed to the rail, and drop the nested row from src/components/account-menu.tsx:95. If the row must stay, compute the same total and mirror it onto the AccountMenu trigger (account-menu.tsx:64) as a dot so the count is visible without opening the menu.
**Systems touched:** notifications, client rail, account menu

## F117 · MEDIUM · Track B
**Screenshot:** screenshots/F117.png
**Title:** No admin surface shows any client's credit balance, yet every denial tells the client to "ask your Karos team"
**Where:** src/app/(app)/clients/page.tsx:11 — fetches assets and jobs only; getClientCredits is never called; src/app/(app)/clients/page.tsx:25 — ClientsGrid receives clients/assets/jobs, and renders only asset and job counts (src/components/clients-grid.tsx:386-389); src/app/(app)/admin/analytics/page.tsx:51 — the analytics fetch block; `grep -in credit` over that file returns nothing; src/components/credits-panel.tsx:129 — the grant/deduct form is admin-only and lives on one client's settings page; src/lib/credits.ts:254 — "Ask your Karos team for a top-up" in the denial the client sees; src/app/api/clients/[id]/chat/route.ts:165 — the copilot is told to suggest the same under 20 credits. Surface: Admin Clients list /clients and Admin Analytics /admin/analytics. Screenshot: live portal 27 Jul 2026, red box 117.
**What you see:** The Clients grid shows each brand with asset and job counts — no credits. Admin Analytics shows model cost in dollars, tokens, runs, errors, logins and feedback — no credits. To learn that a client is out of credits an admin must open that one client's Settings page and scroll to the Credits card. There is no list, no sort, no low-balance flag, no alert.
**Why wrong:** The client-facing wall says to ask the Karos team for a top-up, and the copilot is instructed to say the same below 20 credits — but the Karos team has no queue, no notification and no dashboard telling them who is asking. The only credit-granting control in the product is inside a single client's settings page, so the remedy for every credit denial is an out-of-band conversation the portal neither triggers nor records. A client can sit blocked indefinitely while the agency's own analytics page reports everything green.
**Fix per doc:** Add a credits column to the Clients grid: in src/app/(app)/clients/page.tsx fetch getClientCredits for the listed clients alongside the existing assets/jobs reads (line 11), pass a clientId → availableCredits(credits) map into ClientsGrid, and render it as a Badge next to the asset/job counts in src/components/clients-grid.tsx:386-389 — warning tone under a threshold, danger tone at 0, linking to /clients/[id]/settings. Then add a "Clients low on credits" block to src/app/(app)/admin/analytics/page.tsx using the same helper, so the agency sees the ask before the client has to send an email.
**Systems touched:** credits, admin clients grid, admin analytics, copilot

## F118 · MEDIUM · Track A
**Title:** The Connect page promises "submit managed jobs" and walks through an Instagram job the MCP server has no tool for
**Where:** src/app/(app)/connect/page.tsx:20 — header: "...browse jobs & assets, submit managed jobs, and upload files"; src/app/(app)/connect/page.tsx:62-64 — the Instagram job example; src/app/(app)/connect/page.tsx:66-68 — "Claude Code will read the client's context, submit the managed job to the agent service"; src/app/(app)/connect/page.tsx:75-86 — the Available tools list: run_agent is "Run a repo agent for a client with a prompt"; no managed-job entry; src/lib/mcp/tools.ts:85,110,139,166,198,229,243,270,297,312,341,387 — all twelve tool names; grep for submit_job / submitManagedJob in src/lib/mcp returns nothing; src/lib/actions/external-job-actions.ts:12 — a stale comment claiming "the MCP `submit_job` tool runs the identical path"; src/lib/jobs/submit-managed.ts — the shared core, already factored for non-web callers. Surface: Connect Claude Code (/connect), staff only. (No screenshot — absence finding.)
**What you see:** The page header says Karos exposes an MCP server your Claude Code can use to "read client data, browse jobs & assets, submit managed jobs, and upload files." Step 3 gives the worked example: "submit a 3-post Instagram job focused on their summer launch and tell me the job id", then states Claude Code will read the client's context and submit the managed job to the agent service. A staff member follows it and Claude Code reports there is no such tool.
**Why wrong:** The server exposes twelve tools and the only one that starts work runs a repo agent with a prompt. There is no submit-managed-job tool. The page's own "Available tools" sidebar confirms it — it lists the run-a-repo-agent tool and says nothing about managed jobs. So the headline promise and the worked example both describe a capability the server does not have, and the two halves of the same page contradict each other.
**Fix per doc:** Pick one. Either add a submit_managed_job tool to src/lib/mcp/tools.ts wrapping submitManagedJob from src/lib/jobs/submit-managed.ts (same pattern as run_agent at tools.ts:312; the core already takes an AppUser, so PAT auth works) and add it to the sidebar array at src/app/(app)/connect/page.tsx:75-86 — or rewrite connect/page.tsx line 20 and lines 62-68 to describe running a repo agent with a prompt, which is what actually happens. Either way fix the stale comment at src/lib/actions/external-job-actions.ts:12, which names a tool that does not exist.
**Systems touched:** MCP server, Connect page, external-job-actions, managed jobs

## F119 · MEDIUM · Track A
**Title:** The smallest type in the portal sits below the accessibility contrast floor in 143 places
**Where:** src/app/globals.css:29; src/app/globals.css:212; src/app/globals.css:21; src/app/(app)/admin/analytics/page.tsx:260; src/components/seo-geo-panel.tsx:90; src/components/run-calendar.tsx:508; src/components/integrations-tab.tsx:282; src/components/integrations-tab.tsx:285. Surface: whole portal (every card caption, meta line, stat sub-label, timestamp). (No screenshot — absence finding.)
**What you see:** The ten and eleven pixel supporting text everywhere — the sub-labels under the analytics headline numbers, the per-million pricing in the model table, the coverage lines under the search score tiles, relative timestamps on run rows, platform descriptions on channel cards, the "plus three more" in calendar cells — is a mid grey that visibly fades into the card behind it, especially on a laptop screen at an angle or in daylight.
**Why wrong:** The dim grey against the card surface computes to roughly 3.5 to 1, under the 4.5 to 1 floor for normal-size text — and these are the smallest sizes in the app, so the large-text exemption does not apply. The token is used 463 times, 143 of them on a line that also sets ten or eleven pixel type. The numbers and units that qualify every metric end up the hardest things on the page to read. Light mode is no better, at roughly 3.6 to 1.
**Fix per doc:** In src/app/globals.css:29 lighten `--muted-2` from #77777e to about #8f8f97 (measures 4.8:1 against --surface #242429 at line 21, still clearly subordinate to --muted) and lift the light-mode value at line 212 from #8a887f to about #767469 (currently 3.6:1 on white). Add a separate `--muted-3` pinned to the old #77777e for genuinely decorative uses — icon strokes, dividers, disabled states, the Meter fill at src/components/seo-geo-panel.tsx:89 — and repoint those so the change does not flatten the hierarchy. Then sweep the ten-pixel cases: anything carrying a number or a unit moves to `text-[11px] text-muted`, leaving text-muted-2 for labels.
**Systems touched:** theme tokens/globals.css, accessibility, whole-portal typography

## F120 · MEDIUM · Track A
**Screenshot:** screenshots/F120.png
**Title:** The three agent pages print the raw database job status "review" to clients, when the portal already has a label map that says "In review"
**Where:** src/components/x-agent-intake.tsx:686; src/components/x-agent-intake.tsx:58; src/components/linkedin-agent-intake.tsx:638; src/components/reddit-agent-intake.tsx:313; src/components/job-status.tsx:4; src/components/job-status.tsx:7. Surface: X agent / LinkedIn agent / Reddit agent → Feedback card, recent-runs list (/clients/[id]/x-agent, /linkedin-agent, /reddit-agent). Screenshot: live portal 27 Jul 2026, red box 120.
**What you see:** A list of recent runs reading "Run 2026-07-24 · review", "Run 2026-07-22 · queued", "Run 2026-07-19 · delivered".
**Why wrong:** The status word is printed exactly as it is stored. "review" is not a sentence and not a state a client can interpret, and the same underlying state is displayed as "In review" everywhere else in the portal (dashboard, jobs list, job detail) by the shared status badge. The value is typed as a plain string on the way to the screen, so nothing catches the mismatch.
**Fix per doc:** src/components/job-status.tsx — export a plain label map alongside the existing MAP (line 4), e.g. `export const JOB_STATUS_LABEL: Record<string, string> = { queued: "Queued", running: "Running", review: "In review", approved: "Approved", delivered: "Delivered", failed: "Failed" }` (or export MAP and read `.label`). Then replace `· {r.status}` with `· {JOB_STATUS_LABEL[r.status] ?? r.status}` at src/components/x-agent-intake.tsx:686, src/components/linkedin-agent-intake.tsx:638 and src/components/reddit-agent-intake.tsx:313. While there, the run date on those same lines is an ISO slice — consider the existing formatDate helper in src/lib/utils.ts.
**Systems touched:** agent intake pages (X/LinkedIn/Reddit), job status labels, copy

## F143 · MEDIUM · Track A
**Screenshot:** screenshots/F143.png
**Title:** Every notification is truncated mid-sentence and none of them are notifications
**Where:** src/components/notification-bell.tsx — row rendering and the footer link; the rows are drawn from task/action-item records rather than events. Surface: Notification bell (any page). Screenshot: live portal 27 Jul 2026, red box 143.
**What you see:** Both rows read as cut-off task descriptions: "Provide feedback on the Instagram agent UI and output quality after the..." and "Work with Yair to have all product agents ready and integrated on the...", each stamped "Portal final sprint · 19d ago". The panel footer says "View all meetings →".
**Why wrong:** These are internal task rows surfaced as notifications, cut mid-sentence, under a footer that links to meetings. Nothing here tells the reader what happened or what to do, and a 19-day-old item is still presented as active.
**Fix per doc:** Write notifications as events with a subject and a verb ("Instagram agent run failed · 9h ago"), let them wrap to two lines instead of clipping, point the footer at whatever the rows actually are, and age out or visually de-emphasise anything older than a week.
**Systems touched:** notifications, notification bell, tasks

## F146 · MEDIUM · Track A
**Screenshot:** screenshots/F146.png
**Title:** The Meetings list is not in date order — synced meetings land wherever the sync ran
**Where:** src/lib/data.ts:769-784 — listTranscripts sorts (b.createdAt ?? 0) - (a.createdAt ?? 0); src/lib/types.ts:1016 — meetingDate?: number exists on Transcript; src/app/(app)/transcripts/page.tsx — no re-sort after fetch. Surface: Meetings (/transcripts). Screenshot: live portal 27 Jul 2026, red box 146.
**What you see:** Live right now, top to bottom: Jun 21 → Jun 23 → 7h ago → 4h ago → 8d ago → 7d ago. On the call: "Why is it not in order?" — noticed the moment three freshly synced meetings appeared above newer ones.
**Why wrong:** The list is sorted by createdAt — the moment the record was synced into Firestore — not by when the meeting happened. Fireflies backfills insert old meetings at the top, and the type already carries the real meetingDate field the sort ignores.
**Fix per doc:** Sort by (meetingDate ?? createdAt) descending in listTranscripts, and show meetingDate on the row when present so the displayed date is the one being sorted. One line plus a display touch.
**Systems touched:** meetings/transcripts, data.ts sorting

## F121 · LOW · Track B
**Screenshot:** screenshots/F121.png
**Title:** Dismissing a "new content ready" notification only hides it until the next page load
**Where:** src/components/notification-bell.tsx:64; src/components/notification-bell.tsx:204; src/components/notification-bell.tsx:239; src/lib/actions/transcript-actions.ts:298; src/lib/data.ts:1641. Surface: Notifications bell → "New content ready: ..." row → X. Screenshot: live portal 27 Jul 2026, red box 121.
**What you see:** You click the X on a content-ready notification, tooltip "Dismiss". It disappears and the count drops. Navigate anywhere, or let the page refresh, and it is back with the count restored.
**Why wrong:** The handler only writes to in-page state — there is no server call and nothing is stored. The identical-looking X on meeting action items right below does reach the server and sticks, so the two behave differently for no visible reason and the badge stops being trustworthy.
**Fix per doc:** Either persist it — add a dismissedBy/seenBy field on the job and filter it in listReviewJobs (src/lib/data.ts:1641), behind a small action alongside dismissAssignedActionItemAction (src/lib/actions/transcript-actions.ts:298) — or delete the X (src/components/notification-bell.tsx:204) and let the row clear when the job leaves review, which is how the task rows already behave (see the comment at line 45). Do not ship a Dismiss button whose only effect is local. While in this row: its click-through goes to `/jobs/${j.jobId}` (line 197), which is staff-only and bounces a client to /dashboard — point client viewers at the asset instead.
**Systems touched:** notifications, review jobs, transcript-actions, data.ts, roles

## F122 · LOW · Track A
**Screenshot:** screenshots/F122.png
**Title:** Platform names are title-cased from their ids: "Linkedin", "Youtube"
**Where:** the connected-channels list on the client dashboard page; provider ids are capitalised for display instead of mapped; src/components/client-analytics.tsx:107 — className="capitalize" on {i.platform} is the exact mechanism. Surface: Client dashboard (/clients/[id]) → Connected channels; competitor chips elsewhere. Screenshot: live portal 27 Jul 2026, red box 122.
**What you see:** The channel rows read "Google", "Linkedin", "Youtube". Competitor chips show "ploy.ai" in the sidebar and "PLOY.AI" in the report.
**Why wrong:** "Linkedin" and "Youtube" are misspellings of the brands. They come from title-casing a provider id rather than looking up a display name, so every surface that does this is wrong the same way.
**Fix per doc:** Add one PROVIDER_LABELS map ("linkedin" → "LinkedIn", "youtube" → "YouTube", "x" → "X", "tiktok" → "TikTok") beside the icon helper and use it everywhere a provider is displayed.
**Systems touched:** client dashboard, client analytics, provider display names, competitor chips

## F123 · LOW · Track A
**Screenshot:** screenshots/F123.png
**Title:** "20 agent runs · last 9h ago."
**Where:** the agent-activity block on the client dashboard page. Surface: Client dashboard (/clients/[id]) → Agent activity. Screenshot: live portal 27 Jul 2026, red box 123.
**What you see:** The Agent activity card's only line reads "20 agent runs · last 9h ago."
**Why wrong:** "last 9h ago" is not a sentence. It is the first card many clients read.
**Fix per doc:** "20 runs · last run 9h ago".
**Systems touched:** client dashboard copy

---
## Non-finding sections

### Killed in verification
(Header text: "Claims that did not survive the second pass. Listed so you can see the list was filtered, not just collected.")

1. **"Show recent drafts" chip returns no draft titles** — NOT confirmed. The claim "Asset titles, bodies, statuses and ids never reach the model" is true only of the asset block; the RECENT JOB HISTORY block at src/lib/copilot-context.ts:140-146 feeds up to ten runs with their titles and statuses, which is what a client asking for recent drafts would get back. So the headline "cannot show a single draft" is contradicted by the code. Downgraded medium → low and rewritten as a polish item. Do not put this in front of the developer as a defect.

2. **Competitor rows show no detail — but the analysis is not thrown away, it feeds the copilot** — The headline claim does not hold. The analysed fields ARE consumed: buildCopilotSystemPrompt injects marketTier, threatLevel, overlap, keyStrengths, keyWeaknesses and positioning into the copilot's system prompt (src/lib/copilot-context.ts:106-118), which the client-facing chat route builds on every message (src/app/api/clients/[id]/chat/route.ts:109) — and "Who are our competitors?" is a suggested prompt (src/components/chatbot-widget.tsx:344). So "never shown to anyone" and "throw the entire result away at render time" are both wrong, and the spend is not wasted. What was confirmed: no component renders those fields (grep across src/components and src/app finds no other reference), only name and url are used by the SEO/GEO panel via computeTrackedCompetitors, and add-competitor-modal.tsx has no importer. Severity dropped to low and re-scoped as an enhancement.

3. **The processing banner names "Regenerate", a button clients never see (its "Refresh Task Map" half is accurate)** — Claim does not hold as stated. "Task Map" is NOT absent from the client's portal: buildProactiveActions defines a chip labelled "Refresh Task Map" (src/components/chatbot-widget.tsx:35), the copilot dock is rendered in the client shell (src/app/(app)/layout.tsx:113-127), and chatbot-widget.tsx:305 disables exactly that chip while isAiProcessing is true, with the tooltip "Karos Agents are already building your workspace strategy". So the banner correctly explains a lock on a control the client can see. The "Regenerate" half is real: client-documents.tsx:737 gates the Regenerate button on `isAdmin && clientId` and layout.tsx:107 passes isAdmin={false} for the client shell. Severity dropped from medium to low — one stray internal word plus punctuation.

4. **Title Case labels scattered through a sentence-case product — but the dashboard and Activity examples in the original finding never reach the screen** — Fails as stated on its two headline surfaces. (1) The client dashboard section labels at src/app/(app)/clients/[id]/page.tsx:87, 93, 105 and 115 all carry the class `uppercase`, so "Search & AI visibility" and "AI Insights" both render as ALL CAPS — the case difference the finding calls "unmissable" cannot be seen. (2) The Activity tab strings ("Website Scraped", "Competitor Added", "Competitor Removed", "AI Analysis", "Docs Updated", "Client Created") are EVENT_CONFIG.label values at activity-timeline.tsx:101-161 that are never rendered: EventRow (line 208) uses only cfg.icon/dotClass/iconClass and prints event.title/event.description, and nothing else reads .label. Also dead: add-competitor-modal.tsx:85 "Add Competitor" — that component has zero importers. The remaining instances the verifier did open and confirm rendered, including client-visible ones (copilot chips at chatbot-widget.tsx:321, onboarding steps at 44, "Correct Info" ungated at client-documents.tsx:326, "Run Agent" at tasks-board.tsx:287). Regraded medium → low.

5. **The client sees their credit balance drop with no top-up affordance on the credits screen or the blocked Run button** — The headline claim is wrong: there IS a way for a client to ask for more. ContactUsButton is mounted in the client rail's own account menu at src/components/client-rail.tsx:301 (and in the mobile Company sheet), labelled Support, opening the working sendSupportEmailAction dialog — so "no way anywhere to top up or ask for more" and "a blocked client is left with nothing to click" are both false. What IS true and worth fixing: the grant form is admin-only (credits-panel.tsx:129), the credits panel offers no action to a client, and the disabled Run-now tooltip at custom-agents.tsx:556 names an action with no affordance beside it. That is a discoverability/copy gap, not a dead end, so re-graded from medium to low and marked unconfirmed as stated.

### Closing colophon
"Swept against karos-cmo-42999994788.europe-west1.run.app, client Karos Labs, 27 Jul 2026. Screenshots are the live portal with annotations drawn in before capture; nothing is a mock-up. Line numbers are from branch `claude/portal-bug-sweep-abcd41`."

"Client-role-only screens could not be photographed from an admin session: the sidebar's 'View as client' only scopes the navigation, it does not switch role (finding 60). Those findings carry code references instead."
