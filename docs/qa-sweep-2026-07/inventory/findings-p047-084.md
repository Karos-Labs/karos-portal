# QA sweep findings — pages 47-84

Source: Karos-portal-QA-sweep-FULL-2026-07-27.pdf, pages 47-84. Extracted 2026-07-28.

Boundary notes:
- Page 47 opens with the tail (WHERE + FIX) of **F127** — the finding itself STARTS before p47.
- Page 84 opens the "Workspace, tasks & deliverables" section (28 findings: #46, 47, 48, 50, 51, 53, 54, 55, 56, 57, 58, 149, 150, 60, 61, 62, 63, 64, 65, 66, 67, 68, 69, 70, 136, 71, 72, 73) and the start of **F46** — its FIX CONTINUES past p84.

---

## F127 · (severity not in range) · Track (not in range) — STARTS before p47
**Screenshot:** screenshots/F127.png
**Title:** (not in range — per context: agent card descriptions render the internal lab-manifest string verbatim, with codes like "(e18)", "sub-skill", engine-architecture notes)
**Where:** src/components/custom-agents.tsx — card body renders agent.description verbatim; src/lib/lab-outputs.ts — the import path that stores the manifest string as description; customAgents documents — description holds the lab manifest, with no client-facing alternative field
**What you see:** (before p47)
**Why wrong:** (before p47)
**Fix per doc:** Add a clientBlurb field (1–2 sentences, sentence case, no codes) to the customAgents document and render that on the card and in the run dialog; keep description as the internal field, shown only to staff. Backfill the seven existing agents by hand. Add a guard in the lab-import path that refuses or flags any blurb matching /\be\d{1,2}\)|sub-skill|tonemap|FORGE|Path [A-Z]\b/i so the next import cannot reintroduce it.
**Systems touched:** custom agents catalog, lab import path, agent cards UI

## F131 · BLOCKER · Track A
**Screenshot:** screenshots/F131.png
**Title:** "Run now" is fully enabled on an agent whose own card says "Setup needed"
**Where:** AI Agents (/clients/[id]/agents) — Reddit agent card. src/components/custom-agents.tsx:555-559 — disabled={short} with an explanatory title for the credits case, but no equivalent for the setup case; src/lib/actions/custom-agent-actions.ts — the server-side gate that will refuse the run
**What you see:** The Reddit card shows an amber "Setup needed" chip and, directly beneath it, an active "Run now" button. Nothing states what setup is missing or where to do it. "Setup needed" is itself a button, so the card offers two competing calls to action.
**Why wrong:** Either the click spends credits on a run that cannot succeed, or it fails with an error the client cannot act on. Both are worse than a disabled button, and this is the most likely thing to break in a live demo. The credit-shortfall case already does this correctly — that button is disabled with a title explaining why — so the pattern exists and simply is not applied to the setup case.
**Fix per doc:** Compute readiness where the "Setup needed" chip is rendered and pass it to the run button: when not ready, disable it and set the title to the unblocking step, using the same shape as the existing credits case at custom-agents.tsx:555. Never render an enabled Run beside a blocked chip.
**Systems touched:** custom agents UI, run gating, custom-agent actions

## F25 · HIGH · Track A
**Screenshot:** screenshots/F025.png
**Title:** A client who hits their spend cap gets a greyed-out Run button, no explanation, and advice that would not help
**Where:** Client AI Agents page, /clients/[id]/agents — agent card "Run now". src/components/custom-agents.tsx:555 — disabled={short} on the Run button; custom-agents.tsx:556 — title="Not enough credits. Ask your Karos team for a top-up."; src/components/ui.tsx:38 — Button sets disabled:pointer-events-none, so a disabled button never receives hover; custom-agents.tsx:475 — `short` derived from availableCredits alone; which of the three limits bound is discarded; custom-agents.tsx:466 — the badge is the only visible signal: "{availableCredits} credits available"; src/lib/credits.ts:263 — "It resets on Monday — or ask your Karos team to raise the limit" (post-failure only); src/lib/credits.ts:272 — "It resets on the 1st …" (post-failure only); src/app/(app)/clients/[id]/agents/page.tsx:207 — the page hands down only the clipped number
**What you see:** "Run now" is grey and unclickable. Hovering it produces nothing at all — not even a tooltip. The only clues on the page are a small badge reading "0 credits available" and the line "25 credits per output". The client cannot tell whether they are out of credits, capped for the week, capped for the month, or when any of it resets.
**Why wrong:** Two compounding defects. First, the reason is delivered only as a hover tooltip on a disabled button, and the shared button primitive disables pointer events when disabled, so the browser can never show it — the explanation is unreachable by design. Second, the one message that exists ("Not enough credits. Ask your Karos team for a top-up.") is wrong for two of the three ways spending is blocked: a weekly or monthly cap is not fixed by a top-up, and the correct guidance — resets Monday, resets on the 1st, or ask to raise the limit — exists only in the denial text that appears after an action has already failed, never before.
**Fix per doc:** In src/app/(app)/clients/[id]/agents/page.tsx:207, alongside `availableCredits(credits)` compute which limit binds (compare credits.balance, weeklyLimit − weekSpent, monthlyLimit − monthSpent — the same three terms as availableCredits in src/lib/credits.ts:324-329) and pass a `blockReason` string down to ClientCustomAgents. In src/components/custom-agents.tsx delete the title attribute at line 556 and render that reason as a visible line under the button row (near the "{cost} credits per output" line at 552), worded per case: "Weekly limit reached — resets Monday" / "Monthly limit reached — resets on the 1st" / "Not enough credits". Add the existing ContactUsButton (variant="row", already used at src/components/client-rail.tsx:301) next to it — it needs userName/userEmail, so thread those from the page. Team decision on the 27 Jul call supersedes the softer fix: remove Perplexity and Copilot from the client report entirely — the capture-strip chips, both UnmeasuredEngineCard tiles, and the "Want Perplexity or Copilot coverage?" banner. Keep the internal 5-engine roster so they can return when connectors exist; just filter status "not-wired" out of every client-facing list.
**Systems touched:** credits, custom agents UI, ui.tsx Button primitive, client-rail contact button, SEO/GEO client report (Perplexity/Copilot removal)

## F27 · HIGH · Track B
**Screenshot:** screenshots/F027.png
**Title:** The Reddit agent's schedule dialog offers up to 35 replies a week and bills for them — the product rule is one reply per run, five a week
**Where:** AI Agents (/clients/<id>/agents) → "Set schedule" on the Reddit agent. src/components/custom-agents.tsx:663; custom-agents.tsx:738; custom-agents.tsx:741-764; custom-agents.tsx:794-797; src/app/api/run-scheduled/route.ts:72-77; src/lib/actions/planned-run-actions.ts:167-168; docs/reddit-agent-portal.md:11-14
**What you see:** The dialog is the same one every agent gets: "Posts per week" one to seven, "Outputs per run" one to five, and a cost line reading seven runs times five outputs times the per-output price. For a Reddit agent that is an offer of thirty-five drafted replies a week, called "posts", from a product that never posts.
**Why wrong:** The Reddit contract is explicit and load-bearing: one listing pull, one thread, one drafted reply per run, five a week maximum, and a run that burns a big budget is a build failure. When outputs per run is above one the scheduler prepends an instruction demanding exactly that many distinct outputs and charges the client that multiple on every fire — so the portal sells and bills a shape the agent is instructed never to produce. The word "posts" is also wrong for a draft-only product where a person posts every reply by hand.
**Fix per doc:** Make AgentScheduleModal (src/components/custom-agents.tsx:663) cadence-aware: when isRedditAgentIdentity(agent.key) (already imported from src/lib/custom-agent-launch.ts) pin outputsPerRun to 1 and hide the selector at 754-765, cap the postsPerWeek options at 5 (741-753), and relabel "Posts per week" → "Replies drafted per week" plus the modal description at 738 so it says a person posts each reply. Enforce the same caps server-side in configureClientAgentScheduleAction where the clamps live (src/lib/actions/planned-run-actions.ts:167-168, currently 1-7 and 1-10) so a stale page or a direct call cannot exceed them.
**Systems touched:** schedule dialog, scheduled runs, planned-run actions, credits billing, Reddit agent contract

## F28 · HIGH · Track B
**Title:** The agent intake pages tell clients to pick, edit and skip drafts "in your Workspace archive" — that screen has none of those controls
**Where:** X / LinkedIn / Reddit agent data → Feedback card; then Workspace → Archive. src/components/x-agent-intake.tsx:671-673 — "Picking, editing, or skipping individual drafts happens on the drafts themselves, in your Workspace archive."; src/components/linkedin-agent-intake.tsx:623-625 — identical sentence; src/components/reddit-agent-intake.tsx:298-300 — "Saying whether you posted an individual reply happens on the reply itself, in your Workspace archive, and that is the signal that sharpens the voice fastest."; src/components/asset-card.tsx:715,734,752 — LiDraftsBatch / RedditDraftsBatch / XDraftsBatch mounted only here; src/components/assets-view.tsx:113 and src/app/(app)/jobs/[id]/page.tsx:61 — the only two AssetCard call sites, both staff-only; src/app/(app)/assets/page.tsx:24 — clients are redirected off /assets; src/components/archive-view.tsx:30 — "Read-only — review/approval stays on the staff Library"; src/components/asset-detail-modal.tsx:211,291 — the client's actual draft view: raw content plus "Mark as posted"
**What you see:** The Feedback card tells the client: "Picking, editing, or skipping individual drafts happens on the drafts themselves, in your Workspace archive." The Reddit version says saying whether you posted an individual reply happens on the reply itself, in the Workspace archive, "and that is the signal that sharpens the voice fastest". In the Workspace archive the draft opens a read-only panel with a Copy button and "Mark as posted" — no pick, no edit, no skip, and no per-reply posted flag.
**Why wrong:** Those per-draft controls live in the three drafts-review components, which are mounted only inside the staff asset card, on staff-only routes that redirect clients away. The client's archive is documented as read-only. So the intake copy sends the client to a screen that structurally cannot do what it says — and the per-draft signal the Reddit copy calls the fastest way to sharpen the agent's voice can never be given by the client at all.
**Fix per doc:** Same root cause as the raw-text archive finding — fix them together. Mount the three DraftsBatch components in the client path by rendering them inside src/components/asset-detail-modal.tsx behind the parser check, passing clientId / jobId / assetId exactly as src/components/asset-card.tsx:715-757 does. If they must stay staff-only, rewrite the three Feedback paragraphs (x-agent-intake.tsx:671-673, linkedin-agent-intake.tsx:623-625, reddit-agent-intake.tsx:298-300) to stop promising controls the client does not have — for Reddit, either surface a per-reply posted control or drop the sentence that calls it the fastest signal.
**Systems touched:** agent intake surfaces (X/LinkedIn/Reddit), workspace archive, asset detail modal, asset-card drafts review

## F29 · HIGH · Track B
**Screenshot:** screenshots/F029.png
**Title:** The amber "3 ready" badge on an agent card never goes away, no matter how many times you review the drafts
**Where:** AI Agents (/clients/<id>/agents) — agent cards. src/components/custom-agents.tsx:477-480; custom-agents.tsx:510-519; src/lib/actions/asset-actions.ts:127-135; src/app/api/agent-service/webhook/route.ts:34-39; webhook/route.ts:397-398; src/components/job-status.tsx:4-10
**What you see:** After the first successful run, each agent card grows an amber bell pill reading "1 ready" or "3 ready". You click it, read and approve everything. Come back and it still says "3 ready". It will say that forever, and the count only ever grows as more runs land.
**Why wrong:** The pill counts runs whose status is still "in review". Nothing in the codebase ever moves a run out of that status — approving a deliverable writes the deliverable, not the run — so the badge is permanently stuck on, which trains everyone to ignore the one new-work signal the client has. It also means the "Approved" and "Delivered" entries in the run status badge are unreachable states.
**Fix per doc:** Close the loop in approveAssetAction (src/lib/actions/asset-actions.ts:127-135): after the asset patch, load the producing job via asset.jobId and, once every job.assetIds asset is approved, updateJob(jobId, { status: "approved" }) using the existing updateJob helper in src/lib/data.ts:405. Alternatively compute the pill in src/components/custom-agents.tsx:477-480 from unapproved asset counts rather than job status — but then also delete the approved/delivered rows from the MAP in src/components/job-status.tsx:4-10, since nothing would ever reach them.
**Systems touched:** jobs status lifecycle, asset approval actions, agent-service webhook, custom agents UI badge

## F128 · HIGH · Track A
**Screenshot:** screenshots/F128.png
**Title:** Agent descriptions are cut off mid-word, with no ellipsis and no way to read the rest
**Where:** AI Agents (/clients/[id]/agents) — agent cards. src/components/custom-agents.tsx — description block on the agent card
**What you see:** Instagram Agent's description ends "…video/clip content is the TikTok Agent (e16), a". Reddit's ends on "(one per". TikTok's ends on "(mandatory". The card just stops mid-word. There is no "more" control anywhere.
**Why wrong:** It reads as a rendering bug because it is one — a hard character cut applied to prose that was never written to be truncated. The information is simply lost, and three of the seven cards are affected.
**Fix per doc:** Stop cutting by character count. Clamp visually with -webkit-line-clamp: 3 so the cut always lands on a line boundary, and add a "More" disclosure that expands in place — reuse Disclosure from src/components/seo-geo/disclosure.tsx rather than writing a new toggle. Fixing #127 makes most cards short enough that the clamp never fires.
**Systems touched:** custom agents UI, seo-geo Disclosure reuse

## F129 · HIGH · Track A
**Screenshot:** screenshots/F129.png
**Title:** "Ready to build your weekly content queue." sits on all seven cards — including the one that says Setup needed
**Where:** AI Agents (/clients/[id]/agents) — the status strip on every agent card. src/components/custom-agents.tsx:546 — the constant string
**What you see:** The identical sentence occupies the status strip of every agent. On the Reddit card it appears directly under a "Setup needed" chip. On Branded Shorts — which cannot run without an uploaded video — it also claims readiness.
**Why wrong:** It is filler in the one slot on the card that should carry real state, and on two cards it flatly contradicts the state shown immediately above it. Repeated verbatim seven times, it reads as a placeholder nobody replaced.
**Fix per doc:** Make that line state-derived. Precedence: blocked → what is missing plus a link to fix it ("Add your Reddit accounts to run this"); scheduled → next run date; drafts waiting → "N drafts waiting for review" linking to the review view; otherwise → "No runs yet". Delete the constant.
**Systems touched:** custom agents UI status strip

## F132 · HIGH · Track A
**Screenshot:** screenshots/F132.png
**Title:** Run history rows are labelled with the operator's raw typing, typos included
**Where:** AI Agents (/clients/[id]/agents) — Recent agent runs. src/components/custom-agents.tsx — recent-runs list renders the submitted prompt as the row label; src/lib/actions/custom-agent-actions.ts — the prompt is stored on the job and echoed back
**What you see:** Verbatim rows on screen: "Create 1 linkein post for the company", "Create 1 linkedIn post", "create 1 post for linkedin", "Create a Linkedin post for the company".
**Why wrong:** The client's permanent run history is somebody's typing — misspellings and inconsistent capitals and all. Read together with the failures on those same rows, it looks like flailing.
**Fix per doc:** Label rows by what was produced, not what was typed: "LinkedIn company post · 1 draft". Keep the raw prompt in the run detail for staff. Never echo free-text input as a client-facing label.
**Systems touched:** custom agents run history UI, custom-agent actions (job prompt storage)

## F134 · HIGH · Track A
**Screenshot:** screenshots/F134.png
**Title:** An unfilled template placeholder is shown to the user: "Focus this batch on [person]'s seat."
**Where:** AI Agents → Run now → X agent run dialog → "Common starting points". The run dialog's starting-point suggestions (src/components/custom-agents.tsx / the X intake view config in src/lib/agent-intake-views.ts)
**What you see:** One of the three suggestion chips reads, literally, "Focus this batch on [person]'s seat." Clicking it would put that text into the run prompt.
**Why wrong:** A square-bracket placeholder that was never substituted. It tells the client the feature is unfinished, and if clicked it sends a literal "[person]" into the agent prompt.
**Fix per doc:** Either substitute a real seat name from the client's seat roster (the X agent data already holds the seat list) or reword the chip so it needs no substitution: "Focus this batch on one person's seat." Add a test that fails on any user-facing string matching /\[[a-z_]+\]/.
**Systems touched:** X agent run dialog, agent-intake-views config, test coverage

## F147 · HIGH · Track B
**Screenshot:** screenshots/F147.png
**Title:** One content stream, two agent identities: "Instagram Agent" and "Social posts (IG/TikTok)" run side by side
**Where:** Calendar, Jobs, run history — everywhere runs are labelled. src/lib/agent-service/products.ts:42-43 — taskType social_post, name "Social posts"; customAgents collection — the Instagram Agent instance for the same client; src/components/run-calendar.tsx — chips render both identities on the same days; Team decision, portal review call 27 Jul 2026
**What you see:** On the calendar, 27 Jul stacks an "Instagram Agent" run chip directly above a "Social posts (IG/TikTok)" post chip; 21 Jul shows "TikTok Agent" twice from two different systems. The agents page sells "Instagram Agent"; deliverables and some runs are labelled "Social posts". On the call: "Why is it still social posts and Instagram agent is not the same? It's the same thing… this pisses me off."
**Why wrong:** The managed-product catalog (task_type social_post, name "Social posts") and the customAgents Instagram Agent are two identities for the same deliverable stream, so calendar, jobs and analytics double-report it and a client sees two agents where there is one. The team decided on the call to unify them.
**Fix per doc:** One identity per stream. Map task_type → the client's custom agent identity in a single shared label helper used by calendar, jobs, run history and analytics (the same helper fixes "TikTok Agent" duplicates); retire the client-facing "Social posts" catalog card for clients whose workspace has the Instagram agent. Cross-ref findings 39/45 — the managed products are already unrunnable from the UI, so nothing breaks.
**Systems touched:** agent-service products catalog, customAgents, calendar, jobs, run history, analytics labelling

## F148 · HIGH · Track B
**Title:** The portal has no launch-vs-runs model — the architecture the team decided on has nowhere to live
**Where:** AI Agents (/clients/[id]/agents) — agent cards, schedule dialog, feedback. src/lib/types.ts:449-456 — templateKey/templateName exist on posts and are rendered nowhere as a group; src/lib/custom-agent-launch.ts — a guided run brief, not a launch lifecycle; src/components/schedule-run-modal.tsx — one "Ongoing direction" textarea, no templates; Feedback actions are per-draft only (x/li/reddit draft feedback) — no template or agent-level target; Team decision, portal review call 27 Jul 2026
**What you see:** Every agent card offers exactly two verbs: "Run now" and "Set schedule". There is no launch step that produces the template set, no template list on the card, no per-template run or feedback, no way to reorder the calendar's templates or leave a note for run day. The call spec'd all of it: launch = a setup run producing research + 3-5 templates, CTAs and goals; runs = day-of generation; feedback at the global level or per template; 25 credits to run one template on demand; clients reorder the calendar and drop notes the engine reads when it runs.
**Why wrong:** The data layer is already half-way there — every post is stamped with templateKey/templateName ("By The Numbers", …) — but no UI groups by template, so the concept the team is selling ("give feedback to this format" / "run this format now") has no surface. It also blocks the cost split the call asked for: launch cost vs recurring run cost cannot be separated when both are just "a run".
**Fix per doc:** Add a launch state and a per-client template registry to the agent instance: the launch run persists its templates; the card renders them with per-template Run (25 credits) and per-template feedback; a second feedback control targets the whole agent; calendar slots reference a templateKey plus an optional client note consumed at run time; analytics tag runs launch vs recurring. This is the biggest single work item in the document — it is the product model, not a patch.
**Systems touched:** product model (launch/runs), custom agents, schedule modal, calendar, feedback, analytics, credits

## F30 · MEDIUM · Track B
**Screenshot:** screenshots/F030.png
**Title:** A run you cancelled on purpose comes back as a red "Failed", and a client cannot cancel at all
**Where:** Job detail (/jobs/<id>) and the run rows on AI Agents. src/app/api/agent-service/webhook/route.ts:34-39; webhook/route.ts:377-384; src/lib/types.ts:197-203; src/components/job-status.tsx:4-10; src/lib/actions/external-job-actions.ts:29-44; src/app/(app)/jobs/[id]/page.tsx:47; src/components/managed-job-progress.tsx:15-19
**What you see:** Staff press Cancel on a running agent. The page adds a "Cancellation requested" line and keeps saying Running. Minutes later it flips to a red Failed badge with the error "Job cancelled", and the progress strip shows "Run failed" pinned to the working step. A client who has just fired a twenty-five-minute billable run has no cancel control anywhere — the button exists only on the staff-only run detail page.
**Why wrong:** There is no cancelled state: the delivery handler folds cancelled onto failed and the status badge has no cancelled entry, so a deliberate stop is presented as a breakage. That pollutes failure counts and leaves staff unsure whether the cancel worked. Separately, a client who mis-fires a paid run cannot stop it and cannot reach the only page that could.
**Fix per doc:** Add "cancelled" to JobStatus (src/lib/types.ts:197-203); split it out of STATUS_MAP in src/app/api/agent-service/webhook/route.ts:34-39 so cancelled maps to itself while failed/dead_letter stay failed; give it a neutral tone and "Cancelled" label in the MAP in src/components/job-status.tsx:4-10; and handle it in src/components/managed-job-progress.tsx:15-19 so the strip does not read "Run failed". Then expose a client-safe cancel on the in-flight run row in src/components/custom-agents.tsx (584-634) backed by a client-authorized wrapper around cancelAgentServiceJob — cancelManagedJobAction is requireStaff today. The refund is already handled: refundJobCharge runs for every non-done outcome (webhook/route.ts:196-205).
**Systems touched:** job status model, agent-service webhook, job-status badge, managed-job-progress, external-job actions, client cancel, credits refund

## F31 · MEDIUM · Track B
**Screenshot:** screenshots/F031.png
**Title:** After a client presses Run, the page never updates again — no progress, no completion signal
**Where:** AI Agents (/clients/<id>/agents) — after "Start run". src/app/(app)/clients/[id]/agents/page.tsx:213-238; page.tsx:204; src/components/custom-agents.tsx:995-1010; custom-agents.tsx:1249-1254; custom-agents.tsx:584-634; src/components/auto-refresh.tsx:11; src/app/(app)/jobs/[id]/page.tsx:33; jobs/[id]/page.tsx:53; +1 further reference in the verification record
**What you see:** You press Start run and get a green tick: the agent is working, usually ten to twenty minutes, and deliverables will appear in your Workspace archive once your Karos team approves them. You close the dialog. The run shows in "Recent agent runs" as Queued. Twenty minutes later it still says Queued. Nothing on the page moves and there is no link on the row to follow. Reloading the browser is the only way to see it change.
**Why wrong:** The auto-refresh component and the three-step Queued → Agent working → Ready strip both exist, but they are mounted only on the staff-only run detail page. The client's agents page has no auto-refresh, and client run rows deliberately carry no link, so there is nowhere to go and watch either. A ten-to-twenty-minute wait with a row frozen on "Queued" reads as a stuck run.
**Fix per doc:** In src/app/(app)/clients/[id]/agents/page.tsx render <AutoRefresh /> (src/components/auto-refresh.tsx) in the client branch whenever any row returned by toRunRows has status queued or running. Then reuse <ManagedJobProgress status={run.status} /> (src/components/managed-job-progress.tsx) on the in-flight row inside the "Recent agent runs" list at src/components/custom-agents.tsx:584-634 so the client sees Queued → Agent working → Ready for review. Finding #2 must land first for the "N ready" pill to be a trustworthy arrival signal.
**Systems touched:** client agents page, auto-refresh, managed-job-progress, run rows UI; depends on finding #2

## F32 · MEDIUM · Track A
**Screenshot:** screenshots/F032.png
**Title:** Dialogs cap at 720 pixels tall with one scroll box and no fixed action bar, so Start run scrolls out of sight on the long agent briefs
**Where:** Run agent dialog; X, LinkedIn and Reddit agent data dialogs. src/components/modal.tsx:81; modal.tsx:94-98; src/components/custom-agents.tsx:1249-1258; custom-agents.tsx:1075-1079; custom-agents.tsx:933; src/components/x-agent-intake.tsx:751-763
**What you see:** Open Run now on the X agent. The dialog is 720 pixels tall no matter how large the display. Inside it: title, description, an agent-data button, an intro card of deliverables, a client picker, a row of starting-point chips, the brief fields, the attachments picker, then the cost estimate and Start run. On the agent-data pane it is a company form, a card per seat, an add-a-seat button, a news box and a feedback box. The primary button shares one scroll box with the title, so it is off screen for most of the interaction and there is no bar pinned to the bottom.
**Why wrong:** The dialog hard-codes its height cap and puts title, description, body and actions all inside a single scrolling area, so long forms hide their own submit button and the caller has to reset scroll position by hand every time it swaps panes. On a tall display the dialog leaves hundreds of pixels of available height unused for no reason.
**Fix per doc:** Restructure src/components/modal.tsx into three flex rows inside the panel: a `shrink-0` header holding title, description and the close button (currently 87-96), a `min-h-0 flex-1 overflow-y-auto` body that keeps the existing scrollRef target (94), and an optional `shrink-0 border-t` footer fed by a new `footer?: ReactNode` prop. Raise the cap at line 81 to `max-h-[calc(100dvh-3rem)]`. Then move the estimate-plus-Start-run row from src/components/custom-agents.tsx:1249-1258 and the intake pane's Close row from 1075-1079 into that footer, so the primary action is always visible and the manual `scrollRef.current?.scrollTo({top: 0})` at line 933 can go.
**Systems touched:** modal primitive, custom agents run dialog, agent intake dialogs

## F33 · MEDIUM · Track A
**Title:** Every agent deliverable is titled "<Agent name> - <Client name>" — the client sees their own company name bolted onto each item
**Where:** Workspace → Archive, staff Assets library, job detail — titles of anything an agent produces. src/app/api/agent-service/webhook/route.ts:319-327; src/lib/jobs/submit-custom.ts:207; src/lib/jobs/submit-managed.ts:108; src/lib/agent-service/run-custom-agent.ts:153
**What you see:** Items come out named "Reddit Agent - Acme Corp", "Blog article - Acme Corp", "Karos LinkedIn Company - Acme Corp". In a client's own workspace, where every single item belongs to them, half of every title is their own name.
**Why wrong:** The delivery handler deliberately strips the appended client name so the item is titled just the agent name, and its comment states the format it expects. But it looks for a long dash while all three submit paths write a plain hyphen, so the strip never fires — for any run, from any path. A one-character mismatch, affecting every deliverable.
**Fix per doc:** Export one shared separator constant (e.g. JOB_TITLE_CLIENT_SEPARATOR = " - " from a small shared module) and use it in all four places: the three title builders (src/lib/jobs/submit-custom.ts:207, src/lib/jobs/submit-managed.ts:108, src/lib/agent-service/run-custom-agent.ts:153) and the strip at src/app/api/agent-service/webhook/route.ts:323, replacing the em dash. Fix the stale comment at 319-321 too. Existing asset titles need a one-off backfill or they stay wrong.
**Systems touched:** agent-service webhook delivery, job submit paths, asset titles, data backfill

## F34 · MEDIUM · Track A
**Title:** If the agent service is unconfigured, a client's agents silently vanish behind "No active agents yet"
**Where:** AI Agents (/clients/<id>/agents) — client view. src/app/(app)/clients/[id]/agents/page.tsx:219; page.tsx:230-236; page.tsx:308-313
**What you see:** A client who has three granted agents, live schedules and a run history opens AI Agents and sees an empty box: "No active agents yet — After your Karos team completes the first agent run, that agent will appear here." Their agents, their schedules and their run history are all gone from the page.
**Why wrong:** The empty state is chosen when either the client has no agents or the agent service settings are missing, and the copy only describes the first case. An outage or a bad deploy therefore reads to the client as "you were never set up", contradicting runs they have already received. Staff get an honest "Agent service not configured" message for the very same condition further down the same file, so the correct copy already exists.
**Fix per doc:** Split the condition at src/app/(app)/clients/[id]/agents/page.tsx:219. Keep the current EmptyState only for agents.length === 0. When agents.length > 0 but !agentServiceConfigured, either render <ClientCustomAgents> as normal (the Run button already refuses gracefully) or show a distinct EmptyState whose copy says agent runs are temporarily unavailable and the Karos team has been notified — do not reuse the never-set-up wording.
**Systems touched:** client agents page empty states, agent-service configuration handling

## F35 · MEDIUM · Track A
**Title:** On the staff Agents page you can only discover an agent's client binding and its missing setup by firing a run and reading the error
**Where:** Agents (/agents) → Custom agents → Run. src/app/(app)/agents/page.tsx:57-63; src/components/custom-agents.tsx:365-372; custom-agents.tsx:881; custom-agents.tsx:889-890; custom-agents.tsx:1237-1244; custom-agents.tsx:315-317; src/lib/jobs/submit-custom.ts:110-114; src/lib/custom-agent-launch.ts:829-835
**What you see:** The staff Agents page lists every custom agent, including per-client instances whose card gives no hint which client they belong to. You press Run, the client picker defaults to the first client in the list, you fill in the brief, press Start run — and get a paragraph explaining the agent only runs for one specific client. Same for the X, LinkedIn and Reddit agents: no agent-data button, no setup warning, just a refusal after the brief is written. The offered link navigates away and takes the brief with it.
**Why wrong:** A client's own AI Agents page filters mismatched instances off the list and shows an agent-data button with an inline setup pane, so both gates are visible before anyone types. The staff hub passes neither the setup payloads nor a filtered client list to the same dialog, so both gates can only fire after submission. The refusal does offer a way to the setup, but on this page it is a plain link that discards the brief instead of the in-dialog pane switch the client page uses.
**Fix per doc:** In CustomAgentsHub (src/components/custom-agents.tsx:247): compute perClientAgentSlug(agent.key) per card and, when set, show the bound client's name on the card and pass only clients matching agentKeyMatchesClientSlug into RunCustomAgentModal at 365-372 (disable Run when no eligible client remains) — both helpers are already exported from src/lib/custom-agent-launch.ts:814-835. For the three intake-driven agents, either fetch the setups for the selected client in src/app/(app)/agents/page.tsx and pass xSetup/linkedinSetup/redditSetup through the hub so the dialog gets its data pane, or badge those cards "Set up on the client's AI Agents page" so the gate is visible before the brief is written.
**Systems touched:** staff Agents hub, run dialog client filtering, custom-agent-launch helpers, intake setups

## F36 · MEDIUM · Track A
**Screenshot:** screenshots/F036.png
**Title:** Pressing "Start run" on the LinkedIn or Reddit agent without typing anything is refused — even though the only field is labelled optional
**Where:** AI Agents (/clients/<id>/agents) → run dialog for the LinkedIn and Reddit agents. src/components/custom-agents.tsx:966-969; src/lib/custom-agent-launch.ts:856-865; custom-agent-launch.ts:243-251; custom-agent-launch.ts:380-388; custom-agent-launch.ts:336-346; custom-agent-launch.ts:848-854; src/lib/jobs/submit-custom.ts:117-118; src/components/custom-agents.tsx:1142-1161
**What you see:** The Reddit run dialog has one box, "Anything to steer this run?", whose helper line reads "Optional. The agent picks the thread from the stored Reddit agent data either way." You leave it empty as instructed and press Start run. Red text: "Complete the brief before starting the run." Nothing on the form is marked required, so there is no way to tell what to complete. Same on LinkedIn. Clicking one of the suggestion chips above the box does get you moving, but nothing says that is what the error means.
**Why wrong:** The dialog builds the brief by joining only the non-empty fields and then refuses an empty result, so an agent whose single field is optional can never be run with the form left alone — which is the run these two agents are documented to support. The X agent escapes it only because its scope dropdown carries a default value. The error message also names a requirement that does not exist.
**Fix per doc:** In submit() at src/components/custom-agents.tsx:966-969, when buildCustomAgentPrompt returns empty and profile.fields contains no required field, fall back to a default instruction instead of erroring — the cleanest source is profile.quickStarts[0] (already rendered as the chips at 1142-1161). Keep the existing error only when a required field is genuinely blank. The server core rejects an empty prompt too (src/lib/jobs/submit-custom.ts:117-118), so build the fallback client-side rather than relaxing that guard. Optionally give the two profiles a defaultValue in src/lib/custom-agent-launch.ts (243-251, 380-388), the same trick the X profile uses at 336-346.
**Systems touched:** run dialog validation, custom-agent-launch profiles, submit-custom guard

## F37 · MEDIUM · Track A
**Screenshot:** screenshots/F037.png
**Title:** Staff cross-client lists render every row in the database with no search, filter, sort or paging, and no loading feedback
**Where:** Jobs (/jobs) and Clients (/clients). src/app/(app)/jobs/page.tsx:29-51; src/lib/data.ts:383-393; src/app/(app)/clients/page.tsx:11; src/components/clients-grid.tsx:366; src/components/sidebar.tsx:128-137; src/app/(app)/assets/loading.tsx
**What you see:** The Jobs page is one flat list of every agent run that has ever happened across every client — agent name, client, relative time, status. No status tabs, no client filter, no search, no paging. The Clients page is a three-across grid of every client with no search and no sort, even though the client picker in the rail right next to it does have a search box. Clicking either nav item leaves the previous page frozen on screen with no feedback while the server reads.
**Why wrong:** Both pages read the entire collection and then render every row. Jobs is the page staff use most and the hardest to use at agency scale. Clients additionally reads every deliverable and every run in the database just to print two counts per card. Neither route has a loading placeholder, so the framework holds the old page in place for the whole round trip — only three of twenty-nine routes have one, and the heaviest three are not among them.
**Fix per doc:** For /jobs: move the list into a client component with a filter strip modelled on src/components/assets-view.tsx:69-91 — a status select over JobStatusBadge's states, a client select built from the `clients` array the page already loads at line 14, a search over agentName plus title — then `slice(0, 50)` with a "Load 50 more" button. For /clients: add a search input and a sort select (name / most recent run / most deliverables) above the grid at src/components/clients-grid.tsx:366, and replace the `listAssets()` / `listJobs()` full scans at src/app/(app)/clients/page.tsx:11 with per-client counts. Add `loading.tsx` files re-exporting the same skeleton used by src/app/(app)/assets/loading.tsx to /jobs, /clients, /admin/analytics, /clients/[id]/settings and /clients/[id]/agents.
**Systems touched:** jobs page, clients page/grid, data.ts list scans, route loading states

## F38 · MEDIUM · Track B
**Title:** The Agents hub offers agent-and-client pairings the server is guaranteed to refuse, after the whole brief has been typed
**Where:** Agents (/agents) → a per-client agent instance → Run. src/app/(app)/agents/page.tsx:59; src/components/custom-agents.tsx:249; custom-agents.tsx:255; custom-agents.tsx:365-372; custom-agents.tsx:1122-1140; src/lib/custom-agent-launch.ts:822-835; src/lib/jobs/submit-custom.ts:106-114; src/app/(app)/clients/[id]/agents/page.tsx:248
**What you see:** Staff open the Agents page, see every custom agent including the per-client instances, click Run, get a dialog whose client dropdown lists every active client, fill in the brief and attachments, press Start run — and get told the agent runs only for the client whose lab slug matches, and that this client's slug is a different one. Nothing has run. The typing survives, but the trip was wasted.
**Why wrong:** The hub hands the unfiltered client list straight to the run dialog, and the dropdown never consults the agent's binding. The predicate that decides the outcome is applied on the per-client agents page but not here — and its own docstring claims the opposite, that both the list and the server refusal read the one predicate so a card is never offered that the server would reject. With an instance per client, most offered combinations are dead.
**Fix per doc:** Widen the clients prop: change src/app/(app)/agents/page.tsx:59 to pass `agentsRepoSlug` alongside id and name, and widen the type at src/components/custom-agents.tsx:255 to match. Then at the point the dialog is opened (custom-agents.tsx:365-372) pass only `clients.filter(c => agentKeyMatchesClientSlug(agent.key, c.agentsRepoSlug))` — the same predicate the per-client page uses at src/app/(app)/clients/[id]/agents/page.tsx:248 and the submit core enforces at src/lib/jobs/submit-custom.ts:110. When exactly one client is eligible, drop the dropdown at 1122-1140 and show the name as a fixed chip; when none are, disable the card's Run button with a title explaining the instance is bound to a client whose lab slug is not set, rather than opening a dialog that cannot succeed.
**Systems touched:** staff Agents hub, run dialog client dropdown, custom-agent-launch predicate, submit-custom guard

## F39 · MEDIUM · Track B
**Title:** The four managed lab products (Social, Newsletter, Blog, Landing page) cannot be run from anywhere in the app
**Where:** Client AI Agents (/clients/[id]/agents) — and every surface that names them. src/components/managed-products.tsx:34 — `export function ManagedProducts({`; a repo-wide grep for ManagedProducts / "managed-products" returns only this line; managed-products.tsx:452 — the only submitManagedJobAction call in any component; src/lib/agent-service/products.ts:40 — MANAGED_PRODUCTS, the four-product catalog with full briefFields; src/lib/actions/external-job-actions.ts:16 — submitManagedJobAction (requireStaff); src/app/(app)/clients/[id]/agents/page.tsx:220,299 — both branches render only ClientCustomAgents; src/components/seo-geo/presenter.ts:721-726 — PRODUCT_AGENT_LABELS still names all four to clients
**What you see:** The AI Agents page shows only the custom agents (X / LinkedIn / Reddit instances). There is no card, no run dialog and no entry point for Social posts, Newsletter issue, Blog article or Landing page — even though all four are fully described in the product catalog with taglines, deliverables, time estimates and complete brief forms.
**Why wrong:** The section that renders those four products, with its guided run dialog and its run history, exists and is finished — and nothing in the app imports it. A repo-wide search for it returns only its own definition. So the entire managed-product run path is unreachable from a browser, and every surface that promises those agents (the SEO/GEO fix chips, the Connect page's Instagram example) is promising a product with no front door.
**Fix per doc:** Decide once and make it true everywhere. Either mount <ManagedProducts clientId contextItems jobs jobPreviews /> into src/app/(app)/clients/[id]/agents/page.tsx alongside ClientCustomAgents (the staff branch at line 299 already computes `jobPreviews` for exactly this component, which is strong evidence it was unmounted by accident) — note submitManagedJobAction is staff-only, so if it goes in the client branch at line 220 the action needs a client path. Or delete src/components/managed-products.tsx and strip the four names out of client-facing promises: PRODUCT_AGENT_LABELS in src/components/seo-geo/presenter.ts:721-726 and the Instagram walkthrough on src/app/(app)/connect/page.tsx:62-68.
**Systems touched:** managed products component, agent-service catalog, external-job actions, client/staff agents page, seo-geo presenter, connect page

## F40 · MEDIUM · Track A
**Screenshot:** screenshots/F040.png
**Title:** The schedule dialog's "Posts per week" is actually runs per week — the cost line one paragraph below contradicts the label
**Where:** Client AI Agents → agent card → "Keep <Agent> running" dialog (/clients/[id]/agents). src/components/custom-agents.tsx:743; custom-agents.tsx:755; custom-agents.tsx:795; custom-agents.tsx:689; src/lib/actions/planned-run-actions.ts:167; planned-run-actions.ts:177; planned-run-actions.ts:236; src/lib/scheduled-runs.ts:97
**What you see:** A dropdown labelled "Posts per week" (1-7) next to "Outputs per run" (1-5). Pick 3 and 5 and the cost box directly below reads "3 runs x 5 outputs x 12 credits" — 15 posts a week, not 3. The confirmation that lands in the client's activity feed also says "Set <Agent> to 3 posts per week".
**Why wrong:** The number is the count of days the agent fires; each firing produces the "outputs per run" number of items. So a client sizing their weekly content volume off this label is understating it by up to five times, and the same wrong wording is repeated back to them in the activity log entry. The dialog disproves its own label one paragraph later.
**Fix per doc:** src/components/custom-agents.tsx:743 — change the label text "Posts per week" to "Runs per week". Add the resulting volume to the cost box after line 796 ("Credits are charged when each scheduled run starts."): `{postsPerWeek * outputsPerRun} new drafts a week.` Also fix the activity-log copy at src/lib/actions/planned-run-actions.ts:236, which writes "Set <agent> to N posts per week" — make it "N runs per week (M drafts)". Leave the field name postsPerWeek alone; it is persisted and read in several places.
**Systems touched:** schedule dialog, planned-run actions, activity log copy

## F130 · MEDIUM · Track A
**Screenshot:** screenshots/F130.png
**Title:** Every agent is priced identically at "25 credits per output"
**Where:** AI Agents (/clients/[id]/agents) — agent card footer. src/components/custom-agents.tsx:550 — cost line; src/lib/credits.ts — single default cost rather than a per-agent value
**What you see:** All seven cards print "25 credits per output" — a full video edit, a website rebuild and a single LinkedIn post are priced the same.
**Why wrong:** A flat number across wildly different work reads as a placeholder, and it is the number a client budgets against. (The schedule dialog does correctly derive a weekly total — "3 runs × 1 outputs × 25 credits = 75 credits" — so only the per-agent price is wrong, not the arithmetic.)
**Fix per doc:** Put the cost on the agent record so each agent can price its own output, defaulting to the current value. Leave the schedule dialog's derived weekly total as-is — it is already correct.
**Systems touched:** credits pricing, customAgents record, agent card UI

## F41 · LOW · Track A
**Screenshot:** screenshots/F041.png
**Title:** Clients are shown the raw internal status word for their agent runs — "Run 2026-07-27 · review", "· failed"
**Where:** AI Agents run dialog → X / LinkedIn / Reddit agent data pane → Feedback card. src/components/x-agent-intake.tsx:686; src/components/linkedin-agent-intake.tsx:638; src/components/reddit-agent-intake.tsx:313; src/components/job-status.tsx:4-15; src/lib/agent-intake-views.ts:116-121
**What you see:** Above the feedback box, a list of recent runs reads "Run 2026-07-27 · review", "Run 2026-07-25 · queued", "Run 2026-07-24 · failed". Lowercase internal words, no explanation, and for a client no link — a failed run is a dead line of text with nothing to click and no reason given.
**Why wrong:** These are the internal status values printed verbatim into client-facing copy on three separate surfaces, when the app already has a mapper that renders them as "In review", "Queued", "Failed" with a colour and is used everywhere else. The date is likewise printed in raw machine format while the rest of the app formats dates.
**Fix per doc:** In all three feedback run lists, replace `· {r.status}` with <JobStatusBadge status={r.status} /> from src/components/job-status.tsx — x-agent-intake.tsx:686, linkedin-agent-intake.tsx:638, reddit-agent-intake.tsx:313; the row views already carry the typed status (src/lib/agent-intake-views.ts:116-121). Replace the raw ISO dates (x-agent-intake.tsx:681/684, linkedin-agent-intake.tsx:633/636, reddit-agent-intake.tsx:308/311) with relativeTime or formatDate from src/lib/utils, and do the same for the feedback-history dates at x:725, linkedin:677, reddit:343.
**Systems touched:** agent intake feedback lists, job-status badge, date formatting utils

## F42 · LOW · Track A
**Title:** Seat forms refuse to save over a field that is not marked required
**Where:** AI Agents run dialog → X agent data → "Add a seat" and each seat card (and the same two LinkedIn forms). src/components/x-agent-intake.tsx:84; x-agent-intake.tsx:285-288; x-agent-intake.tsx:492; x-agent-intake.tsx:584; src/lib/actions/x-agent-actions.ts:187; x-agent-actions.ts:305; src/components/linkedin-agent-intake.tsx:423; linkedin-agent-intake.tsx:551; +2 further references in the verification record
**What you see:** You add a seat: type the person's name, their handle, skip "Anything we must never post" because nothing indicates it is required, press Add seat. Red text: "Tell us what we must never post (or write nothing)." The company form above marks its two mandatory questions with a red asterisk; the seat form marks nothing, and the field's own placeholder invites you to skip it.
**Why wrong:** The server rejects an empty off-limits answer on both seat paths, add and edit, but neither label carries the required marker that the same file already defines and uses on the company form. The user has to fail the save to learn the rule. The LinkedIn seat forms have the identical mismatch.
**Fix per doc:** Add <RequiredMark /> (defined at src/components/x-agent-intake.tsx:84) to the "Anything we must never post" labels in SeatCard (line 492) and AddSeatForm (line 584), matching the company form at 285-288, and soften the placeholders so they read as a required choice rather than an optional one. Do the same in src/components/linkedin-agent-intake.tsx at 423 and 551 — its server actions reject the empty answer at src/lib/actions/linkedin-agent-actions.ts:141 and 219. Leave the LinkedIn company field at 159 alone: it is genuinely optional there and already labelled so.
**Systems touched:** X agent intake forms, LinkedIn agent intake forms, x-agent/linkedin-agent actions validation

## F43 · LOW · Track A
**Title:** Staff land on a completely blank AI Agents page for any client whose agent list is empty
**Where:** AI Agents (/clients/<id>/agents) — staff view. src/app/(app)/clients/[id]/agents/page.tsx:298-307; page.tsx:247-249; src/components/custom-agents.tsx:437; src/lib/custom-agent-launch.ts:829-835
**What you see:** Page header "AI Agents — Run custom AI agents for this client and track their deliverables", three small header links, and then nothing. No cards, no empty state, no hint that agents need importing or that the client's lab-repo slug is unset. Just white space to the bottom of the viewport.
**Why wrong:** The agents section returns nothing at all when there are no agents and no run history, and the page's only empty state belongs to the other branch (service unconfigured). This is the first thing a staff member sees for a brand-new client and it offers no next action, even though both causes — no enabled custom agents imported, or every enabled agent being a per-client instance bound to a different client's slug — are named right there in the filter.
**Fix per doc:** In src/app/(app)/clients/[id]/agents/page.tsx, before rendering <ClientCustomAgents> in the staff branch, add an EmptyState (from src/components/ui) when enabledAgents.length === 0 and toRunRows(jobs, true).length === 0: name both causes and link to /agents to import or enable an agent, plus /clients/<id>/settings when client.agentsRepoSlug is unset, since that is what the agentKeyMatchesClientSlug filter at line 248 drops per-client instances on.
**Systems touched:** staff agents page empty state, custom-agent-launch filter, ui EmptyState

## F44 · LOW · Track A
**Screenshot:** screenshots/F044.png
**Title:** The client AI Agents page stacks two near-identical headings and taglines on top of each other
**Where:** Client AI Agents page (/clients/[id]/agents). src/app/(app)/clients/[id]/agents/page.tsx:216; page.tsx:217; src/components/custom-agents.tsx:457; custom-agents.tsx:459; custom-agents.tsx:460
**What you see:** "AI Agents — Your active AI team, run agents now or set their weekly production pace." and below it a second heading "Your AI agents — Your always-on AI team. Run an agent now or choose its weekly production pace."
**Why wrong:** The page header and the section header say the same thing twice in slightly different words ("active AI team" versus "always-on AI team", "set their weekly production pace" versus "choose its weekly production pace"), one in Title Case and one in sentence case. It reads like two people wrote the page and neither deleted the other's line, and it spends the top of the fold on nothing.
**Fix per doc:** Keep one. src/app/(app)/clients/[id]/agents/page.tsx:216-217 — set `title="AI agents"` and `description="Your always-on AI team. Run an agent now, or set its weekly production pace."`. Then in src/components/custom-agents.tsx:457-461 suppress the duplicate for client viewers: render the h2 only when `!viewerIsClient` (keep "Custom agents" for staff) and gate the `<p>` at 458-462 the same way. Keep the credits badge at 464-468 unconditionally — it is the only unique content in that header row.
**Systems touched:** client agents page header, custom agents section header

## F45 · LOW · Track A
**Title:** Two managed-product run components and their submit action are dead code — the four catalog products cannot be started from the agents area at all
**Where:** Agents (/agents) and AI Agents (/clients/<id>/agents). src/components/managed-products.tsx:34; src/components/client-managed-agents.tsx:61; src/lib/actions/external-job-actions.ts:16-26; src/app/(app)/agents/page.tsx:57-63; src/app/(app)/clients/[id]/agents/page.tsx:298-307; page.tsx:270-278; src/lib/execution-engine.ts:315-323; src/lib/actions/task-actions.ts:133; +1 further reference in the verification record
**What you see:** Staff open Agents or a client's AI Agents page looking for Social posts, Newsletter issue, Blog article or Landing page — the four catalog products with taglines, deliverables and time estimates. Neither page lists them. No card, no Run button, no mention. The only way to start one is to put a task on the client's board and let the board execute it.
**Why wrong:** A complete guided run dialog for these four (per-product brief fields, attachment profiles, live/dormant cards, run history) exists as one component, and a client-facing view of them exists as another, and neither component is imported anywhere in the repo. The server action they call has no other caller, so it is unreachable too. That is a few hundred lines of maintained-looking dead UI that will drift out of sync with the catalog, and staff who expect to launch a blog article from the Agents page have no signpost telling them the task board is where it happens.
**Fix per doc:** Pick one direction. To retire: delete src/components/managed-products.tsx and src/components/client-managed-agents.tsx, delete submitManagedJobAction from src/lib/actions/external-job-actions.ts:16-26 (keep the submitManagedJob core — the task engine calls it at src/lib/execution-engine.ts:318), and drop the now-unused jobPreviews block at src/app/(app)/clients/[id]/agents/page.tsx:270-278. To restore: mount <ManagedProducts clientId contextItems jobs jobPreviews /> in that page's staff branch alongside <ClientCustomAgents>, and <ClientManagedAgents> in the client branch.
**Systems touched:** managed products / client-managed-agents dead code, external-job actions, execution engine, agents pages

## F135 · LOW · Track A
**Screenshot:** screenshots/F135.png
**Title:** "3 runs × 1 outputs × 25 credits" — unpluralised units in the cost line
**Where:** AI Agents → Set schedule → "Keep X Agent running". src/components/schedule-run-modal.tsx — the estimated weekly cost line
**What you see:** The estimated-cost line reads "3 runs × 1 outputs × 25 credits".
**Why wrong:** "1 outputs". The rest of the dialog is carefully written, so this reads as unfinished.
**Fix per doc:** Pluralise from the value: `${n} ${n === 1 ? "output" : "outputs"}`.
**Systems touched:** schedule-run modal copy

## F46 · BLOCKER · Track B — CONTINUES past p84
**Title:** A client can never act on a draft — the pick/post/skip loop the intake copy promises does not exist for them, and their copy of the draft is an unformatted text dump
**Where:** Workspace → Archive tab (/tasks) — where the agent forms tell clients their drafts land. src/components/x-agent-intake.tsx:670-673; src/components/linkedin-agent-intake.tsx:622-625; src/components/reddit-agent-intake.tsx:297-301; src/components/archive-view.tsx:84; archive-view.tsx:90; src/components/asset-detail-modal.tsx:211; src/components/asset-card.tsx:433-457; asset-card.tsx:712-764; +12 further references in the verification record
**What you see:** The X, LinkedIn and Reddit forms tell the client that picking, editing or skipping individual drafts happens on the drafts themselves, in their Workspace archive. The client opens Workspace, goes to the Archive tab, clicks the tile for the batch, and gets one wall of text with every hash mark, asterisk, backtick and quote arrow showing exactly as typed. No cards, no thread link to click, no Post / Posted with edits / Didn't post / Request a change buttons. Nothing to do but read it and close.
**Why wrong:** The whole per-draft loop — the four outcome actions that train the agent, the copy-the-reply-and-open-the-thread hand-off, the LinkedIn compose prefill — is built and wired, but it is mounted only inside the asset card. The only two places that render an asset card are the staff-only job detail page and the staff Assets library, and both client routes into that library redirect client users away. The Archive tab instead opens a read-only detail modal that prints the asset's text verbatim. So the promise on three intake surfaces cannot be kept, the deliverable reaches the client as unrendered source (house rule: never render raw model copy to a client), and the outcome rows the Reddit contract calls the only copy of the client's outcomes can in practice only be written by staff — even though the server action behind them already accepts client callers.
**Fix per doc:** (CONTINUES past p84 — FIX not in this range.)
**Systems touched:** workspace archive, asset detail modal, asset-card drafts review, agent intake copy, draft outcome actions

---

Section note captured on p84: "Workspace, tasks & deliverables" — 28 findings: #46, 47, 48, 50, 51, 53, 54, 55, 56, 57, 58, 149, 150, 60, 61, 62, 63, 64, 65, 66, 67, 68, 69, 70, 136, 71, 72, 73.
