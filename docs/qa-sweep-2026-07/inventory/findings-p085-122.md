# QA sweep 2026-07-27 — findings extracted from pages 85–122

Source PDF: Karos-portal-QA-sweep-FULL-2026-07-27.pdf. Screenshot content omitted per spec; everything else captured.
F77 straddles the p122 boundary; its full body (p123–124) is completed here — this file is its sole owner.

---

F46 → see findings-p047-084.md

---

## F47 · BLOCKER · Track B
**Title:** The client's Archive shows the agent deliverable as raw text with all its formatting symbols on screen — and shows unapproved drafts straight away
**Where:** src/components/asset-detail-modal.tsx:211 (only content renderer: `<p className="whitespace-pre-wrap …">{asset.content}</p>`, no parser, no markdown renderer); src/components/archive-view.tsx:90 (ArchiveView opens AssetDetailModal for every tile); src/components/archive-view.tsx:30 (header comment: "Read-only — review/approval stays on the staff Library"); src/components/asset-card.tsx:442,448,455 (staff card runs parseLiDrafts / parseRedditDrafts / parseXDrafts on asset.content); src/components/asset-card.tsx:715,734,752 (LiDraftsBatch / RedditDraftsBatch / XDraftsBatch, the parsed per-draft UI, mounted only here); src/lib/li-drafts.ts:46, src/lib/reddit-drafts.ts:289, src/lib/x-drafts.ts:79 (the three parsers); src/app/api/agent-service/webhook/route.ts:337,346 (asset created with content = raw text artifact and status "draft"); src/lib/asset-visibility.ts:6 ("Drafts remain visible so pending work is reviewable"; only future-dated (locked) assets are redacted); +2 further references in verification record
**What you see:** Workspace → Archive tab (/tasks, client role) → tile → asset detail modal. A client opens Workspace → Archive and clicks a tile from the X, LinkedIn or Reddit agent. The detail panel prints the whole deliverable as plain text with every formatting symbol visible — hash marks in front of "Reddit answer drafts" and "Account 1", asterisks around bold words, angle brackets in front of the quoted reply, pipe characters where a table should be. Ten drafts run one after another with no cards, no headings and no separation. The status badge on the tile reads "draft", even though the run confirmation told the client that deliverables show up here only after the Karos team approves them.
**Why wrong:** These deliverables are structured documents by contract, and staff get a proper parsed per-draft review panel for the exact same content. The client's archive prints the stored text verbatim, so the client sees the machine formatting rather than the drafts. It breaks the standing rule that unpolished model output never reaches a client, and it is the most-visited client screen after a run. Second, unapproved drafts are deliberately not filtered out of the client library, so the draft appears the instant the run finishes — directly contradicting the promise made in the run confirmation dialog.
**Fix per doc:** src/components/asset-detail-modal.tsx: before the raw fallback at line 211, run the same three parsers the staff card uses (parseLiDrafts from src/lib/li-drafts.ts, parseRedditDrafts from src/lib/reddit-drafts.ts, parseXDrafts from src/lib/x-drafts.ts, guarded by the same content sniffs as asset-card.tsx:442-455) and render LiDraftsBatch / RedditDraftsBatch / XDraftsBatch, passing clientId, jobId and assetId exactly as asset-card.tsx:715-757 does. As a floor for everything else, route asset.content through a markdown renderer instead of the bare `<p>`. Separately close the approval lie: either filter out status==="draft" for client viewers where the archive set is built (src/app/(app)/tasks/tasks-body.tsx:64, the getClientLibraryAssets call), or change the run-dialog sentence at src/components/custom-agents.tsx:1002-1003 to say drafts appear straight away and are marked as drafts until the team approves them.
**Systems touched:** archive/asset viewer, drafts parsers, agent-service webhook assets, asset visibility, custom-agent run dialog copy

---

## F48 · HIGH · Track B
**Title:** "Autopilot on" stays on forever but only ever runs one batch
**Where:** src/components/tasks-board.tsx:145; src/components/tasks-board.tsx:699; src/lib/actions/settings-actions.ts:39; settings-actions.ts:96; src/lib/execution-engine.ts:466; vercel.json:6; settings-actions.ts:87 (client-billed path likewise calls runClaimedTasks once); settings-actions.ts:38-39 (the only other read of settings.autopilot: same-state idempotency guard); +3 further references in verification record
**What you see:** Workspace → Board → "Automated" tab → Autopilot toggle. You flip the switch, it turns green, reads "Autopilot on" and stays that way. It works through at most five of the pending items that existed at the moment you flipped it. Everything created afterwards sits in Pending untouched while the switch keeps saying Autopilot is on.
**Why wrong:** The persistent "on" state promises standing automation that no code path provides. The flag is written to the client's settings and read back only to render the switch and to make a repeat toggle idempotent; the batch runner is called only from the toggle itself, and no scheduled job looks at the flag. A client switches it on, walks away, and believes the board is being worked. The only way to get another batch is to switch it off and on again.
**Fix per doc:** Pick one and make the label honest. (a) Wire the flag into the existing cron: src/app/api/run-scheduled/route.ts already runs every 5 minutes (vercel.json crons) and drains due schedules — add a pass that loads clients with settings.autopilot === true and calls runAutopilotBatch (src/lib/execution-engine.ts:466) for each, reusing the claim-then-charge loop in updateAutopilotAction (src/lib/actions/settings-actions.ts:45-88) so billable clients are still charged per task. Or (b) relabel the control in src/components/tasks-board.tsx AutopilotToggle (line 98) as a one-shot button — "Run up to 5 pending tasks now" — and stop persisting an on state that nothing honours.
**Also reported as:** "Autopilot on" stays on forever but only ever runs one batch — every task created afterwards sits in Pending.
**Systems touched:** task board autopilot, settings actions, execution engine, cron (run-scheduled), credits

---

## F50 · HIGH · Track B
**Title:** "Refresh Task Map" promises a market-footprint scan; the run never looks outside the account
**Where:** src/components/chatbot-widget.tsx:34 (icon flips to Globe on hasGoogleIntegration); chatbot-widget.tsx:36 (sublabel "Scan market footprint & surface operational priorities"); chatbot-widget.tsx:37-38 (trigger string "Scan the web and analyze our market footprint…", never sent); chatbot-widget.tsx:310 (scan_inbox routes to onRefreshTaskMap(); action.trigger is only used by the other three); src/components/strategy-war-room.tsx:171-174 ("debating your Task Map live … stress-testing against your analytics"); src/lib/agent-swarm.ts:500-506 (buildSwarmContext inputs: client, assets, integrations, benchmarks, custom agents only); agent-swarm.ts:509-524 (gapSummary is connected platforms vs the 14-day calendar); agent-swarm.ts:246-252 (generateObject turn with no tools array at all); +3 further references in verification record
**What you see:** AI Copilot panel → "Refresh Task Map" action → Strategy War Room modal (right rail, every workspace page). The top action reads "Refresh Task Map — Scan market footprint & surface operational priorities", and when Google is connected its icon changes to a globe. Clicking it opens "The Strategy War Room", which says three specialists are debating your task map live and stress-testing it against your analytics. After roughly a minute of scrolling console lines it closes and up to fifteen task cards appear on the board.
**Why wrong:** Nothing looks at the market, the web, the client's own site, or the inbox. The only inputs to the debate are: which connected social channels have nothing scheduled in the next fourteen days, the stored brand tone/visual-style fields, the client's own past engagement scores, and the list of custom agents. The company website is loaded but never passed in. The portal already owns a real web-search tool used elsewhere, so this reads as unfinished rather than a deliberate limit. Two knock-on breaks: (a) the button's own instruction text ("Scan the web and analyze our market footprint…") is dead code — the click handler diverts this one action to the War Room and never sends it; (b) every task this path creates is forced to Karos-managed, so the mandatory onboarding / re-authenticate checklist the prompt calls non-negotiable can never be produced by this button — it only exists on the typed-chat path.
**Fix per doc:** Pick one and do it fully. (a) Honest copy: in src/components/chatbot-widget.tsx change the sublabel at line 36 to what the swarm actually does (e.g. "Rebuild your task map from calendar gaps and past performance"), drop the hasGoogleIntegration/Globe branch at line 34 (keep ListTodo), and delete the unreachable trigger string at lines 37-38 since line 310 never sends it. (b) Make it true: add `web_search: anthropic.tools.webSearch_20250305({ maxUses: 3 })` to the generateObject call in runTurn (src/lib/agent-swarm.ts:246-252) following the existing pattern in src/lib/intel/report.ts:280, and add `client.website` to the SwarmContext returned by buildSwarmContext (src/lib/agent-swarm.ts:563-571) plus the prompt block in buildTurnPrompt. Either way, give the onboarding/re-auth coverage contract a reachable control: today it is only produced by the chat path (src/lib/ai/prompts/proactive-assistant.ts:372-379), and the War Room cannot create client_managed tasks at all because persistSwarmTasks hardcodes owner "karos_managed" (src/lib/agent-swarm.ts:465).
**Systems touched:** AI copilot panel, Strategy War Room / agent swarm, web-search tooling, task creation ownership

---

## F51 · HIGH · Track B
**Title:** A client clicking "New content ready" in their notification bell gets silently bounced to their dashboard
**Where:** src/components/notification-bell.tsx:193; notification-bell.tsx:197; src/app/(app)/jobs/[id]/page.tsx:20; src/lib/auth.ts:341; src/app/(app)/dashboard/page.tsx:16; src/components/client-rail.tsx:210; src/app/(app)/layout.tsx:55; src/components/client-home-overview.tsx:76; +4 further references in verification record
**What you see:** Client shell → notification bell → "New content ready" row (any client page). The bell shows "New content ready: <asset title>" with a clickable sub-line "<Agent> · Pending review · 2h ago". Clicking that sub-line lands the client back on their own dashboard with no explanation and no content. (The bold title line above it is not clickable at all.)
**Why wrong:** The row links to the staff-only job detail screen, which allows admins and employees only and redirects anyone else to the staff dashboard, which in turn redirects a client to their own client page. And these rows are only ever populated for client accounts — staff never get them — so this link is dead 100% of the time it is shown. This is the client's primary "your content is ready" affordance. The neighbouring dashboard code already knows the rule: it carries a comment saying client users cannot open the staff-only jobs screen and routes elsewhere instead.
**Fix per doc:** src/components/notification-bell.tsx:197 — replace `href={`/jobs/${j.jobId}`}` with a client-safe target and make the whole row clickable (currently only the timestamp sub-line at 193-201 is a link). Do NOT copy ClientHomeOverview's `/assets?view=library&status=draft`: src/app/(app)/assets/page.tsx:24 redirects CLIENT_USER to /tasks and drops the query string, so that link is itself a soft dead end. The correct destination is the Workspace archive, which currently has no deep link — so (a) make the tab in src/components/progress-view.tsx:39 initialize from a `view` search param (`useSearchParams`, accept board|activity|archive), then (b) point the bell at `/tasks?view=archive`. Thread a `viewerIsClient` prop from src/components/client-rail.tsx:210 and src/components/app-header.tsx:19 so staff keep the job link. Leave the transcript rows alone — src/app/(app)/transcripts/[id]/page.tsx:15 calls requireUser() with no role list, so that link is not gated.
**Also reported as:** A client's "New content ready" notification links to a staff-only page and bounces them back to their own dashboard.
**Systems touched:** notification bell, routing/auth guards, workspace archive deep link (progress-view)

---

## F53 · HIGH · Track A
**Screenshot:** screenshots/F053.png
**Title:** A one-time 100-credit charge is sold to the client as "≈ $29/mo"
**Where:** src/components/linkedin-seats-workspace.tsx:126 ("adding it costs {seatCost} credits (≈ $29/mo)"); src/lib/credits.ts:122 (same "(≈ $29/mo)" inside blocked-state reason string); src/lib/credits.ts:60 (source comment that seeded it: "(~$29/mo equivalent). Charged once per seat added over the limit."); src/lib/actions/seat-actions.ts:87 (the only seat_purchase charge in the repo, fired once inside addEmployeeSeatAction); src/components/onboarding-socials-step.tsx:52 (onboarding embeds IntegrationsTab, which renders the same seat workspace (src/components/integrations-tab.tsx:482), so the string appears there too)
**What you see:** Client settings /clients/[id]/settings — add-seat form and seat-limit blocker; the same component inside the onboarding wizard. "This is beyond your 2-seat plan - adding it costs 100 credits (≈ $29/mo)." The button underneath says "Add & charge 100 credits". The blocked state says "Adding another employee seat costs 100 credits (≈ $29/mo) - top up credits or upgrade your plan to continue."
**Why wrong:** There is no recurring seat billing anywhere in the product. The seat charge happens exactly once, at the moment the seat is added, and no scheduled job ever charges it again. Presenting a single 100-credit debit as a monthly rate misstates the price in both directions: the client either expects to be billed again and isn't, or believes 100 credits buys a month of a seat when it buys the seat outright. It is a pricing claim the product cannot honour, shown at the exact moment the client decides to spend.
**Fix per doc:** Delete "≈ $29/mo" from both strings — src/components/linkedin-seats-workspace.tsx:126 and the `reason` template in src/lib/credits.ts:122 — and state the charge as it behaves: "Adding a 3rd seat is a one-time 100-credit charge." Update the stale comment at credits.ts:58-62 at the same time. If a recurring seat fee is actually intended, that is a separate feature; do not describe it in the UI until a re-charge path exists. Note src/lib/__tests__/seat-architecture.test.ts asserts on evaluateSeatAddition, so re-run it after editing the reason string.
**Systems touched:** credits/pricing copy, LinkedIn seats, onboarding wizard, tests

---

## F54 · HIGH · Track A
**Screenshot:** screenshots/F054.png
**Title:** A task moved to "Review Pending" from the ticket vanishes from the board with no way back
**Where:** src/components/task-ticket-modal.tsx:35; task-ticket-modal.tsx:523; task-ticket-modal.tsx:703; task-ticket-modal.tsx:724; src/components/tasks-board.tsx:597; tasks-board.tsx:470; src/components/progress-view.tsx:74
**What you see:** Workspace → Board → "Depending on you" tab → open an In Progress task → footer button. On the "Depending on you" tab you open a task that is In Progress. The ticket footer offers "Move to Review Pending". You click it, the ticket closes, and the card is gone from the board. The tab counter still counts it, but there is no column, no filter setting and no drag target that can bring it back.
**Why wrong:** That tab deliberately shows only three columns because client-owned work is never supposed to sit in Review Pending, yet the ticket footer puts it there in one click. The card becomes unreachable: the status dropdown can filter to Review Pending but the column still is not rendered, so the count and the cards on screen disagree and it reads as if the work was deleted. It hits both the client's own Workspace and the staff Task Board, since both render the same board component.
**Fix per doc:** In src/components/task-ticket-modal.tsx stop reading the next status from the flat STATUS_NEXT map (line 33): when inferOwner(task) === "client_managed" (the helper already exists at line 69, and isClientManaged is already computed at line 526), in_progress must advance to "completed", skipping review_pending. Belt and braces: in src/lib/actions/task-actions.ts updateTaskStatusAction (line 70) reject status === "review_pending" when inferOwnerEngine(task) === "client_managed", and in src/components/tasks-board.tsx tasksByColumn (line 602) fold any client_managed review_pending task into the pending bucket while activeTab === "client" so already-stuck tasks resurface.
**Systems touched:** task board, task ticket modal, task-actions status machine

---

## F55 · HIGH · Track B
**Screenshot:** screenshots/F055.png
**Title:** Branded "Connect with Instagram" buttons open a popup showing a bare error sentence, and only admins are told why
**Where:** src/components/integrations-tab.tsx:149-152 (comment: hasOAuthSupport is "Decoupled from isOAuthEnabled (env-var check) so all users can see the Connect button"); integrations-tab.tsx:306-310 (explanatory hint wrapped in `isAdmin && hasOAuthSupport && !isOAuthEnabled`); integrations-tab.tsx:652-657 (same hint for the Google card, also isAdmin-only); integrations-tab.tsx:735 (`const isAdmin = currentUserRole === "KAROS_ADMIN"` — employees are excluded too); src/app/api/auth/social/[provider]/route.ts:22,24,26,30,35 (five bare `new Response("Unauthorized"|"Missing clientId"|"Forbidden"|"Unknown provider"|"OAuth not configured for this provider")` returns); src/app/api/auth/social/[provider]/callback/route.ts:35-38 (errorPage(), the postMessage HTML shell that DOES notify the opener); integrations-tab.tsx:762-773 (listener only handles karos_oauth_success / karos_oauth_error); integrations-tab.tsx:799-806 (popup-closed interval silently clears the "Connecting…" state with no error)
**What you see:** Client Settings → Integrations (/clients/[id]/settings), also the onboarding socials step. A client or employee clicks the full-colour "Connect with Instagram" (or Connect Google Suite) button. A popup opens containing one plain sentence on a blank white page: "OAuth not configured for this provider". The card behind it sits on "Connecting…" until the popup is closed by hand, then quietly reverts. No message ever appears in the app.
**Why wrong:** The Connect button is deliberately shown to everyone regardless of whether the server is configured, but the one line that explains the failure — "OAuth env vars not set. The button above will fail until configured." — is shown only to admins, so employees and clients never see it. And the authorization route answers its four failure cases with bare text bodies, while the app only listens for the structured success/failure messages the callback page sends. The failure therefore never reaches the app at all. Non-admins get a beautiful button whose entire behaviour is a blank popup.
**Fix per doc:** src/app/api/auth/social/[provider]/route.ts: replace the five bare `new Response(...)` returns at lines 22, 24, 26, 30 and 35 with the same postMessage HTML shell the callback uses — export errorPage()/htmlPage() from src/app/api/auth/social/[provider]/callback/route.ts (lines 18-38) into a shared module and call it, so the parent's karos_oauth_error listener fires and popupError renders at integrations-tab.tsx:844-848. Then in src/components/integrations-tab.tsx either drop the `isAdmin` condition on the two hints (lines 306, 652) behind client-safe wording ("This channel isn't connectable yet — ask your Karos team"), or disable the Connect button when !isOAuthEnabled with that sentence as its title.
**Systems touched:** OAuth/social integrations, integrations tab, onboarding socials step

---

## F56 · HIGH · Track B
**Title:** Every client user can hand out permanent workspace access, and the key can never be rotated from the UI
**Where:** src/app/(app)/clients/[id]/settings/page.tsx:199 (card gated only on `client.clientKeyId`; no role or group-admin check); src/components/client-key-inline.tsx:26 (renders the key verbatim, with a copy button (28) and share instruction (37)); src/lib/auth.ts:121 (a valid client key auto-approves the signup: disabled:false, approvedAt set (auth.ts:132-133)); src/lib/actions/client-actions.ts:104 (regenerateClientKeyAction exists and works); src/components/client-key-panel.tsx:9 (ClientKeyPanel (with its confirm at line 29) is imported nowhere; `grep -rn ClientKeyPanel src/` returns only this file); src/app/(app)/team/page.tsx:36 (comment noting clientKeyId "is enough to join that client's workspace")
**What you see:** Client settings /clients/[id]/settings — "Invite your team" card. Any signed-in client user — not just their group admin — sees their workspace's invite key in plain text with a copy button and the line "Share this key with a teammate so they can join your workspace." Nowhere in the product is there a button to change that key.
**Why wrong:** That key auto-approves any new signup straight into the workspace as a linked client user, with no admin review — full access to that client's documents, assets, agents and credits. Once it leaks (pasted into a Slack channel, forwarded, screenshotted) there is no remediation path on screen: a regenerate action exists on the server and a panel component to drive it exists, but that panel is imported by no page — its only reference in the repo is its own file. So the rotate control is dead code and the key is effectively permanent. The Team page even documents this exact hazard about handing the key to a client component, and then the settings page shows it to every client user anyway.
**Fix per doc:** In src/app/(app)/clients/[id]/settings/page.tsx:199-209, gate the whole card on `isStaff || user.isGroupAdmin` (both are already in scope on that page) so ordinary client users cannot mint teammates, and render a rotate control beside the key — either swap ClientKeyInline for the existing ClientKeyPanel (src/components/client-key-panel.tsx), or add a button wired to regenerateClientKeyAction (src/lib/actions/client-actions.ts:104) behind the same staff/group-admin gate. If ClientKeyPanel is not the intended surface, delete the file rather than leaving a working rotate control unreachable.
**Systems touched:** auth/signup keys, client settings, team access security

---

## F57 · HIGH · Track A
**Title:** The AI Execution Guide shows clients raw markup — "## Overview", "**Task**" and all
**Where:** src/lib/ai/prompts/proactive-assistant.ts:726; src/components/task-ticket-modal.tsx:366; task-ticket-modal.tsx:685; src/lib/doc-render.ts:126; src/components/client-documents.tsx:341
**What you see:** Workspace → Board → open a task you own → "AI Execution Guide". You click "Generate Plan" and get an unstyled wall of text with literal heading marks and asterisks in it — "## Overview", "## Prerequisites", "## Step-by-Step Execution" — instead of headings and bullets.
**Why wrong:** The plan is rendered as pre-formatted text, so the model's markup is shown verbatim. The prompt explicitly orders that exact heading structure, so this happens on every single generation, not in an edge case. It is client-facing (the guide only renders for client-owned tasks) and breaks the standing rule against putting raw model copy in front of a client — and the portal already ships the renderer for exactly this, used by the client context documents.
**Fix per doc:** In src/components/task-ticket-modal.tsx AiPlanSection (line 320) replace the `<pre>` at line 366 with the existing renderer: renderFullDoc(plan) from @/lib/doc-render via dangerouslySetInnerHTML, copying the pattern at src/components/client-documents.tsx:341. Wrap the result in a collapsible summary so the guide does not push the comment box below the fold. Leave the Generated Deliverable block (line 122) on `<pre>` unless the deliverable is known to be markdown — a social caption's line breaks are load-bearing and a markdown pass would reflow them; if you do convert it, gate on the artifact containing markdown headings.
**Systems touched:** task ticket modal, doc renderer, AI execution guide

---

## F58 · HIGH · Track B
**Title:** The Autopilot switch spends the client's credits and fires five agent runs with no label, no price and no confirmation
**Where:** src/lib/actions/settings-actions.ts:45-46 (enabling as a billable client actor pulls up to 10 pending tasks); settings-actions.ts:47 (filters to karos_managed and `.slice(0, 5)`); settings-actions.ts:58-60 (chargeClientCredits per task with `await plannedTaskExecutionCost(claimed)`); settings-actions.ts:87 (dispatches the charged batch via runClaimedTasks); src/components/tasks-board.tsx:110-121 (toggle() calls updateAutopilotAction immediately, no confirm step); tasks-board.tsx:125-147 (whole control: a switch and the words "Autopilot on"/"Autopilot off", plus an error line); src/components/custom-agents.tsx:550 (comparison case: "{cost} credits per output" printed next to Run now)
**What you see:** Workspace → Board → "Automated" tab (/tasks) → Autopilot toggle. A bare toggle labelled "Autopilot off". No tooltip, no helper text, no price, no confirmation step. One click charges the client for up to five task executions and starts five agent runs. The only feedback is a red line if the charge is refused.
**Why wrong:** Turning the switch on claims up to five pending automated tasks, charges the planned execution cost for each one, and dispatches them — all on the first click. Everywhere else in the portal a spend is announced before it happens: the run dialog prints "N credits per output" right beside the Run button, and the schedule modal shows an estimated weekly cost. This is the largest single-click spend in the product and the only one with no price attached to it.
**Fix per doc:** src/components/tasks-board.tsx AutopilotToggle (line 98): add helper text under the switch stating that turning it on immediately runs up to five pending automated tasks and charges credits for each, and gate the enable path behind a confirm step showing the credit total and the eligible task count. Source the total from a small read-only server action wrapping plannedTaskExecutionCost (src/lib/execution-engine.ts, already imported by settings-actions.ts) over the same batch selection used at settings-actions.ts:46-47, the way the schedule modal surfaces estimated weekly cost. Keep disabling instant and unconfirmed.
**Systems touched:** autopilot, credits, task board, execution engine

---

## F149 · HIGH · Track B
**Title:** Nothing marks a post as posted, so the archive shows everything the moment it is generated
**Where:** src/components/archive-view.tsx (no status filter on the client archive); src/lib/asset-lifecycle.ts:25-30 (published/scheduled semantics already exist); asset-detail-modal (the "Already posted it?" control already writes the confirmation); Team decision, portal review call 27 Jul 2026
**What you see:** Workspace → Archive; Calendar → day detail. Every generated deliverable appears in the archive immediately, badged "draft". The call decided the opposite flow: the calendar day card gets "Mark as posted"; the archive lists only what was actually posted (about the last 30 days); upcoming days show template slots without exposing pre-generated content — explicitly to avoid the churn risk of a client saying "just give me all the posts".
**Why wrong:** The schema already models it — assets have scheduled/approved/published states and the detail panel even asks "Already posted it?" — but the archive never filters on any of it. So the portal reveals the whole pre-generated batch, which is both the approval-promise break (finding 47) and the exact churn scenario the team named.
**Fix per doc:** Filter the client archive to posted content (status published, or the client's "Already posted it?" confirmation) and age it out after ~30 days; add "Mark as posted" to the calendar day card wired to the same status; keep drafts on the staff side only. Cross-ref finding 47 for the raw-markup half of the archive problem.
**Systems touched:** archive view, asset lifecycle/status, calendar day card

---

## F150 · HIGH · Track B
**Title:** Video deliverables have no path into the portal — clips are hand-delivered by email
**Where:** src/lib/types.ts:545 (kind: "image" | "document" | "text" | "other" — no video); src/components/task-ticket-modal.tsx:113 (the only `<video>` element in the app); src/lib/types.ts:388-396 (mimeType documents "video/mp4" for the download path only); Team decision, portal review call 27 Jul 2026: GCS block storage, agent service fetches from it
**What you see:** Assets / Archive / Calendar — any video deliverable (podcast clips, branded shorts, TikTok). Podcast clips for a real client are delivered as a daily email from the Hello account because the portal cannot hold them; the produced clips live on a laptop. On the call: "It works — it's just we don't have the system to show the videos on the portal… we will use GCP block storage, and then the agent service could ask for the data."
**Why wrong:** Asset.kind has no video variant ("image" | "document" | "text" | "other"), the asset detail panel renders text and images only, and the single video player in the codebase is inside the task-ticket modal. Meanwhile three of the seven agents on the client's page (Branded Shorts, TikTok, Instagram clips lane) exist to produce video. The mimeType field already anticipates "video/mp4" for downloads — ingestion and playback are the missing halves.
**Fix per doc:** Add kind "video" with a GCS-backed upload/URL field per the call decision; render a `<video controls>` player in the asset detail panel (lift the task-ticket player); show clips on calendar/archive like posts, with the existing copy-caption button beside them. Then the email workaround can retire.
**Systems touched:** asset types/schema, GCS storage, asset detail panel, calendar/archive, agent service

---

## F60 · MEDIUM · Track A
**Screenshot:** screenshots/F060.png
**Title:** "View as client" mode is invisible — real impersonation gets a banner, the client switcher gets nothing
**Where:** src/app/(app)/layout.tsx:147 (banner renders only for real impersonation (isImpersonating && realAdmin)); src/components/app-header.tsx:16 (staff header holds support, theme and notifications only; no context indicator); src/components/sidebar.tsx:100 (picker label is "View as client", with an Eye icon at 98); sidebar.tsx:103 (exit is an unlabelled 12px X inside the picker button); src/components/team-manager.tsx:154 (impersonation control also labelled "View as" with the same Eye icon (153)); src/components/impersonation-banner.tsx:29 (real-impersonation bar, with a labelled "Exit impersonation" button (47)); src/app/(app)/layout.tsx:162 (StaffCopilotDock appears purely from context (src/components/staff-chatbot-widget.tsx:15), with no accompanying indicator)
**What you see:** Staff workspace shell (AppHeader) while a client context is active. An admin picks a client from the eye row at the bottom of the rail (or just opens a client page). The nav becomes four client tabs and a client copilot panel slides in on the right. The top of the screen says nothing. The only hint is the client's name inside that eye row, at the very bottom of the rail, below the fold on a short window.
**Why wrong:** There are two separate features both labelled "View as" with the same eye icon: real impersonation, which swaps the session to the target user and does get a persistent bar with a labelled exit, and this client-context switcher, which changes what the admin sees but leaves them acting as staff — full visibility, staff-only controls live, runs free. Nothing on screen distinguishes the two, so an admin cannot tell whether what they are looking at is what the client sees, or whether their next click spends the client's credits. Exiting differs too: a labelled exit button versus a tiny unlabelled X nested inside another button.
**Fix per doc:** Give client-context mode its own persistent bar. Add a small client component beside AppHeader in src/app/(app)/layout.tsx:150 (it must be a client component to read useActiveClient) modelled on src/components/impersonation-banner.tsx but distinctly worded — "Client context: Acme · staff view, full visibility, runs are free" — with a labelled "Exit client view" button calling setActiveClient(null) and routing to /clients (same body as clearClient in sidebar.tsx:79-85). Then rename the two features so they are not both "View as": sidebar.tsx:100 → "Client context", team-manager.tsx:154 → "Sign in as".
**Systems touched:** staff shell/layout, impersonation vs client-context UX, sidebar

---

## F61 · MEDIUM · Track B
**Title:** A task the platform then refuses still costs the client a credit
**Where:** src/lib/actions/task-actions.ts:328-329 (chargeTaskAssist runs before anything is validated); task-actions.ts:43-59 (helper charges CREDIT_COSTS.taskAssist and returns only a denial message); task-actions.ts:367-372 (capacity refusal, early return, no refund); task-actions.ts:376-379 (duplicate refusal, early return, no refund); src/lib/credits.ts:51 (taskAssist = 1 credit); credits.ts:287-315 (applyCredit with kind "refund" hands back window spend; the doc comment names the failed-run case); src/lib/data.ts:2056-2062 (creditClientCredits, the transactional refund entry point); src/components/chatbot-widget.tsx:277-292 (both refusals render in the red danger style); +1 further reference in verification record
**What you see:** AI Copilot panel → "Describe a task you need done… / Add" (and the same bar on the task board). Type a task, press Add, wait a couple of seconds, and get a red bar saying either that a similar task already exists on your board, or that the Karos AI queue is at capacity with fifteen active tasks. No task is created — and the credit balance in the left rail has gone down by one.
**Why wrong:** The charge is taken before both the capacity check and the duplicate check, and neither refusal path hands it back. The platform already has a refund mechanism, explicitly built so a failed run does not eat the weekly allowance, so charging for a refused write is an oversight rather than policy. It is also the one place a client can watch a credit disappear for nothing — and the duplicate case is dressed as an error when it is really information.
**Fix per doc:** In src/lib/actions/task-actions.ts, on both early returns (lines 367-372 and 376-379) call creditClientCredits from @/lib/data (src/lib/data.ts:2062) with amount CREDIT_COSTS.taskAssist, kind "refund", operation "task_execution" and chargedAt set to the charge time, guarded by isBillableClientActor. Moving chargeTaskAssist below the checks is not equivalent — the Haiku routing call at line 346 has already cost real money by then and the title it produces is what the duplicate check needs — so refund, do not reorder. In the panel, split the two cases in src/components/chatbot-widget.tsx:277-292 so a duplicate renders as a neutral info pill naming the existing task rather than a red error; apply the same split at src/components/quick-add-task-bar.tsx:94-108.
**Systems touched:** credits (charge/refund), task-actions, copilot panel, quick-add bar

---

## F62 · MEDIUM · Track B
**Screenshot:** screenshots/F062.png
**Title:** Adding a competitor from the sidebar does nothing visible when you are not on a client page
**Where:** src/components/client-context-sections.tsx:89; client-context-sections.tsx:92; src/components/sidebar.tsx:394; sidebar.tsx:356; src/lib/active-client-context.tsx:42; active-client-context.tsx:56; src/app/(app)/clients/[id]/layout.tsx:36; src/lib/actions/competitor-actions.ts:478; +6 further references in verification record
**What you see:** Staff sidebar → COMPETITOR TRACK "+" quick-add, with a client selected but while on Dashboard, Settings, Assets, a job page or a meeting page. With a client selected you click the plus next to COMPETITOR TRACK, type a name and hit Add. The spinner runs, "Analyzing competitor profile…" appears, the box closes with no error — and the list is unchanged. The competitor only appears once you navigate into that client's own pages.
**Why wrong:** The row is created and a model analysis of the whole tracked list is run and logged as usage, so retrying burns more spend each time and the person cannot tell whether the feature works. The staff sidebar reads its competitor list purely from in-browser context, and the only thing that refills that context lives in the client-page layout. On any other route that layout is not in the tree, so the refresh at the end of the add can never bring the new row back.
**Fix per doc:** Stop feeding the sidebar list from route-scoped context. Preferred: change addCompetitorByNameAction (src/lib/actions/competitor-actions.ts:461) to return the { company, created } it already computes from upsertManualCompetitor, and have CompetitorTrack (src/components/client-context-sections.tsx:80) append the row to local state on success — the same optimistic pattern handleRemove already uses with removedIds (line 54). Alternative: fetch competitors for the active client in src/app/(app)/layout.tsx and pass them into Sidebar as props so revalidatePath + router.refresh() actually re-render them. Either way, widen ClientContextSync's dependency list (src/lib/active-client-context.tsx:56) beyond competitors.length so a report-to-manual promotion, which leaves the count unchanged, still re-syncs.
**Also reported as:** Adding a 6th competitor silently hides one you already track; re-adding an existing one says nothing.
**Systems touched:** competitor track, sidebar, active-client context, competitor actions, AI usage/spend

---

## F63 · MEDIUM · Track A
**Screenshot:** screenshots/F063.png
**Title:** Broken icon names render a sparkle glyph across 24 files — including beside the red error message in Correct Info and on the Tasks nav item
**Where:** src/components/icon.tsx:10 (`icons[name] ?? icons.Sparkles`); node_modules/lucide-react 1.21.0 (exported icons map has 1737 keys and contains none of AlertCircle, AlertTriangle, CheckCircle, CheckCircle2, CheckSquare, Loader2, BarChart2, Wand2); src/components/correct-info-modal.tsx:120 (name="AlertCircle" beside the error text); src/components/sidebar.tsx:35 (Tasks nav item icon "CheckSquare"); src/components/onboarding-wizard.tsx:205 (name="CheckCircle"); src/components/notification-bell.tsx:146 (name="CheckCircle2"); src/components/tasks-board.tsx:624 (name="CheckSquare" in the empty state)
**What you see:** Correct Info error box; left nav Tasks row; onboarding wizard success ticks; task board; notification list; and 18 other files. A correction fails and the red error box shows a decorative sparkle next to the failure text. In the left nav, Tasks has a sparkle where every other item has a real icon. The onboarding wizard's completion ticks, the notification list's success marks and the task board's empty state are all sparkles too.
**Why wrong:** The shared icon component looks names up in the icon library's name map and quietly falls back to a sparkle for anything unknown. The installed library version (1.21.0) renamed a batch of icons and its name map contains only the new canonical names — the old names survive as module exports but are absent from the map the component uses. So every call site still using an old name renders a sparkle: 37 of them across 24 files. A cheerful sparkle attached to an error message, and an indistinguishable sparkle where a completion tick belongs, are the worst possible substitutions.
**Fix per doc:** Rename at all 37 call sites (24 files, findable with a grep for the eight dead names): AlertCircle→CircleAlert, AlertTriangle→TriangleAlert, CheckCircle and CheckCircle2→CircleCheck, CheckSquare→SquareCheck, Loader2→LoaderCircle, BarChart2→ChartNoAxesColumn, Wand2→WandSparkles. Then make the fallback in src/components/icon.tsx:10 loud in development (console.warn when a name misses) so the next library rename cannot fail silently.
**Systems touched:** icon component, lucide-react, 24 files of call sites

---

## F64 · MEDIUM · Track A
**Screenshot:** screenshots/F064.png
**Title:** Clicking a task notification drops you on a board tab that does not contain that task
**Where:** src/components/notification-bell.tsx:292; notification-bell.tsx:314; src/components/tasks-board.tsx:439; tasks-board.tsx:486
**What you see:** Notifications bell → any "Pending tasks" or "Ready for review" row → Workspace. The notification row has an arrow implying it takes you to the task. It goes to the board with no task identity, and the board always opens on the "Automated" tab. If the item you clicked is one of yours, you land on a tab that does not contain it — nothing on screen is the thing you just clicked.
**Why wrong:** The click-through target is wrong for a whole class of notifications, so the bell reads as broken — and it is worst for exactly the items the bell exists to chase, the ones the client has to do themselves.
**Fix per doc:** Link to `/tasks?task=<id>` from TaskAlertRow (src/components/notification-bell.tsx:286) and in TasksBoard (src/components/tasks-board.tsx:430) read that param with useSearchParams to (a) set activeTab from inferOwner of the matching task (helper at line 65) instead of the hardcoded "karos" default at line 439, and (b) set selectedTaskId so TaskTicketModal opens on mount. Cheap interim: derive the tab in the notification row and link to `/tasks?tab=client`, with TasksBoard honouring a tab param, so the person at least lands where the card is.
**Systems touched:** notification bell, task board, deep linking

---

## F65 · MEDIUM · Track A
**Screenshot:** screenshots/F065.png
**Title:** Task-creating actions never hand you off to the board where the tasks landed
**Where:** src/components/chatbot-widget.tsx:220-226 (success feedback is a static label, taskId is discarded); chatbot-widget.tsx:277-292 (confirmation pill: icon + message only); src/components/strategy-war-room.tsx:195-200 (done footer is text only); strategy-war-room.tsx:138-144 (auto-closes 1.6s after done); src/lib/actions/task-actions.ts:346-355 (Haiku rewrites title/description and picks the owner); task-actions.ts:382-397 (createClientTask returns the id; the action returns taskId and owner but not the title); src/app/api/clients/[id]/chat/route.ts:610 ("Created N tasks in your task board" string the model paraphrases); grep for Link|href in chatbot-widget.tsx, strategy-war-room.tsx, copilot-dock.tsx — zero matches
**What you see:** AI Copilot panel → the quick-add task box, the War Room completion state, and Content Dispatch. Type a task and press Add: a small green pill says "AI-managed task added" and that is the end of it. Finish a War Room run: a green bar says "Consensus reached - 7 tasks locked into your map", then the window closes itself. Run a Content Dispatch: the assistant's closing line is prose along the lines of "Created 5 tasks in your task board." In all three cases there is nothing to click, and you may be sitting on the dashboard, the assets page or a document, none of which show tasks.
**Why wrong:** The panel is pinned to every workspace page, so it creates work from contexts that never show the result. None of the three components involved contain a single link. The quick-add is the worst of the three: the description is rewritten by a model and the owner is chosen for you, so the card that exists may not carry the words the client typed — and the confirmation shows neither the resulting title nor a way to look at it.
**Fix per doc:** Return the generated title from ingestCustomUserTaskAction (src/lib/actions/task-actions.ts:311 return type and line 397 — it already returns taskId and owner, add `title: parsed.title`). Then in src/components/chatbot-widget.tsx:277-292 render the confirmation as `Added "<title>"` plus a next/link to /tasks. Apply the identical change to the duplicate bar at src/components/quick-add-task-bar.tsx:33-41 for consistency (there the board is already on screen, so a link is optional). Add a "View task map" link to the War Room done footer at src/components/strategy-war-room.tsx:195-200 and remove the 1.6s auto-close at line 142 so the link is reachable. A ?task=<id> deep link is not supported today — src/app/(app)/tasks/page.tsx reads no searchParams — so link to /tasks unless you also add that.
**Systems touched:** copilot panel, strategy war room, quick-add bar, task actions

---

## F66 · MEDIUM · Track A
**Title:** The client's Archive is every deliverable they have ever received in one wall, with no filter, search, collapse or paging
**Where:** src/components/archive-view.tsx:74-88; src/components/assets-view.tsx:69-91; src/app/(app)/tasks/tasks-body.tsx:59; src/lib/data.ts:503-513; src/components/seo-geo/gap-list.tsx:143-152
**What you see:** Client Workspace → Archive tab (/tasks). An agent name, then every tile that agent has ever produced, then the next agent, and so on. A client on three posts a week for a year sees roughly 150 tiles in one continuous scroll. There is no status filter, no date range, no search box, no way to fold up an agent group, and no show-more — while staff looking at the same deliverables get a status dropdown and a channel dropdown.
**Why wrong:** The client view maps every group and every item with no limit and no controls. The audience with the least context gets the least navigable surface. It also compounds the first finding: with no filter and no address state, nothing in the portal can send a client to "my drafts".
**Fix per doc:** Give ArchiveView the same control bar AssetsView has: add `status`, `agent` and a text `search` over `asset.title`, rendered in the identical `flex flex-wrap items-center gap-2 rounded-lg border border-border bg-surface-2 p-2` strip from src/components/assets-view.tsx:69-91, and seed `status` from the URL so finding 1's deep link can target drafts. Cap each agent group at 12 tiles behind a "Show all N" button reusing the collapse pattern at src/components/seo-geo/gap-list.tsx:143-152, and make the group heading at archive-view.tsx:77-81 a button that folds its grid.
**Systems touched:** archive view, assets view patterns, deep-linkable filters

---

## F67 · MEDIUM · Track B
**Title:** The resume a client uploads is never read — but the UI says it powers their voice
**Where:** src/components/settings-form.tsx:188 ("Used to write LinkedIn advocacy content in your authentic voice."); src/components/resume-uploader.tsx:88 ("Powers your LinkedIn advocacy voice"); src/app/api/users/resume/route.ts:42 (stores resumeUrl on the user; line 44 mirrors only the URL onto the seat); src/lib/execution-engine.ts:372 (resumeText is sourced from seat.backgroundContext, not from the uploaded file); src/lib/actions/seat-actions.ts:104 (backgroundContext is only ever written from an action input no UI supplies (grep: no caller passes it)); src/lib/ai/prompts/proactive-assistant.ts:636 (fallback branch: "Background/resume reference on file: {url}"); src/lib/execution-engine.ts:397 (generateText called with a prompt and no tools, so the URL cannot be fetched)
**What you see:** Account settings /settings — Profile tab, "Resume / CV" card (and the same uploader in the onboarding wizard). A drag-and-drop uploader captioned "Used to write LinkedIn advocacy content in your authentic voice", and once uploaded, "Powers your LinkedIn advocacy voice". The user uploads their CV and reasonably expects the agent to write like them. It does not.
**Why wrong:** The file's contents never reach a model. The upload endpoint stores the file and mirrors only its web address onto the employee seat; the advocacy prompt then pastes that address in as a bare line of text, and the generation call is issued with a prompt and no tools, so the model has no way to open or read it. The field the prompt actually uses to shape the voice is a raw-text background field, which no screen in the product ever fills in. The upload therefore does nothing except add an unusable link to a prompt.
**Fix per doc:** Extract text at upload time: in src/app/api/users/resume/route.ts, after uploadBytes at line 40, parse the PDF/DOCX/TXT to plain text and write it to the seat's `backgroundContext` via the existing updateEmployeeSeat call at line 44 (alongside resumeUrl) — that is the field src/lib/ai/prompts/proactive-assistant.ts:634 actually reads, mapped at src/lib/execution-engine.ts:372. Until that ships, change both captions (src/components/settings-form.tsx:188 and src/components/resume-uploader.tsx:88) to something true — "Stored for your Karos team" — and drop the dead URL branch at proactive-assistant.ts:635-636 so the model is not fed a link it cannot open (src/lib/__tests__/artifact-prompt.test.ts:49 asserts on that branch and will need updating).
**Systems touched:** resume upload, employee seats, LinkedIn advocacy prompts, execution engine, tests

---

## F68 · MEDIUM · Track B
**Screenshot:** screenshots/F068.png
**Title:** The staff notification bell can never show a review or a task
**Where:** src/app/(app)/layout.tsx:55; layout.tsx:58; layout.tsx:150; src/components/notification-bell.tsx:50; notification-bell.tsx:258; src/lib/data.ts:1641; src/lib/data.ts:1664
**What you see:** Staff workspace → header bell (all pages). As an admin or employee the bell only ever lists meeting action items assigned to you. "Ready for review" and "Pending tasks" never appear no matter how full the review queue is, the panel says "All caught up!" when you have no meeting items, and the footer always offers "View all meetings" instead of the task board.
**Why wrong:** The workspace layout hands the bell empty lists for reviews and tasks unless the viewer is a client user, so two of its three feeds are structurally unreachable for the people who actually run the agency — and the empty state then asserts nothing needs attention while drafts sit in review.
**Fix per doc:** In src/app/(app)/layout.tsx populate both feeds for staff before rendering AppHeader (line 150): scope with listClients() for admins and listClients({ employeeId: user.uid }) for employees, then aggregate — listClientTasks already takes an optional clientId (src/lib/data.ts:1664-1670), so one cross-client call with a status filter works today, and the annotate-with-client-name pattern to copy is in src/app/(app)/tasks/tasks-body.tsx:92-104. listReviewJobs (src/lib/data.ts:1641) takes a single clientId, so either add an ids variant or map over the scoped clients. Cap the lists and show the client name on each row (ClientTask carries clientId).
**Systems touched:** notification bell, workspace layout, data layer (listReviewJobs/listClientTasks)

---

## F69 · MEDIUM · Track A
**Title:** When generation is running or has failed, the Documents group tells the client the wrong story — and "our team has been notified" is not true
**Where:** src/components/client-documents.tsx:764-767 (single, unconditional empty-state line); client-documents.tsx:749 (isAiProcessing used only to disable Regenerate); src/components/ai-processing-banner.tsx:40-41 ("Our team has been notified and is on it"); src/lib/data.ts:281-290 (releaseAiProcessingLock writes only the aiProcessingError field); src/app/(app)/clients/[id]/page.tsx:84 and src/app/(app)/clients/[id]/settings/page.tsx:96 (the only staff-visible readers of that field)
**What you see:** Documents nav group empty state (client portal rail) plus the workspace banner shown after a failed run. A client who finished onboarding half an hour ago sees, under Documents, that their brand and strategy documents will appear once onboarding completes — implying they still have setup to do. If the run failed, that same line persists, alongside a banner saying we hit a snag and our team has been notified and is on it.
**Why wrong:** The documents list already receives the flag telling it a generation cycle is in flight, but only uses it to disable the admin Regenerate button — the empty state never mentions that generation is running, and never distinguishes running from failed from not-started. And nothing is notified: releasing the processing lock only writes an error string onto the client record. No email, no activity entry, no staff notification. The only reader of that field is the banner itself, which a staff member sees solely by opening that one client's dashboard or settings page. The reassurance is copy over a missing code path.
**Fix per doc:** Two parts. (1) In src/components/client-documents.tsx:764, branch the empty state on state the component already has: isAiProcessing → "Karos Agents are writing your documents now — this takes a few minutes."; otherwise the existing onboarding line. Pass the client's aiProcessingError down (both mount points, src/components/sidebar.tsx:385 and src/components/client-rail.tsx:138, already hold the client record) for a third branch: "Generation stopped early. Your Karos team is on it." (2) Make the reassurance true: in the failure path of releaseAiProcessingLock (src/lib/data.ts:281) — or at each of its callers, which already have the failure string — write a logActivity entry and notify the assigned staff via the existing Resend mailer, or surface an error badge on the /clients list.
**Systems touched:** client documents, AI processing banner/lock, notifications (Resend), activity log

---

## F70 · MEDIUM · Track A
**Title:** X drafts are titled with lab-internal vocabulary — clients read "Avenue 3 · News-reaction (live)" (and LinkedIn drafts read "Post 2 · POV thread")
**Where:** src/components/x-drafts-review.tsx:137; src/components/li-drafts-review.tsx:153; src/lib/x-drafts.ts:106; x-drafts.ts:23; src/lib/li-drafts.ts:72; src/components/asset-card.tsx:752; docs/x-agent-portal.md:103; docs/x-agent-portal.md:48
**What you see:** Assets / Workspace archive → X and LinkedIn drafts batch cards (asset detail and archive). Each draft block is headed by whatever the agent wrote in its own document: "Avenue 1 · Build-in-public", "Avenue 3 · News-reaction (live)", "Avenue 4 · Quote-comment". On the LinkedIn reader the same pattern appears as "Post 1 · knowledge/explainer", "Post 2 · POV thread".
**Why wrong:** The heading text is passed through verbatim from the agent's deliverable into the card title. "Avenue", the lane slugs, and the "(live)" freshness flag are production vocabulary from the agent contract, not language a client uses — nobody outside the lab knows what an Avenue or a POV single is. This is raw model-authored copy rendered straight to a client, which is exactly what the house copy rule forbids, and it is the first thing a client reads on every draft.
**Fix per doc:** Humanize at the render boundary, not in the parsers (the raw string is also used as the draftRef sent to the feedback actions — x-drafts-review.tsx:77 and li-drafts-review.tsx:99 — so those must keep the original text). Add a shared helper, e.g. src/lib/draft-lane-label.ts, exporting `laneLabel(heading: string): string` that strips a leading "Avenue N · " / "Post N · " / "Draft N · " prefix, looks the remainder up case-insensitively in a map (build-in-public → "Building in public", knowledge/explainer → "Explainer", news-reaction → "Reacting to the news", quote-comment → "Quote reply", reply → "Reply", pov single → "Your point of view", pov thread → "Your point of view (thread)") and sentence-cases anything unmapped. Then use it at src/components/x-drafts-review.tsx:137 (`{laneLabel(draft.avenue)}`) and src/components/li-drafts-review.tsx:153 (`{laneLabel(draft.lane)}`). Reddit needs no change — src/components/reddit-drafts-review.tsx:220 already prefers the real thread title and only falls back to the internal formula name.
**Systems touched:** X/LinkedIn drafts readers, draft lane labelling, client-facing copy

---

## F136 · MEDIUM · Track A
**Screenshot:** screenshots/F136.png
**Title:** Task cards are a single column of tall blocks — roughly two fit on screen
**Where:** src/components/tasks-board.tsx (column/card layout); src/components/progress-view.tsx (board view wrapper)
**What you see:** Workspace / Task Board (/tasks). The board renders one wide column of cards, each with a large empty lower half. At a normal window size about two cards are visible; the 14 pending items need continuous scrolling. Status is a word ("Pending") rather than a column.
**Why wrong:** It is called a board but behaves as a list, so the one thing a board is for — seeing status distribution at a glance — is unavailable. The wasted vertical space inside each card is what pushes the count off screen.
**Fix per doc:** Give the card a fixed compact height (title, client chip, age, status dot) and lay the statuses out as real columns at wide widths, falling back to the current stacked list under the tablet breakpoint. Move the description behind hover or the ticket modal.
**Systems touched:** task board layout, progress view

---

## F71 · LOW · Track A
**Title:** Client-facing strings use a spaced hyphen where the rest of the UI uses an em dash — including the "drafts only, we never post" promise on all three agent pages
**Where:** src/app/(app)/clients/[id]/x-agent/page.tsx:32; src/app/(app)/clients/[id]/linkedin-agent/page.tsx:37; src/app/(app)/clients/[id]/reddit-agent/page.tsx:35; src/components/x-agent-intake.tsx:671; x-agent-intake.tsx:715; src/components/reddit-drafts-review.tsx:288; reddit-drafts-review.tsx:424; src/components/chatbot-widget.tsx:325; +4 further references in verification record
**What you see:** X / LinkedIn / Reddit agent pages and drafts readers, copilot dock, processing banner, onboarding (portal-wide). "Drafts only - a person always posts." · "Drafts only - we never post to Reddit, you post the reply yourself." · "Sent - it feeds the next run." · "Locked - a workspace build is already running" · "Your final version - the wording you will actually post." · "Logged - the reason tunes r/SaaS" · "Let's get your workspace set up - it only takes a minute."
**Why wrong:** The same product uses em dashes correctly in hundreds of other strings, so punctuation visibly changes voice between adjacent screens. A spaced hyphen reads as a typo or a stray minus sign rather than a clause break, and it lands in the highest-trust copy in the product: the "we never post for you" promise on all three agent pages.
**Fix per doc:** Hand-replace " - " with " — " in user-visible strings only. Highest value first: the three agent page descriptions (x-agent/page.tsx:32, linkedin-agent/page.tsx:37, reddit-agent/page.tsx:35), then the intake and drafts readers (x-agent-intake.tsx has 11 hits, linkedin-agent-intake.tsx 10, reddit-drafts-review.tsx 8, li-drafts-review.tsx 3, reddit-agent-intake.tsx 3), then chatbot-widget.tsx:325, onboarding-wizard.tsx:161/177, ai-processing-banner.tsx:22/41, and the REC_COPY descriptions in src/lib/seo-geo.ts:949, 952, 953, 956, 958. Use `/usr/bin/grep -rn -- ' - ' 'src/app/(app)' src/components` (about 57 real copy hits out of 107 raw matches — the rest are arithmetic like `b.createdAt - a.createdAt`, skip those). Two traps: ai-processing-banner.tsx:22 will NOT appear in that grep because the hyphen opens a JSX text node, and do not touch the em-dash ban inside generated post text documented in docs/x-agent-portal.md.
**Systems touched:** portal-wide copy/punctuation, agent pages, drafts readers, onboarding, seo-geo copy

---

## F72 · LOW · Track A
**Screenshot:** screenshots/F072.png
**Title:** One page, three different names: the staff sidebar says "Tasks", the client rail says "Workspace", the staff page header says "Task Board"
**Where:** src/components/sidebar.tsx:35; src/components/client-rail.tsx:72; src/app/(app)/tasks/tasks-body.tsx:70; tasks-body.tsx:108; src/components/notification-bell.tsx:264; src/components/client-home-overview.tsx:99
**What you see:** /tasks and /clients/[id]/tasks (sidebar, client rail, page header, notification bell, dashboard card). A client's left rail item reads "Workspace" and the page it opens is titled "Workspace" — but their dashboard's attention card says "Open items on your task board" and the notification bell footer says "View task board". For staff the same route renders a header reading "Task Board" while their sidebar item says "Tasks", and once they pick a client it becomes "Workspace".
**Why wrong:** Four names for one destination. A client is pointed at their "task board" and no such label exists anywhere in their navigation. Staff click "Tasks", arrive at "Task Board", then at "Workspace". Nobody can build a stable mental model, and support conversations get ambiguous.
**Fix per doc:** Standardize on "Workspace". src/components/sidebar.tsx:35 — `label: "Tasks"` becomes `label: "Workspace"`. src/app/(app)/tasks/tasks-body.tsx:108 — `<PageHeader title="Task Board" />` becomes `<PageHeader title="Workspace" description="Every client's board in one place." />` (the scoped view at line 69-72 is already "Workspace"). src/components/notification-bell.tsx:264 — "View task board →" becomes "View workspace →". src/components/client-home-overview.tsx:99 — hint "Open items on your task board." becomes "Open items on your workspace board." Leave the copilot's internal prompt text (src/lib/ai/prompts/proactive-assistant.ts) and the chat route strings alone unless you also relabel the copilot chip.
**Systems touched:** navigation labels, sidebar, client rail, notification bell, dashboard copy

---

## F73 · LOW · Track A
**Title:** The Activity tab's empty state promises clients "notes" that are filtered out of their view, and calls every agent run a "campaign"
**Where:** src/components/activity-timeline.tsx:374; activity-timeline.tsx:77; activity-timeline.tsx:45; activity-timeline.tsx:63; activity-timeline.tsx:228; src/components/progress-view.tsx:82
**What you see:** Client Workspace → Activity tab (/tasks). Empty state: "No activity yet — Events appear here as work is done: Intel Reports, campaigns, branding updates, and notes." Once populated, every agent run renders as "<Agent> campaign drafted" — including a single Reddit reply draft — and the research report row reads "Intel Report generated".
**Why wrong:** Two problems in one panel. Manual notes are stripped from the timeline for client accounts, so a client is told to expect the one thing they will never be shown, and "Intel Reports" is an internal product name that appears nowhere else in their portal. And "campaign" is used for every job, contradicting the "run", "deliverable" and "draft" vocabulary on every other client surface — one Reddit reply is not a campaign. The empty state also offers no next action.
**Fix per doc:** src/components/activity-timeline.tsx:374 — replace "Events appear here as work is done: Intel Reports, campaigns, branding updates, and notes." with "Every agent run, brand update, and competitor change shows up here as your team works." and, when `isStaff` is false (the flag already exists at line 342), add a link to `/clients/<clientId>/agents` labelled "Run an agent" — clientId is already a prop. Line 45 — replace `title: \`${j.agentName} campaign drafted\`` with `` `${j.agentName} delivered a draft` ``. Line 63 — replace "Intel Report generated" with "Research report ready". Do NOT bother editing EVENT_CONFIG.label (lines 101-161): those strings are never rendered.
**Systems touched:** activity timeline, client-facing copy

---

# Documents section (starts p118)

Section header: "Documents — Brand Voice, Market Strategy and the rest. Regenerate silently destroys every document and every client correction, and the viewer leaks the generator's own markup." 16 findings: #74, 75, 76, 77, 78, 79, 80, 81, 82, 83, 84, 85, 138, 86, 139, 140.

---

## F74 · HIGH · Track B
**Screenshot:** screenshots/F074.png
**Title:** "Correct Info" charges the client 2 credits and reports success even when the correction was thrown away
**Where:** src/lib/actions/intel-actions.ts:404 (chargeDocCorrection before the model call); intel-actions.ts:416 (`if (corrected.trim() === doc.content.trim()) return;` — silent early return); intel-actions.ts:381 (wrapper returns { ok: true } regardless); src/lib/intel/pipeline.ts:667 (`return currentContent;` when all structural recovery fails); src/components/correct-info-modal.tsx:53-58 (no error means onSuccess()); src/components/client-documents.tsx:352 (onSuccess closes the modal and the document)
**What you see:** Document viewer slide-over → Correct Info modal (any document, client or staff). You type "Our pricing is $49/mo, not $99/mo", hit Apply Correction, see "Applying correction to Product Information…", then the modal and the document both close as if it worked. Reopen the document and the old price is still there. Your credit balance has dropped by 2.
**Why wrong:** The correction helper returns the ORIGINAL content unchanged whenever the model's output fails a structural check (section count differs, or length is outside 75-140% of the original), and the action then early-returns without writing, without logging, and without telling the caller anything. The result handed back to the browser is a plain success, so the modal runs its success path: close modal, close document, refresh. The credit charge happens before the model call and there is no refund, so a client pays for a no-op. The modal's own promise — "Only the facts you name will change. Everything else stays identical." — becomes "nothing changed and you paid for it."
**Fix per doc:** Change applyTargetedDocCorrection in src/lib/actions/intel-actions.ts to return { changed: boolean } instead of void. On the `corrected.trim() === doc.content.trim()` branch at line 416, refund the charge with creditClientCredits({ kind: "refund", amount: CREDIT_COSTS.targetedCorrection, chargedAt }) from src/lib/data.ts:2060 (guarded by the same isBillableClientActor check chargeDocCorrection uses), and have applyTargetedDocCorrectionAction return { error: "We could not apply that correction safely — nothing was changed and you have not been charged. Try naming the fact more specifically." }. src/components/correct-info-modal.tsx already renders that string in its red error box at lines 118-123 and skips onSuccess.
**Systems touched:** doc correction (intel actions/pipeline), credits refund, correct-info modal

---

## F75 · HIGH · Track B
**Title:** After Regenerate or a correction, the staff sidebar keeps serving the OLD document — the buttons look like they did nothing
**Where:** src/lib/active-client-context.tsx:51 (setActiveClient inside the effect); active-client-context.tsx:56 (deps [client.id, contextDocs.length, competitors.length, isAdmin]); src/app/(app)/clients/[id]/layout.tsx:36-41 (ClientContextSync mounted for the whole client sub-tree); src/components/sidebar.tsx:385-390 (ClientDocuments reads activeClient.contextDocs and activeClient.client); src/components/client-documents.tsx:805-808 (Regenerate onSuccess only calls router.refresh()); client-documents.tsx:793-796 (Correct Info onDocUpdated only calls router.refresh())
**What you see:** Staff left sidebar Documents group while inside a client (any /clients/[id]/* page) — the only place staff can open these documents. Regeneration finishes, the modal closes, the page refreshes. You click Brand Voice in the sidebar and read the exact same text as before. Same after a correction: the document closes, updates, and reopens identical. It only changes after a full browser reload, or after leaving the client area entirely and coming back. The Regenerate button also fails to grey out while a fresh run is in flight.
**Why wrong:** The sidebar's documents come from a React context that is only re-seeded when the client id, the document COUNT, the competitor count, or the admin flag changes. Regenerate and Correct Info change document CONTENT, not the count, so the page refresh fetches fresh documents on the server and the effect never re-runs — the stale array stays in state. The same stale snapshot also carries the client record the Regenerate button reads its locked state from. The client layout stays mounted across the whole client sub-tree, which is exactly where these buttons live, so the stale window lasts the whole session. The code comment at the dependency list admits the gap.
**Fix per doc:** In src/lib/active-client-context.tsx, make the sync content-sensitive: replace contextDocs.length in the ClientContextSync dependency list (line 56) with a cheap signature such as contextDocs.map(d => `${d.id}:${d.version}`).join("|") — version is already bumped on every write by updateContextDocContent (src/lib/data.ts:1321) — and include the fields the sidebar reads off the client record (client.isAiProcessing, client.aiProcessingStartedAt). Alternatively drop the dependency list entirely and add a shallow-equality guard inside ActiveClientProvider's setActiveClient.
**Systems touched:** active-client context, sidebar documents, regenerate/correct-info flows

---

## F76 · HIGH · Track B
**Screenshot:** screenshots/F076.png
**Title:** Internal, staff-only analyst documents are shipped to the client's browser — and the client nav will show one whenever condensation drops a section
**Where:** src/app/(app)/layout.tsx:77 (listClientContextDocs(user.clientId) with no tier argument); layout.tsx:90 (full array passed to ClientRail); src/components/client-rail.tsx:1 ("use client", so those docs are serialized to the browser); src/components/client-documents.tsx:46-51 (pickDoc's internal-tier fallback); src/lib/types.ts:784-786 (tier rules: internal is admin/employee only, internal-only never published); src/lib/copilot-context.ts:52-54 (copilot prompt includes tier "internal"); src/app/api/clients/[id]/chat/route.ts:69 (chat route loads all tiers for a CLIENT_USER); src/lib/intel/condense.ts:90-93 (retry only checks the first and last section); +1 further reference in verification record
**What you see:** Client portal left rail Documents group and the client-facing Copilot dock. A client opens Brand Voice or Competitor Analysis and, in the fallback case, reads the internal analyst version: methodology notes, sourcing workflow, competitor-derogatory labels — exactly the content the client-facing rewrite exists to strip. Separately, the internal-only Action Plan and Guidelines documents never appear as rows, but their full text is sitting in the page data the client's browser already downloaded, and the Copilot answers client questions out of the internal versions.
**Why wrong:** The client portal shell loads every context document for the client with no tier filter and hands the whole array to a browser component, so internal and internal-only text is serialized into the page payload for a client user. On top of that, the document picker deliberately falls back to the internal-tier document whenever the client-tier copy has fewer top-level sections — and a dropped middle section is exactly the condensation hiccup the pipeline's own retry only partially catches (it checks the first and last section, not the middle). The type definition states the rule being broken: internal is admin/employee only, internal-only is never published. The Copilot compounds it by feeding internal-tier text into the client-facing system prompt.
**Fix per doc:** Three edits. (1) src/app/(app)/layout.tsx:77 — pass the tier for the client shell: listClientContextDocs(user.clientId, "client") (the parameter already exists at src/lib/data.ts:1242). (2) src/components/client-documents.tsx — gate pickDoc's internal fallback behind a new explicit prop (e.g. allowInternalFallback, true only from src/components/sidebar.tsx:385); for client viewers render a "This document is being rebuilt — check back shortly" row instead of the internal copy. (3) src/app/api/clients/[id]/chat/route.ts:69 — filter contextDocs to tier "client" when user.role === "CLIENT_USER" before passing them to buildCopilotSystemPrompt at line 109, mirroring the role-based asset filter already at line 107.
**Systems touched:** document tiers, client shell/layout, client documents picker, copilot chat route, data leak

---

## F77 · HIGH · Track B
**Screenshot:** screenshots/F077.png
**Title:** Regenerate destroys every document and every correction the client ever made, with no warning and no version history
**Where:** src/lib/data.ts:1301-1309 (replaceClientContextDocs deletes all rows for the client, then writes new ones); src/lib/intel/pipeline.ts:904 (the pipeline's only write is that wholesale replace); src/lib/intel/pipeline.ts:893-901 (every new doc written with `version: 1`); src/lib/actions/intel-actions.ts:432-441 (corrections logged to the feedbacks store via logFeedback); src/app/(app)/admin/analytics/page.tsx:56 (listFeedbacks has exactly one caller, and it is not the pipeline); src/components/client-documents.tsx:446 (Regenerate modal body text says nothing about replacement); src/components/client-documents.tsx:599 (Schedule modal describes the same run as recurring)
**What you see:** Documents nav group → Regenerate modal; and the recurring Schedule, which runs the same pipeline unattended. The Regenerate modal only says you may optionally add run-specific context for this regeneration. Nothing tells you the run deletes and re-creates every document. Corrections a client applied through Correct Info are gone afterwards, and there is nowhere in the product to see a previous version, a comparison, or even how many times a document has changed.
**Why wrong:** The pipeline's final step deletes every context-document row for the client in one batch and writes fresh ones stamped as version 1 — so the content, the version counter and every hand-applied correction are wiped together. Corrections are written to a separate feedback store, but nothing in the intel pipeline ever reads that store back (its only reader is the admin analytics page), so the next run has no memory of them and confidently restores the wrong facts. The Schedule modal makes this recurring and unattended, so a client's corrections evaporate on a cadence nobody warned them about.
**Fix per doc:** Two parts. (1) In the RegenerateModal body in src/components/client-documents.tsx (around line 446), add a plain sentence: "This replaces all documents. Corrections applied since the last run will be lost." (2) In runOnboardPipeline (src/lib/intel/pipeline.ts:726), load this client's feedback rows — add a data.ts reader alongside listFeedbacks that filters by clientId and scope single_doc/global — and append them to the per-document generation prompt as verified client ground truth, using the same ABSOLUTE GROUND TRUTH framing applyDocCorrections already applies to correction text. If a fuller fix is wanted, keep the prior row instead of deleting it in replaceClientContextDocs (src/lib/data.ts:1301) so a previous version survives.
**Systems touched:** document regeneration pipeline, context docs data layer, feedback store, regenerate/schedule modal copy, version history
