{
  "$id": "brief/newsletter_issue",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "issue_theme": { "type": "string", "maxLength": 500 },
    "must_include": {
      "type": "array",
      "maxItems": 20,
      "items": { "type": "string", "maxLength": 500 },
      "description": "Items the issue must cover (announcements, links, dates)."
    },
    "notes": { "type": "string", "maxLength": 4000 }
  }
}
